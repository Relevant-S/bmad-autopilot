# BMAD-extension audit

This document is the public audit surface that operationalizes the BMAD-extension discipline for the Automator. Its source authorities are FR64 (`_bmad-output/planning-artifacts/prd.md` line 899 — convention classification), FR65 (line 900 — public audit document maintenance), NFR-I4 (line 961 — BMAD-core-absorption migration behavior), ADR-002 (`_bmad-output/planning-artifacts/architecture.md` lines 99-204 — the 3×3 portability matrix that informs the `upstream-proposal` vs `automator-internal` boundary at the architectural level), ADR-003 (architecture.md lines 205-334 — and specifically the "Unknown-unknown skip-classes routing" elaboration at lines 279-292, which delegates the operational workflow this doc owns), and Pattern 7 (architecture.md lines 999-1005 — story-doc section adherence, which names `## QA Behavioral Plan` as an upstream-proposal story-doc section).

This doc IS authoritative for: the per-convention table; the five canonical principle paragraphs (epic-close marker sweep, remediation-shape, spike-with-bounded-timebox, atomic-vs-aggregated, finding-taxonomy bumps — the no-introductions principle); the skip-class recognition workflow; the per-convention-add procedure. This doc does NOT enumerate every marker class in `schemas/marker-taxonomy.yaml` (the taxonomy is the authoritative enumeration; this doc lists conventions, not markers); it does NOT re-derive ADR-002's 3×3 classification matrix (architecture.md is canonical for that); it does NOT specify implementation details for any individual convention (those live in the conventions' implementing FRs/NFRs).

The doc is **review-enforced**, not CI-enforced (per ADR-003 line 292) — it is a contributor-discipline artifact, not a CI gate.

---

## Classification taxonomy

Per FR64, every Automator-introduced convention must carry exactly one of three classifications. Multi-value, novel-value, and unclassified entries are forbidden — the extension-purity anti-metric (`_bmad-output/planning-artifacts/prd.md` line 103 — "≤ 20% automator-internal without an explicit revisit condition") measures against this single-value column.

- **`automator-internal`** — an implementation detail that lives at the Automator's wrapper layer (orchestrator, hook scripts, run-state schema, envelope fields) and does NOT extend the BMAD-core lifecycle, story-doc template, or upstream conventions. Choose this when the convention is private to the Automator's mechanism and would have no meaning outside it. Examples: orchestrator wrapper flags, internal envelope fields that wrap (without reshaping) existing BMAD-core taxonomies, run-state field naming. Migration plan: `N/A — internal-only`. NFR-I4 does not apply (the convention is never absorbed upstream because it has no upstream-relevant surface).
- **`upstream-proposal`** — an extension that reshapes a BMAD-core surface (lifecycle states, story-doc sections, architectural patterns) and is a candidate for upstream RFC submission to BMAD core. Choose this when the convention WOULD have meaning in BMAD core and where the architectural pathway is "ship in the Automator first; if BMAD core absorbs it, follow NFR-I4's migration." Migration plan: populated per NFR-I4's four-stage flow — **acknowledgment** (BMAD core ships an equivalent feature) → **adapter window** (both paths work simultaneously) → **deprecation** (the Automator's path is marked deprecated with a release-notes flag) → **removal** (the Automator's path is removed in a subsequent minor release).
- **`research-needed`** — the classification is genuinely undecided pending upstream decision, primitive stability, or community input. Choose this when neither `automator-internal` nor `upstream-proposal` is honest yet and the team has bounded the research via the spike-with-bounded-timebox pattern (see § Research-blocker handling). Migration plan: `pending research conclusion`. The classification MUST resolve to one of the other two before MVP close — see the per-release re-audit cadence in § Skip-class recognition workflow.

The choice constrains future migration behavior per NFR-I4: classifying a convention as `upstream-proposal` is a commitment to ship adapter behavior when BMAD core absorbs the equivalent feature; classifying as `automator-internal` is a commitment that the convention is never absorbed (and so misclassifying an absorbable convention as `automator-internal` traps the project on a forked path). Honest classification at introduction time is therefore a load-bearing engineering decision, not a documentation chore.

---

## Per-convention table

The five MVP-close seed entries are listed in chronological-introduction order (the order they were named in the architecture / PRD). Routine adds to this table are append-only — existing rows are not reordered, so the diff history doubles as the audit trail and PR review remains tractable. Each entry carries a single classification value drawn from the 3-tier set; entries are NEVER classified with multiple values, NEVER classified with novel values, NEVER left unclassified.

| Convention name | Classification | Rationale | Migration plan | Revisit conditions |
|---|---|---|---|---|
| `qa` lifecycle state (between `review` and `done`) | `upstream-proposal` | Behavioral verification is a load-bearing seam between code review and merge — not a sub-state of `review`. The `qa` lifecycle state is named in PRD FR23 (`_bmad-output/planning-artifacts/prd.md` line 838) and consumed by Story 2.4. If BMAD core absorbs the lifecycle extension, the Automator follows NFR-I4. | Per NFR-I4: **acknowledgment** — BMAD core ships a `qa` (or equivalently-named) lifecycle state in the canonical state machine; **adapter window** — the Automator's orchestrator accepts both the BMAD-core state name and the Automator's, normalizing on read; **deprecation** — the Automator's standalone state is flagged in release notes with the BMAD-core mapping; **removal** — the standalone state is removed once users have migrated within the documented adapter window. | BMAD core ships a `qa` (or equivalently-named) lifecycle state (NFR-I4 acknowledgment trigger); or upstream RFC is rejected (classification flips to `automator-internal` with a documented forked-path commitment). |
| `## QA Behavioral Plan` story-doc section | `upstream-proposal` | Plan-driven AC iteration with persistence (PRD FR23, line 838) needs a documented story-doc section so the plan survives across re-runs and AC-hash drift detection has a stable target. Pattern 7 (architecture.md lines 999-1005) names this section as an upstream proposal. If BMAD core adopts the section, the Automator follows NFR-I4. | Per NFR-I4: **acknowledgment** — BMAD core's story-doc template adds the section; **adapter window** — the Automator writes/reads at both the BMAD-core-canonical heading and its own; **deprecation** — the Automator-canonical heading is flagged with mapping notes; **removal** — the Automator-canonical heading is removed once usage migrates. | BMAD core's story-doc template adds the section (NFR-I4 acknowledgment trigger); or upstream RFC is rejected. |
| `retry_mode: fix-only` Dev invocation flag | `automator-internal` | An implementation-detail flag living at the orchestrator-Dev wrapper layer (PRD FR10, line 821 — Orchestrator applies a capability-level fix-only constraint to Dev's invocation). It is NOT a BMAD-core lifecycle change; it is mechanism, not contract. | N/A — internal-only. | BMAD core ships an equivalent retry-scope mechanism. |
| Seam-transition orchestrator pattern (sensor-not-advisor envelopes + flow-policy-in-orchestrator) | `upstream-proposal` | The architectural pattern that makes multi-specialist coordination work without shared state — specialists return schema-validated envelopes describing what they did/saw; the orchestrator owns flow policy. Long-term-vision candidate for upstream RFC per ADR-002's cell-1 stability commitment (architecture.md lines 99-204). | Per NFR-I4: **acknowledgment** — BMAD core ships a canonical seam-transition pattern; **adapter window** — the Automator's orchestrator can both produce and consume BMAD-core's canonical envelopes; **deprecation** — the Automator's standalone envelope shape is flagged with mapping notes; **removal** — the Automator's standalone shape is removed once consumers migrate. | BMAD core ships a canonical seam-transition pattern (NFR-I4 acknowledgment trigger); or upstream RFC is rejected. |
| `failed_layers` graceful-degradation field on Review-BMAD envelope | `automator-internal` | Wraps `bmad-code-review`'s existing 3-layer taxonomy (Blind Hunter / Edge Case Hunter / Acceptance Auditor) per PRD FR28 (line 847) without reshaping it. The field is mechanism for the Automator's per-layer failure surface, not an extension of BMAD-core's review contract. | N/A — internal-only. | BMAD core ships a layer-failure-tracking field on the review envelope. |
| `meta: review-completeness` field on Review-BMAD synthetic findings | `automator-internal` | Story 3.3 (`failed_layers` graceful-degradation three-channel atomic emission per FR28 + FR56) pairs each failed review layer with one synthetic `decision_needed: HIGH` finding emitted by `surface_failed_layers` so the human reviewer at PR-bundle reading time sees the per-layer failure alongside the `failed_layers` envelope-field declaration AND the `review-layer-failed` marker comment. The `meta` field is the wrapper-layer-only discriminator that distinguishes wrapper-generated meta-findings (whose presence signals review incompleteness) from layer-produced content findings (whose presence signals AC violations); without the discriminator, downstream tooling (Story 3.4's PR-bundle review-section rendering at epics.md line 1729) cannot filter the two finding kinds apart. The field is mechanism for the Automator's per-layer failure surface — by analogy to the `failed_layers` row above — not an extension of BMAD-core's review contract; BMAD core has no upstream-relevant analogue at MVP. The schema enum is intentionally narrow (`[review-completeness]` — single value); future meta-finding kinds (e.g., the Story 4.9 `verification_mode: exploratory-heuristic` precedent at epics.md line 2068) extend the enum via a follow-on schema bump recorded as a new row below this one per the append-only discipline. | N/A — internal-only. | BMAD core ships an equivalent meta-finding mechanism on the review envelope, OR BMAD core's review surface adopts the synthetic-finding pattern for failed-layer surfacing. |
| `plan_drift` optional top-level field on QA envelope (Story 4.2 — AC-hash plan-drift diagnostic-context carrier) | `automator-internal` | Story 4.2 (AC-hash plan-drift detection + `plan_status` reset per FR23) consumes Story 4.1's `drift-suspected` `PlanPersistAction` token AS-IS and surfaces drift through a NEW pure-library `qa_plan_drift.surface_plan_drift` two-channel atomic emission: (channel 1) a freshly-regenerated `QABehavioralPlan` with `plan_status` reset to `generated` even from `human-reviewed` per epics.md line 1845, (channel 2) a `plan-drift-detected` marker emission record (the marker class itself was added proactively by Story 1.4 at `schemas/marker-taxonomy.yaml` line 114; consumed AS-IS — NOT a new marker class). The `plan_drift` envelope-field is the structural carrier the QA wrapper writes to so the bundle assembler's QA-section render can surface the `### Plan drift detected` H3 sub-section + the structured marker comment co-located. The shape is the four-field diagnostic context named verbatim by epics.md line 1851 (`story_id` string, `prior_plan_status` ∈ {`generated`, `human-reviewed`} mirroring Story 4.1's `PlanStatus` Literal, `prior_ac_hash` 64-char SHA-256 hex per architecture.md line 1591, `current_ac_hash` same shape) under `additionalProperties: false`. Field is OPTIONAL (absent on no-drift / `write-new` / `reuse-existing` runs; present-and-object on `drift-suspected` runs — the optional-non-null idiom). The field is mechanism for the Automator's wrapper-side drift-detection surface — by analogy to the `failed_layers` Review-BMAD-specific extension at envelope.schema.yaml lines 92-97 — NOT an extension of BMAD-core's QA envelope contract; BMAD core has no upstream-relevant analogue at MVP because BMAD core does not own a behavioral-verification specialist. Consumers: the QA wrapper at `agents/qa.md` Procedure step 6's `drift-suspected` branch writes the field; the bundle assembler at `tools/loud-fail-harness/src/loud_fail_harness/bundle_assembly.py`'s `_render_per_ac_section` + `_render_qa_plan_drift_subsection` reads the field and emits the `### Plan drift detected` H3 sub-section + the per-emission `validate_marker_emission` defense-in-depth call. | N/A — internal-only. | BMAD core ships an equivalent behavioral-verification envelope surface that includes drift-detection diagnostic carriage. |
| `provisioned_env` optional top-level field on run-state (Story 4.3 — orchestrator-owned env-lifecycle record) | `automator-internal` | Story 4.3 (full env provisioning lifecycle, orchestrator-owned per FR7 + NFR-S6) consumes the `env-setup-failed` and `orphan-process-cleanup` marker classes from `schemas/marker-taxonomy.yaml` lines 102 + 172 AS-IS (no taxonomy bump) and the `env-provisioned` + `env-torn-down` event classes from `schemas/orchestrator-event.yaml` lines 92-94 + 302-346 AS-IS (no event-schema bump), and surfaces the per-story env state through a NEW pure-library substrate module at `tools/loud-fail-harness/src/loud_fail_harness/env_provisioning.py` exposing the four env-lifecycle primitives (`allocate_ephemeral_port`, `provision_env`, `teardown_env`, `cleanup_orphan_processes`) + the two-channel atomic emission helper `surface_env_setup_failure`. The `provisioned_env` run-state field is the structural carrier the orchestrator skill writes to so the QA dispatch payload can surface the provisioned env into QA's input channel without QA ever provisioning it itself (preserving FR7's invariant that env lifecycle is orchestrator-owned, NOT QA-owned). The shape is the four-field record named by Story 4.3 AC-6 (`env_kind` ∈ {web, api} mirroring the orchestrator-event schema's existing `env_kind` enum byte-for-byte, `port` integer ∈ [1, 65535], `pid` integer ≥ 1, `started_at` ISO-8601 timestamp) under `additionalProperties: false`. Field is OPTIONAL (absent on `current_state ∈ {ready-for-dev, in-progress, review, done}` AND between `teardown_env` success and the next story's provisioning; present-and-object only between `provision_env` success and `teardown_env` success — the optional-non-null idiom). The dev's-call YAML idiom recorded here (per AC-6 of Story 4.3): on teardown the field is REMOVED from the document (`dict.pop("provisioned_env", None)`) rather than set to `null`; the bumped schema's `oneOf: [{$ref}, {type: "null"}]` accepts both forms (absent OR null), and removal produces cleaner YAML on inspection by the practitioner per NFR-O2. The dev's-call sweep-event `env_kind` (per AC-5 of Story 4.3): `cleanup_orphan_processes` emits a single sweep-summary `env-torn-down` event with `outcome="orphan-process-cleanup"`, NOT one event per orphan; the schema's per-class shape requires `env_kind` so the caller passes a single value at this seam (`env_kind` is taken from the orchestrator-skill-side knowledge of the project type from `qa-runbook.yaml`). The field is mechanism for the Automator's wrapper-side env-provisioning surface — by analogy to the `last_envelope` and `pending_qa_dispatch_payload` optional top-level fields already in `schemas/run-state.yaml` — NOT an extension of BMAD-core's lifecycle contract; BMAD core has no upstream-relevant analogue at MVP because BMAD core does not own a behavioral-verification specialist nor an orchestrator-owned env lifecycle. Consumers: `loud_fail_harness.env_provisioning.provision_env` writes the field; `loud_fail_harness.env_provisioning.teardown_env` clears the field; the LLM-runtime protocol at `skills/bmad-automation/steps/env-provisioning.md` (Story 4.3 NEW) reads the field at QA dispatch time + after QA return; downstream consumers Story 4.4 (web Playwright provisioner), Story 4.5 (API HTTP provisioner), Story 4.10 (env-setup-fail escalation routing), and Story 4.12 (evidence-persistence size budgets) build on this carrier without modifying the field shape. | N/A — internal-only. | BMAD core ships an equivalent orchestrator-owned env-lifecycle surface that includes provisioned-env state carriage. |
| `steps/qa-driver-playwright.md` LLM-runtime protocol + the `qa-runbook.yaml` `dev_server_command` / `masked_selectors` field consumption + the `[REDACTED]` masked-selector sentinel (Story 4.4 — project-type-specific web Playwright MCP driver substrate) | `automator-internal` | Story 4.4 (Playwright MCP driver for `web` project type per FR17 + ADR-002 graceful-degrade) consumes the `playwright-mcp-unavailable` marker class from `schemas/marker-taxonomy.yaml` line 226 AS-IS (no taxonomy bump), the `playwright-launch-failed` sub-classification of `env-setup-failed` from line 111 AS-IS, the `$defs/ac_result` envelope shape at `schemas/envelope.schema.yaml` lines 164-194 AS-IS, and the `playwright-mcp` dependency declaration at `schemas/dependencies.yaml` lines 99-110 AS-IS, and lands a NEW pure-library substrate module at `tools/loud-fail-harness/src/loud_fail_harness/playwright_driver.py` exposing the project-type-specific (web) driver primitives (the `PlaywrightProvisioner` Provisioner-Protocol implementation that plugs into Story 4.3's `provision_env`, the `PlaywrightTeardown` Teardown-Protocol implementation, the seven `WebDriver` Protocol actions per epics.md line 1904, the per-AC `verify_ac` primitive, the `MaskedSelectorPolicy` redaction, the mid-run `surface_playwright_mcp_unavailable` two-channel atomic emission). The NEW step file at `skills/bmad-automation/steps/qa-driver-playwright.md` is the LLM-runtime protocol the QA wrapper composes against at AC-iteration time for `web` project types: it names the seven `WebDriver` Protocol method ↔ `mcp__playwright__browser_*` tool mappings (the substrate library is pure Python and CANNOT itself invoke MCP tools — that is structurally an LLM-runtime concern bound to the QA wrapper's prompt-execution context per ADR-004's substrate-vs-LLM-runtime split), the `PlaywrightAvailabilityProbe` + `DevServerRunner` runtime bindings, the mid-run `playwright-mcp-unavailable` failure-mode procedure, the at-provisioning `playwright-launch-failed` failure-mode routing chain through Story 4.3's `surface_env_setup_failure`, and the `MaskedSelectorPolicy` runtime application. The `qa-runbook.yaml` field consumption recorded here (per AC-7 of Story 4.4): the LLM-runtime protocol reads `dev_server_command` (a single string command line consumed by the `DevServerRunner.start(port)` runtime binding to `subprocess.Popen` of the dev-server command) AND `masked_selectors` (a list of CSS selectors consumed by `MaskedSelectorPolicy(masked_selectors=tuple(...))` to redact sensitive content from screenshots / DOM snapshots / network traces BEFORE persisting under `_bmad-output/qa-evidence/{story-id}/{run-id}/` per FR49). Story 7.5 will scaffold the runbook stub; until Story 7.5 lands, the practitioner maintains the runbook manually. The dev's-call `[REDACTED]` masked-selector sentinel (per AC-1 of Story 4.4): the substrate's `MASKED_REDACTION_SENTINEL = "[REDACTED]"` is the literal string substituted for masked-selector content per the verbatim epic AC at epics.md line 1917 ("evidence files respect masked-selector configuration if present in qa-runbook.yaml"). The sentinel string is short enough that redaction does not inflate evidence size meaningfully; Story 4.12's evidence-persistence size budgets read the post-redaction output. The convention is mechanism for the Automator's wrapper-side web-driver surface — by analogy to the `provisioned_env` run-state field row above — NOT an extension of BMAD-core's QA envelope contract; BMAD core has no upstream-relevant analogue at MVP because BMAD core does not own a project-type-specific behavioral-verification driver substrate. Consumers: `loud_fail_harness.playwright_driver.PlaywrightProvisioner` is composed by `loud_fail_harness.env_provisioning.provision_env` at provision-time (Story 4.3); `loud_fail_harness.playwright_driver.verify_ac` is composed by the QA wrapper at AC-iteration time (Story 4.13 forthcoming wrapper thickening); `loud_fail_harness.playwright_driver.surface_playwright_mcp_unavailable` is composed by the QA wrapper on mid-run MCP-tool-call exceptions; `loud_fail_harness.playwright_driver.MaskedSelectorPolicy` redaction is applied by `EvidenceCapturer` implementations BEFORE persisting; downstream consumers Story 4.5 (API HTTP driver — sibling pure-library module), Story 4.6 (plan-driven AC iteration consumes `verify_ac`), Story 4.10 (escalation routing consumes `PlaywrightMcpUnavailableEmission`), Story 4.12 (evidence persistence reads the post-redaction output), Story 4.13 (QA wrapper completion thickens `agents/qa.md`) build on this convention without modifying the field shape. | N/A — internal-only. | BMAD core ships an equivalent project-type-specific behavioral-verification driver surface (web Playwright + API HTTP). |
| `steps/qa-driver-http.md` LLM-runtime protocol + the `qa-runbook.yaml` `api_server_command` field consumption + the AS-IS reuse of `MASKED_REDACTION_SENTINEL` / `MaskedSelectorPolicy` / `AcResult` / `EvidenceCapturer` / `NoOpEvidenceCapturer` from Story 4.4's `playwright_driver` + the no-new-emission-helper structural choice for the mid-run "API broken" path (Story 4.5 — project-type-specific api HTTP driver substrate) | `automator-internal` | Story 4.5 (HTTP api driver for `api` project type per FR17) consumes the `dev-server-not-ready` sub-classification of `env-setup-failed` from `schemas/marker-taxonomy.yaml` line 112 AS-IS (no taxonomy bump — both at-init "API server failed to launch" and mid-run "API service broken" paths share the same sub_cause per the remediation-shape principle in this document's § Marker class boundaries; both routes share the remediation surface "the underlying server process is not responding"), the `$defs/ac_result` envelope shape at `schemas/envelope.schema.yaml` lines 164-194 AS-IS via direct import of Story 4.4's `AcResult`, and the `playwright-mcp.by_project_type.api.profiles.*.profile: opt-in-skip` declaration at `schemas/dependencies.yaml` lines 111-116 AS-IS (api project type has NO Playwright-MCP dependency; HTTP uses Python stdlib `http.client`), and lands a NEW pure-library substrate module at `tools/loud-fail-harness/src/loud_fail_harness/http_driver.py` exposing the project-type-specific (api) driver primitives (the `HttpProvisioner` Provisioner-Protocol implementation that plugs into Story 4.3's `provision_env` with runner-then-probe ordering OPPOSITE of Story 4.4's `PlaywrightProvisioner`, the `HttpTeardown` Teardown-Protocol implementation, the four `ApiDriver` Protocol actions per epics.md line 1938 plus `inspect_network_trace`, the per-AC `verify_ac` primitive, the api-side `_apply_api_masked_selector_policy` extension, the `ApiServerNotReady` at-init exception, the `ApiServiceBroken` mid-run exception). The NEW step file at `skills/bmad-automation/steps/qa-driver-http.md` is the LLM-runtime protocol the QA wrapper composes against at AC-iteration time for `api` project types: it names the four `ApiDriver` Protocol method ↔ Python-stdlib `http.client` mappings, the `ApiAvailabilityProbe` + `ApiServerRunner` runtime bindings, the mid-run "API broken" failure-mode procedure, the at-provisioning `dev-server-not-ready` failure-mode routing chain through Story 4.3's `surface_env_setup_failure`, the FR24a verification-fail vs FR24b env-setup-fail distinction (separate code paths, separate envelopes per epics.md line 1947), and the api-side `MaskedSelectorPolicy` runtime application. The `qa-runbook.yaml` field consumption recorded here (per AC-7 of Story 4.5): the LLM-runtime protocol reads `api_server_command` (a single string command line consumed by the `ApiServerRunner.start(port)` runtime binding to `subprocess.Popen` of the api-server command with `start_new_session=True` per the cross-driver convention) AND `masked_selectors` (consumed by the same `MaskedSelectorPolicy` shape Story 4.4 codified — REUSED AS-IS via direct import from `playwright_driver`). The AS-IS reuse of `MASKED_REDACTION_SENTINEL` (`"[REDACTED]"`), `MaskedSelectorPolicy`, `AcResult`, `EvidenceCapturer`, and `NoOpEvidenceCapturer` from Story 4.4's `playwright_driver` is recorded here because Story 4.4 was the canonical declaration site (Story 4.4 landed first in build order); these primitives are project-type-agnostic and Story 4.5 imports them at module-load time without re-declaring or modifying them — a future refactor MAY relocate them to a shared module, but Story 4.5 does NOT perform that refactor (scope discipline). The api-side `_apply_api_masked_selector_policy` extension WRAPS Story 4.4's `_apply_masked_selector_policy` for the CSS-selector branch AND ADDS plain-text-selector matching for HTTP header values (case-insensitive on the header-name match), JSON-body field-name patterns, and query-string parameter names — without modifying Story 4.4's algorithm. The no-new-emission-helper structural choice (per AC-6 of Story 4.5): the mid-run "API broken" path re-uses Story 4.3's existing `surface_env_setup_failure` AS-IS because the marker class is the existing `env-setup-failed` from Story 1.4 / Story 4.3, NOT a new class. The asymmetry vs Story 4.4 — which DID introduce `surface_playwright_mcp_unavailable` because its mid-run path needed a structurally distinct `playwright-mcp-unavailable` marker class (the remediation differs: "transient MCP-tool-surface connectivity loss" vs "API server not responding") — is the canonical example of the per-marker remediation-shape principle: marker classes are remediation-shaped, not emission-point-shaped; two routes with the same remediation surface share the marker class. The convention is mechanism for the Automator's wrapper-side api-driver surface — by analogy to Story 4.4's web-driver row above — NOT an extension of BMAD-core's QA envelope contract; BMAD core has no upstream-relevant analogue at MVP. Consumers: `loud_fail_harness.http_driver.HttpProvisioner` is composed by `loud_fail_harness.env_provisioning.provision_env` at provision-time for `api` project types (Story 4.3); `loud_fail_harness.http_driver.verify_ac` is composed by the QA wrapper at AC-iteration time (Story 4.13 forthcoming wrapper thickening); the QA wrapper's `try / except ApiServiceBroken` clause calls `loud_fail_harness.env_provisioning.surface_env_setup_failure` AS-IS (no new emission helper); `loud_fail_harness.http_driver._apply_api_masked_selector_policy` is applied to trace excerpts BEFORE they reach the `EvidenceCapturer` so sensitive header values / JSON-body fields / query-string parameters are redacted regardless of whether the capturer's own implementation handles api-side patterns; downstream consumers Story 4.6 (plan-driven AC iteration consumes `verify_ac` for `api` project types), Story 4.10 (escalation routing consumes `EnvSetupFailureEmission` AS-IS), Story 4.12 (evidence persistence reads the post-redaction output AND the per-trace-record `_TRACE_BODY_EXCERPT_MAX_CHARS` body excerpt bound), Story 4.13 (QA wrapper completion thickens `agents/qa.md`) build on this convention without modifying the field shape. | N/A — internal-only. | BMAD core ships an equivalent project-type-specific behavioral-verification driver surface (web Playwright + API HTTP). |
| `tools/loud-fail-harness/src/loud_fail_harness/qa_ac_iteration.py` substrate module + the `smoke-first-abort` marker class AS-IS reuse from `marker-taxonomy.yaml` line 188 + the no-new-step-file structural choice + the no-`agents/qa.md`-modification structural choice (Story 4.6 — plan-driven AC iteration framework with smoke-first ordering) | `automator-internal` | Story 4.6 (plan-driven AC iteration with smoke-first abort per FR22b) consumes the `smoke-first-abort` marker class from `schemas/marker-taxonomy.yaml` line 188 AS-IS (no taxonomy bump — Story 1.4's proactive add per the epic-close marker sweep already enumerated the class with the canonical `diagnostic_pointer` "fix the smoke-failing surface, then re-run QA"), the `$defs/ac_result` envelope shape at `schemas/envelope.schema.yaml` lines 164-194 AS-IS via direct import of Story 4.4's `AcResult` (the existing schema's top-level `ac_results: array` already accepts ≥ 1 entries; multi-AC is exercised by the new fixtures without schema bump), Story 4.4's `playwright_driver.verify_ac` (web project type) AS-IS, Story 4.5's `http_driver.verify_ac` (api project type) AS-IS, Story 4.4's `WebDriver` / `EvidenceCapturer` / `MaskedSelectorPolicy` Protocols AS-IS via direct import (project-type-agnostic primitives whose canonical declaration site Story 4.4 codified), and Story 4.5's `ApiDriver` Protocol AS-IS via direct import; and lands a NEW pure-library substrate module at `tools/loud-fail-harness/src/loud_fail_harness/qa_ac_iteration.py` exposing the iteration primitives (`SMOKE_FIRST_ABORT_MARKER` constant, the `ProjectType` Literal alias, the four frozen Pydantic models `SmokeFirstAbortDiagnosticContext` / `SmokeFirstAbortEmissionRecord` / `SmokeFirstAbortEmission` / `AcIterationResult`, the `PlanAbsentForIteration` exception, the `surface_smoke_first_abort` Pattern-5 atomic-on-failure emission helper, and the `iterate_acs` main composition function). The no-new-step-file structural choice (per AC-10 of Story 4.6): THIS story does NOT add a new step file. The iteration framework has no LLM-runtime tool surface — pure-Python composition of Story 4.4's `qa-driver-playwright.md` and Story 4.5's `qa-driver-http.md` step files at AC-iteration time. The two existing step files already declare Story 4.6 as a forward consumer (the verbatim line at `qa-driver-playwright.md` line 102: "Story 4.6 — plan-driven AC iteration framework consumes `verify_ac` at iteration time"; analogous in `qa-driver-http.md`); THIS story closes the forward-pointer commitment without introducing new tool-surface bindings. The asymmetry vs Stories 4.4 / 4.5 (which DID introduce step files because their per-driver primitives bind to MCP tools / `http.client` calls — both are LLM-runtime tool-surface bindings) is structurally distinct: drivers bind to runtime tools; the iteration framework is pure-Python composition. The no-`agents/qa.md`-modification structural choice (per AC-12 of Story 4.6): THIS story does NOT modify `agents/qa.md`. The QA wrapper's existing forward-pointer at line 34 ("Stories 4.6 + 4.7 thicken the wrapper IN PLACE to plan-driven AC iteration") IS the in-place-thickening commitment THIS story preserves. Story 4.13 owns wrapper thickening completion (composes `iterate_acs` once at the wrapper's "Drive the running product per `project_type`" step; consumes the `AcIterationResult` for envelope projection). Modifying `agents/qa.md` at THIS story would (a) violate the in-place thickening invariant codified at `architecture.md` lines 1469-1485, (b) duplicate work Story 4.13 owns, (c) risk a wrapper that composes against `iterate_acs` BEFORE Story 4.7's triple-enforcement schema lands. The asymmetry vs Stories 4.4 / 4.5 (which ALSO did NOT modify `agents/qa.md`) preserves the "all-at-once thickening" cohesion at Story 4.13. The convention is mechanism for the Automator's wrapper-side iteration surface — by analogy to Story 4.5's row above — NOT an extension of BMAD-core's QA envelope contract; BMAD core has no upstream-relevant analogue at MVP because BMAD core does not own a plan-driven behavioral-verification framework with smoke-first ordering. Consumers: `loud_fail_harness.qa_ac_iteration.iterate_acs` is composed by the QA wrapper at AC-iteration time (Story 4.13 forthcoming wrapper thickening); `loud_fail_harness.qa_ac_iteration.surface_smoke_first_abort` is invoked by `iterate_acs` itself when AC-1 returns `status="fail"`; the resulting `AcIterationResult.smoke_first_abort: SmokeFirstAbortEmissionRecord \| None` is consumed by Story 4.10 (escalation routing — `verification-fail` contract) and Story 4.11 (PR-bundle render of the smoke-first-abort H3 sub-section + structured marker comment); downstream consumers Story 4.7 (AC-assertion-evidence triple structural enforcement at the schema level) thickens the `AcResult` validation that `verify_ac` produces, Story 4.8 (three-tier evidence hierarchy), Story 4.9 (three exploratory heuristics + `verification_mode` field), Story 4.13 (QA wrapper completion thickens `agents/qa.md`) build on this convention without modifying the `iterate_acs` API shape. The new fixture `qa-fail-smoke-first-abort.yaml` is the canonical envelope-corpus demonstration of the `smoke-first-abort` wrapper shape; the marker class was already covered pre-Story-4.6 by `examples/synthetic-stories/smoke-first-abort.md` (Story 1.7), so `fixture-coverage` reports 29 covered classes both pre- and post-Story-4.6 (envelope corpus grows from 20 to 23; synthetic-story corpus and covered-class count are unchanged). | N/A — internal-only. | BMAD core ships an equivalent plan-driven behavioral-verification iteration framework with smoke-first ordering AND structural emission of an analogous `smoke-first-abort` (or equivalently-named) marker class. |
| `$defs/ac_result` `if/then` conditional invariant in `schemas/envelope.schema.yaml` (Story 4.7 — AC-assertion-evidence triple structural enforcement) | `automator-internal` | Story 4.7 (FR19 — `_bmad-output/planning-artifacts/prd.md` line 833 — "every passing AC has at least one assertion AND at least one evidence reference; AC-assertion-evidence triple — structurally enforced, not optional") consumes `Draft202012Validator` jsonschema infrastructure AS-IS (no validator-version bump — JSON-Schema-2020-12 supports `if`/`then` natively at `envelope_validator.py` line 46), the existing `$defs/ac_result` shape at `schemas/envelope.schema.yaml` lines 164-194 AS-IS (no field renames; only an `allOf` containing one `if/then` clause is added as a sibling of the `properties` block), the existing `envelope-validator` CLI surface AS-IS (no new entry-point declaration in `pyproject.toml`; no new harness module added under `tools/loud-fail-harness/src/loud_fail_harness/` — the only code change is `format_errors`'s diagnostic-rewriting branch + an optional `envelope: dict | None = None` keyword argument for AC-id resolution), the existing 23-fixture envelope corpus AS-IS (NO new envelope fixtures — every entry with `status: pass` across the 11 `qa-*.yaml` fixtures already complies with the triple invariant per the corpus-inspection sweep at story-create time; the `qa-fail-mid-iteration-continue.yaml` mixed-status fixture confirms the conditional applies per-entry, not per-envelope), the existing `test_envelope_validator.py` contract-coverage matrix discipline AS-IS (NO new test file — the new ≥6 negative-path tests are appended to the existing module under two new docstring-matrix headings, preserving the contract-coverage-matrix-as-canonical-aggregator pattern), and the existing `_minimal_valid_envelope` test-helper convention AS-IS (a new `_minimal_qa_envelope` helper is added alongside `_minimal_finding` to reduce per-test boilerplate). The schema-level enforcement choice (per AC-1 of Story 4.7): the conditional `if: { properties: { status: { const: pass } }, required: [status] }, then: { properties: { assertions: { minItems: 1 }, evidence_refs: { minItems: 1 } } }` lives INSIDE `$defs/ac_result` as a sibling of `properties` so it modulates the existing field declarations without restating them; the `then` branch adds ONLY `minItems` constraints (the `required` keys are already declared at the top-level `required` list at lines 168-171); the base `properties.assertions` / `properties.evidence_refs` declarations stay UNCHANGED — they remain `type: array, items: { type: string }` without a top-level `minItems` constraint, so failing-AC and blocked-AC entries are still allowed to carry empty arrays per the verbatim epic AC at `epics.md` lines 2009-2012 ("a failing AC result… the schema does not require ≥ 1 assertion or ≥ 1 evidence ref… failures may have partial evidence, which is fine for diagnostic purposes"); the conditional's `required: [status]` reinforcement inside the `if` clause is the JSON-Schema idiom for "this conditional applies WHEN the property is present" so an `ac_result` lacking `status` does not vacuously match `if` and trigger `then`. The `format_errors` AC-id-aware diagnostic refinement (per AC-7 / AC-8 of Story 4.7): when a `ValidationError` has `validator == "minItems"` AND its `absolute_path` matches `["ac_results", <int>, "assertions" | "evidence_refs"]`, the rendered diagnostic line names the FR19 invariant explicitly (`"AC-assertion-evidence triple invariant: passing AC requires ≥ 1 entry in `<array_name>` (see FR19)"`) rather than emitting the raw `"is too short"` jsonschema phrasing; when the optional `envelope: dict | None` keyword argument is non-None, the rewriter resolves the offending entry's `ac_id` via `envelope["ac_results"][index]["ac_id"]` defensively (try/except `(KeyError, IndexError, TypeError)`; falls back to the index form `ac_results[N]` on any structural mismatch to avoid masking the original validation error with a downstream diagnostic-formatter bug); the CLI's `main()` is threaded to pass the parsed envelope through to `format_errors` via an inlined YAML load (mirrors `validate_file`'s behavior; preserves the existing harness-level error semantics for OSError / YAMLError / non-mapping shapes). The no-`agents/qa.md`-modification structural choice (per AC-13 of Story 4.7): THIS story does NOT modify `agents/qa.md`. Story 4.13 owns wrapper thickening completion (composes the iteration framework + triple-conformant envelope projection per the verbatim epic AC at `epics.md` lines 2192-2193 — "the agent (same identity as Story 2.10) reflects the thickened FR16-FR25 surface — full AC iteration with smoke-first ordering (4.6), three-tier evidence (4.8), AC-assertion-evidence triple (4.7) ..."). Modifying `agents/qa.md` at THIS story would (a) violate the in-place thickening invariant codified at `architecture.md` lines 1469-1485 (the QA wrapper's surface stays put until Story 4.13 closes the thickening), (b) risk a wrapper that announces triple-enforcement in its prose BEFORE / AFTER the validator-side schema bump lands (out-of-order rollout), (c) duplicate work Story 4.13 owns. The no-`marker-taxonomy.yaml`-bump structural choice (per AC-12 of Story 4.7): the triple invariant is enforced at SCHEMA-validation time — BEFORE the run can reach a marker-emission seam. A non-compliant envelope is rejected by `envelope-validator` at the CI gate (exit code 1); it never reaches the orchestrator, never reaches the bundle assembler, never reaches the marker-emission infrastructure. There is no "triple-violated" marker class because the failure mode is "the validator rejected this envelope," reported via the standard CI failure surface (CI exit code; `format_errors` output in CI logs), not via a marker. The asymmetry vs Story 4.4 / 4.5 / 4.6 (which DID consume marker classes from `schemas/marker-taxonomy.yaml`) preserves the contract-shape-vs-runtime-behavior split: marker classes name remediation surfaces for runtime failure modes; schema constraints name well-formedness rules for envelope shapes — different artifacts, different lifecycles. Per ADR-002 line 185 ("Cell-1 architectural core is schema-versioned. Changes to cell-1 components are major-version events"), this story constitutes a cell-1 schema bump. The current `$id` at line 24 (`https://bmad-autopilot.local/schemas/envelope.schema.yaml`) does NOT carry an explicit semantic-version segment, and prior cell-1 bumps in this codebase (Story 4.2's `plan_drift` field add at lines 103-112; Story 4.3's `provisioned_env` add to `run-state.yaml`) similarly did not bump the `$id` URL — the convention in the repo is per-row audit-trail entries in this document rather than `$id`-encoded versions. THIS story follows that convention: NO `$id` change, but THIS row IS the version-bump record. The convention is mechanism for the Automator's substrate-side specialist envelope contract — by analogy to Story 4.2's `plan_drift` field row above — NOT an extension of BMAD-core's QA envelope contract; BMAD core has no upstream-relevant analogue at MVP because BMAD core does not own a behavioral-verification specialist with structurally enforced AC-assertion-evidence triples. Consumers: the bumped schema is consumed by `loud_fail_harness.envelope_validator.validate_envelope` AS-IS (the constructor at `envelope_validator.py` line 46 already handles `if/then` natively); the `format_errors` rewrite is consumed by the CLI's `main()` at envelope-validation-failure time; downstream consumers Story 4.10 (escalation routing reads triple-conformant `AcResult` records per the verification-fail contract), Story 4.11 (PR-bundle render of plan-persistence-compromise visibility), Story 4.13 (QA wrapper completion projects triple-conformant envelopes through `iterate_acs` composition) build on this convention without modifying the schema shape. | N/A — internal-only. | BMAD core ships an equivalent behavioral-verification envelope surface that includes structurally enforced per-AC assertion+evidence cardinality constraints. |
| `tools/loud-fail-harness/src/loud_fail_harness/qa_evidence_tier.py` substrate module + `$defs/evidence_ref` schema addition + `$defs/ac_result.semantic_verification` enum tightening + `$defs/ac_result.evidence_refs.items` object-shape thickening + `Tier-3-not-configured` marker class AS-IS reuse from `marker-taxonomy.yaml` line 78 + the no-`agents/qa.md`-modification structural choice + the no-new-step-file structural choice (Story 4.8 — three-tier evidence hierarchy + Tier-3 not_configured marker) | `automator-internal` | Story 4.8 (FR20 + FR21 — `_bmad-output/planning-artifacts/prd.md` lines 834-835 — "QA applies a three-tier evidence hierarchy — mechanical (Tier 1) / outcome (Tier 2) / semantic (Tier 3)" + "QA emits `semantic_verification: not_configured` as a loud-fail marker when Tier 3 is required by a story's QA Behavioral Plan but semantic-verification tooling isn't set up") consumes the `Tier-3-not-configured` marker class from `schemas/marker-taxonomy.yaml` lines 78-84 AS-IS (no taxonomy bump — Story 1.4's proactive add per the epic-close marker sweep already enumerated the class with the canonical multiline `diagnostic_pointer` "configure semantic verification per `qa-runbook.yaml`, OR accept the lower-tier evidence as sufficient for this AC"), the `examples/synthetic-stories/Tier-3-not-configured.md` synthetic-story AS-IS (existed since Story 1.7; `fixture-coverage` reports 29 covered classes UNCHANGED), the existing `MarkerClassRegistry` substrate seam from Story 2.6 AS-IS via `validate_marker_emission`, the `Draft202012Validator` jsonschema infrastructure AS-IS at `envelope_validator.py` line 46, the `$defs/ac_result` shape's top-level `required` + `additionalProperties` + `allOf` discipline UNCHANGED (only the per-property shape changes), and the existing 23-fixture envelope corpus migrated in-place (no fixture additions other than the new canonical `qa-pass-tier-3-not-configured.yaml` demonstration; corpus 23 → 24). The schema-level enforcement choice (per AC-1 of Story 4.8): the cell-1 schema bump introduces a NEW `$defs/evidence_ref` (sibling of `$defs/ac_result`; closed `additionalProperties: false`; `required: [path, tier]`; `path: minLength: 1`; `tier` is a closed string enum mirroring `qa_behavioral_plan.ExpectedEvidenceTier` byte-for-byte: `tier-1-mechanical`, `tier-2-outcome`, `tier-3-semantic`) AND tightens `$defs/ac_result.properties.evidence_refs.items` from `{ type: string }` to `{ $ref: "#/$defs/evidence_ref" }` AND tightens `$defs/ac_result.properties.semantic_verification` from the loose `oneOf: [object, string]` to a closed string enum mirroring `prd.md` line 596 byte-for-byte: `verified`, `not_configured`, `not_applicable`. The `required` token from the loose pre-bump `oneOf` is INTENTIONALLY rejected by the closed enum: `required` is the PLAN-side `semantic_verification_requirement` value (the forward-looking promise the plan makes about an AC), NOT the per-AC RESULT (what HAPPENED at verification time). The cross-field rule "plan declared Tier-2 required → result has ≥ 1 tier-2-outcome evidence ref" is wrapper-level cross-validation, deferred to Story 4.13 wrapper completion; the schema enforces only the per-item shape AND the `semantic_verification` enum closure. The new pure-library substrate module at `tools/loud-fail-harness/src/loud_fail_harness/qa_evidence_tier.py` exposes the three-tier primitives: `TIER_3_NOT_CONFIGURED_MARKER` symbolic constant, `EvidenceTier` / `SemanticVerificationResult` / `SemanticVerificationRequirement` Literal aliases (the result-side and plan-side enums duplicated here from `qa_behavioral_plan` to avoid Story-1.x ↔ Story-4.x cross-coupling, exercised by a byte-equality contract test), the `EvidenceRef` frozen Pydantic model (Pydantic-projection counterpart of `$defs/evidence_ref`), the three frozen Pydantic models `Tier3NotConfiguredDiagnosticContext` (carrying `story_id` + `ac_id` + `how_to_enable_pointer` per FR31's actionable remediation surface) / `Tier3NotConfiguredEmissionRecord` (carrying `marker_class="Tier-3-not-configured"` + the diagnostic context) / `Tier3NotConfiguredEmission` (co-exposing `marker_record` + `diagnostic_context` per Story 4.6's emission-shape pattern), the `PlanAndConfigForEvaluation` tiny model bundling the two `evaluate_semantic_verification` inputs, the `surface_tier_3_not_configured` Pattern-5 atomic-on-failure emission helper (mirrors Story 4.6's `surface_smoke_first_abort` byte-for-byte: `validate_marker_emission` runs FIRST; on registry rejection `UnknownMarkerClass` propagates per Pattern 5), and the `evaluate_semantic_verification` five-branch decision function (per the verbatim epic AC at `epics.md` lines 2034-2047: `not_applicable → ("not_applicable", None)`; `optional + configured → ("verified", None)`; `optional + not_configured → ("not_applicable", None)` — opt-in-without-config collapses to non-applicability per the doctrine extension; `required + configured → ("verified", None)`; `required + not_configured → ("not_configured", marker_record)` per epics.md lines 2036-2038). The function does NOT touch `AcResult.status` — pass/fail is determined by Tier-1+Tier-2 mechanical/outcome verification per the verbatim epic AC at line 2038 ("the marker signals visibility of the gap, not a failure"). The `_HOW_TO_ENABLE_POINTER` module-level constant carries the verbatim multiline string from `marker-taxonomy.yaml` lines 80-83; a freshness test asserts byte-equality with the YAML's `diagnostic_pointer` field, preventing silent drift. The `format_errors` AC-id-aware diagnostic refinement (per AC-8 of Story 4.8): two NEW path-conditional rewrite branches are appended AFTER the Story 4.7 FR19 branch — the tier-enum-violation branch (matches `["ac_results", <int>, "evidence_refs", <int>, "tier"]` enum violations; renders `"three-tier evidence hierarchy invariant: tier must be one of {tier-1-mechanical, tier-2-outcome, tier-3-semantic} (see FR20)"`) and the semantic_verification-enum-violation branch (matches `["ac_results", <int>, "semantic_verification"]` enum violations; renders the analogous FR21 invariant text); both branches resolve the AC-id defensively per the Story 4.7 pattern (try/except `(KeyError, IndexError, TypeError)`; falls back to index form `ac_results[N]`); both branches are scoped to the matching paths so unrelated enum errors (e.g., `status: "halfpass"`) keep their standard surface. Driver-side thickening (per AC-10 of Story 4.8): `playwright_driver.AcResult` is bumped — `evidence_refs: tuple[str, ...]` → `tuple[EvidenceRef, ...]`; `semantic_verification: Literal["not_applicable"]` → `SemanticVerificationResult` (the closed enum); the default `"not_applicable"` stays for backward-compat with the existing AC-1-only Tier-1-only Epic-2 wrapper usage at `agents/qa.md` line 35 (Story 2.10 contract); the six `AcResult(...)` constructor sites across `playwright_driver.verify_ac` + `http_driver.verify_ac` wrap each evidence path in `EvidenceRef(path=p, tier="tier-1-mechanical")` since drivers capture mechanical evidence by definition (Tier-2 outcome elevation is wrapper composition deferred to Story 4.13); `http_driver.py` re-exports `AcResult` AS-IS via the existing direct import from `playwright_driver`. The transitive AC-16 enumeration deviation (recorded for the Story 4.13 wrapper-completion reading): the bumped `AcResult.evidence_refs` flows through `qa_ac_iteration.surface_smoke_first_abort` (which copies `ac1_result.evidence_refs` into `SmokeFirstAbortDiagnosticContext.failed_evidence_refs: tuple[str, ...]` — the Story 4.6 surface) AND through the `bundle_assembly._render_per_ac_section` evidence-list render loop AND through several existing test files' `AcResult(...)` constructor sites + envelope-fixture helpers; the dev's call recorded here is to apply the minimal transitive shims (1) `qa_ac_iteration.surface_smoke_first_abort` projects `tuple(ref.path for ref in ac1_result.evidence_refs)` so the diagnostic shape stays unchanged at `tuple[str, ...]`, (2) `bundle_assembly._render_per_ac_section` extracts `ref["path"]` when the item is a dict (post-Story-4.8 shape) and falls back to `str(ref)` for any pre-Story-4.8 callers, (3) test files (`test_qa_ac_iteration.py`, `test_qa_plan_drift.py`, `test_qa_wrapper.py`, `test_walking_skeleton_smoke.py`, `test_http_driver.py`, `test_playwright_driver.py`) get minimal `EvidenceRef(...)`-wrapping at their `AcResult(...)` constructor sites + path-key-extraction at their `entry["evidence_refs"][0]` checks + `$defs` inlining at their `_make_ac_result_validator` helpers so the relative `$ref: "#/$defs/evidence_ref"` resolves locally inside the scoped sub-schema validators. AC-16's tighter enumeration was pessimistic about the transitive bump impact; the alternative (adding a Pydantic-side coercion validator on `AcResult.evidence_refs` to auto-wrap strings as `EvidenceRef(path=str, tier="tier-1-mechanical")`) was rejected because it would silently mask future bugs where a caller forgets to specify a tier. The fixture migration (per AC-13 of Story 4.8): all 11 existing `qa-*.yaml` envelope fixtures have their `evidence_refs` items migrated in-place via deterministic regex-based YAML transform from string-form `"path/to/file"` to object-form `{path: "path/to/file", tier: tier-1-mechanical}` (Tier-1 is the canonical post-migration default — every existing fixture's evidence was captured by Story 2.10 / 4.4 / 4.5 / 4.6 driver runs which produce mechanical evidence by definition; promoting any evidence to Tier-2 / Tier-3 retroactively would misrepresent capture provenance). The migration preserves top-of-file commentary, indentation, and every non-evidence_refs field byte-for-byte. The new envelope fixture `qa-pass-tier-3-not-configured.yaml` (3-AC happy path; AC-2's `semantic_verification: not_configured`; envelope-level `status: pass`; `findings: []` since Tier-3-not-configured is a marker not a finding per the FR30 vs FR55 distinction) is the canonical envelope-corpus demonstration of the wrapper-side projection when `evaluate_semantic_verification` returns `("not_configured", emission_record)`; the bundle assembler (Story 4.11) renders the structured marker comment from the per-AC tier-3-not-configured emission. Total envelope corpus: 23 → 24. The no-`agents/qa.md`-modification structural choice (per AC-13 of Story 4.7's discipline + Story 4.13's wrapper-completion ownership): THIS story does NOT modify `agents/qa.md`. The forward-pointer prose drift at `agents/qa.md` line 106 (currently saying `semantic_verification` thickens to the object form `{tier: 3, status: configured | not_configured}`) is documented here for Story 4.13's reading — Story 4.13 owns the wrapper thickening completion + corrects the forward-pointer prose to match THIS story's closed-enum schema bump. Modifying `agents/qa.md` at THIS story would (a) violate the in-place thickening invariant codified at `architecture.md` lines 1469-1485, (b) duplicate work Story 4.13 owns, (c) risk a wrapper that announces tier-aware projection BEFORE / AFTER the schema bump lands (out-of-order rollout). The no-new-step-file structural choice (mirrors Story 4.6's no-step-file precedent at extension-audit.md line 39): the three-tier evaluation is pure-Python composition with no LLM-runtime tool surface — `evaluate_semantic_verification` consumes the plan's `semantic_verification_requirement` AND the runbook's `tier_3_configured` flag (both already in scope at the wrapper layer at Story 4.13 time); `surface_tier_3_not_configured` is a Pattern-5 emission helper that takes the runtime registry. The asymmetry vs Stories 4.4 / 4.5 (which DID introduce step files) is structurally distinct: drivers bind to runtime tools; the three-tier evaluation is pure-Python composition. The no-`marker-taxonomy.yaml`-bump structural choice: the marker class was added proactively at Story 1.4 / Epic 1 close (verifiable today at lines 78-84), so THIS story consumes the class AS-IS. The no-`orchestrator-event.yaml`-bump structural choice: markers are emitted via the existing `MarkerClassRegistry` substrate seam (Story 2.6) without new event classes. Per ADR-002 line 185 ("Cell-1 architectural core is schema-versioned. Changes to cell-1 components are major-version events"), this story constitutes a cell-1 schema bump; THIS row IS the version-bump record (no `$id` URL change per the prevailing convention — per-row audit-trail entries in this document, not `$id`-encoded versions). The convention is mechanism for the Automator's wrapper-side three-tier evidence surface — by analogy to Story 4.7's row above — NOT an extension of BMAD-core's QA envelope contract; BMAD core has no upstream-relevant analogue at MVP because BMAD core does not own a behavioral-verification specialist with structurally typed evidence-tier hierarchy. Consumers: `loud_fail_harness.qa_evidence_tier.evaluate_semantic_verification` is composed by the QA wrapper at AC-iteration time (Story 4.13 forthcoming wrapper thickening); `loud_fail_harness.qa_evidence_tier.surface_tier_3_not_configured` is invoked by `evaluate_semantic_verification` itself on the `required + not_configured` branch; the resulting `Tier3NotConfiguredEmissionRecord` is consumed by Story 4.10 (escalation routing reads tier-aware `evidence_refs` from the bumped envelope) and Story 4.11 (PR-bundle render of per-AC `Tier-3-not-configured` marker comment) and Story 4.12 (evidence persistence consumes the per-evidence tier for size-budget partitioning) and Story 4.13 (QA wrapper completion thickens `agents/qa.md`); downstream consumers build on this convention without modifying the schema shape. | N/A — internal-only. | BMAD core ships an equivalent behavioral-verification envelope surface that includes structurally-typed evidence-tier hierarchy AND closed-enum semantic_verification result projection. |
| `tools/loud-fail-harness/src/loud_fail_harness/qa_exploratory_heuristics.py` substrate module + `$defs/finding.properties.verification_mode` finding-discriminator field add (sibling of Story 3.3's `meta` discriminator) + `$defs/heuristic_skipped_emission` schema addition + top-level `heuristic_skipped_emissions: array` optional QA-extension field + `marker-taxonomy.yaml` `heuristic-skipped.sub_classifications` bump from `[]` to `[empty-state, error-state, auth-boundary]` + `bundle_assembly._render_per_ac_section` thickening with new `### Exploratory heuristic findings` H3 sub-section + the `heuristic-skipped` marker class AS-IS reuse from `marker-taxonomy.yaml` line 86 + the no-`agents/qa.md`-modification structural choice + the no-new-step-file structural choice (Story 4.9 — three exploratory heuristics with verification_mode field) | `automator-internal` | Story 4.9 (FR22 — `_bmad-output/planning-artifacts/prd.md` line 836 — "QA runs three MVP exploratory heuristics per applicable story (empty state / error state / auth boundary)") consumes the `heuristic-skipped` marker class from `schemas/marker-taxonomy.yaml` lines 86-92 AS-IS (no marker_class add — Story 1.4's proactive add per the epic-close marker sweep already enumerated the class with the canonical multiline `diagnostic_pointer`; THIS story bumps the `sub_classifications` list under the existing class per the explicit allowance at `marker-taxonomy.yaml` line 35 — "Adding or refining a sub_classification under an existing class"), the `examples/synthetic-stories/heuristic-skipped.md` synthetic-story AS-IS (existed since Story 1.7; `fixture-coverage` reports 29 covered classes UNCHANGED), the existing `MarkerClassRegistry` substrate seam from Story 2.6 AS-IS via `validate_marker_emission`, the `Draft202012Validator` jsonschema infrastructure AS-IS at `envelope_validator.py` line 46, and the post-Story-4.8 `EvidenceRef` + `SemanticVerificationResult` types AS-IS in the new fixtures. Per ADR-002 line 185 ("Cell-1 architectural core is schema-versioned. Changes to cell-1 components are major-version events"), the new `verification_mode` field + the new `$defs/heuristic_skipped_emission` block + the new top-level `heuristic_skipped_emissions` field constitute a cell-1 schema bump; THIS row IS the version-bump record. The structural-choice rationale for separate `verification_mode` field vs extending `meta` enum (per AC-1(b) — single-responsibility per discriminator field; the comment block at envelope.schema.yaml lines 162-168 already forecast this divergence): `meta` covers Review-BMAD wrapper-layer-only review-completeness synthesis, `verification_mode` covers QA wrapper-layer-only exploratory-heuristic synthesis. The two discriminators are orthogonal — a finding could in principle carry BOTH; the schema permits it without ambiguity. Future maintainers MUST add new discriminators as siblings, not as enum extensions of EITHER existing field. The structural-choice rationale for the FLAT `$defs/heuristic_skipped_emission` schema vs the NESTED Pydantic model (per AC-10): the Pydantic side carries a NESTED `diagnostic_context: HeuristicSkippedDiagnosticContext` for in-process composition richness — mirrors Story 4.6's `SmokeFirstAbortEmissionRecord` + Story 4.8's `Tier3NotConfiguredEmissionRecord` patterns; the schema side uses a FLAT shape (`marker_class` + `sub_classification` + `story_id`) for bundle-render directness + YAML compactness. The wrapper projects between the two at envelope-write time via `model.model_dump(exclude={"diagnostic_context"})` + manual `story_id` injection from the diagnostic context. The intentional shape divergence — Pydantic-rich, YAML-flat — is documented for Story 4.13's wrapper-side composition. The new pure-library substrate module at `tools/loud-fail-harness/src/loud_fail_harness/qa_exploratory_heuristics.py` exposes the three-heuristic primitives: `HEURISTIC_SKIPPED_MARKER` + `EXPLORATORY_HEURISTIC_VERIFICATION_MODE` symbolic constants, `HeuristicKind` + `VerificationMode` Literal aliases (the heuristic-kind enum duplicated here from `qa_behavioral_plan.HeuristicApplicability` to avoid Story-1.x ↔ Story-4.x cross-coupling, exercised by a byte-equality contract test), the three frozen Pydantic models `HeuristicSkippedDiagnosticContext` (carrying `story_id` + `heuristic_kind`) / `HeuristicSkippedEmissionRecord` (carrying `marker_class="heuristic-skipped"` + `sub_classification` + the diagnostic context) / `HeuristicSkippedEmission` (co-exposing `marker_record` + `diagnostic_context` per Story 4.6 / 4.8's emission-shape pattern), the `surface_heuristic_skipped` Pattern-5 atomic-on-failure emission helper (mirrors Story 4.6 / 4.8 byte-for-byte), the `tag_heuristic_finding` shallow-copy helper (mirrors `review_layer_failure._build_synthetic_finding` discipline; double-tagging precondition `assert`), and the `evaluate_heuristic_applicability` decision function (reads ONLY `plan.entries[*].heuristic_applicability` per FR16 invariant — heuristic-applicability is plan-driven, not raw-AC-driven; any-AC-declaration wins per FR22 — heuristics are story-level, not AC-level). The `format_errors` AC-id-aware diagnostic refinement (per AC-4): one NEW path-conditional rewrite branch is appended AFTER the Story 4.8 semantic_verification-enum branch — the verification_mode-enum-violation branch (matches `["findings", <int>, "verification_mode"]` enum violations; renders `"exploratory-heuristic discriminator invariant: verification_mode must be \"exploratory-heuristic\" (the only currently-defined wrapper-layer-only QA discriminator value; see FR22)"`); the branch resolves the finding-id defensively per the Story 4.7 + 4.8 pattern (try/except `(KeyError, IndexError, TypeError)`; falls back to index form `findings[N]`); the branch is scoped to the matching path so unrelated enum errors keep their standard surface. Bundle-side thickening (per AC-9): `_render_per_ac_section` partitions the QA envelope's TOP-LEVEL `findings` array on the `verification_mode` discriminator (heuristic findings vs AC-driven findings); the `### Exploratory heuristic findings` H3 sub-section appears AFTER `### Plan drift detected` (canonical order: per-AC blocks → plan-drift → exploratory-heuristic findings); per-emission `validate_marker_emission(marker_registry, HEURISTIC_SKIPPED_MARKER)` defense-in-depth call (mirrors Story 3.3 + Story 4.2's pattern); the sub-section is COMPLETELY OMITTED when both `findings` and `heuristic_skipped_emissions` are empty (no orphan heading); AC-driven findings (without `verification_mode`) are NOT rendered by this section (Story 4.13 may add an AC-driven-findings render section). The verbatim epic AC at `epics.md` line 2079 — "heuristic findings DO NOT pollute the `ac_results` array" — is structurally satisfied by the partition itself. The structural-choice rationale for the partition-on-`verification_mode` bundle-render approach vs an explicit `heuristic_findings: array` envelope field: keeps the finding-shape uniform per FR51's "uniform envelope across specialists" + lets the bundle assembler partition on read; the alternative was rejected because it would duplicate the data structure and risk findings landing in BOTH places. The two new envelope fixtures `qa-pass-with-heuristic-findings.yaml` (verification-mode-tagged finding shape demonstration; envelope-level `status: pass` since heuristic findings do NOT flip pass/fail per FR22 + epics.md line 2069's bucket-and-severity discipline) + `qa-pass-with-heuristic-skipped.yaml` (all-three-heuristics-skipped shape demonstration). Total envelope corpus: 24 → 26. The no-`agents/qa.md`-modification structural choice (per AC-13 of Story 4.7's discipline + Story 4.13's wrapper-completion ownership): THIS story does NOT modify `agents/qa.md`. The forward-pointer prose drift at `agents/qa.md` line 41 (currently saying `"Do NOT execute the three exploratory heuristics — empty state / error state / auth boundary (Epic 4's Story 4.9 owns the heuristics per FR22)"`) is documented in this story's Completion Notes for Story 4.13's reading + prose correction. The no-new-step-file structural choice (mirrors Story 4.6's no-step-file precedent at extension-audit.md line 39 + Story 4.8's no-step-file precedent at line 41): the heuristic primitives are pure-Python composition; the LLM-runtime tool surface is the EXISTING `qa-driver-playwright.md` / `qa-driver-http.md` step files Stories 4.4 / 4.5 already declare — heuristics drive the running product through the SAME driver primitives `verify_ac` consumes. The convention is mechanism for the Automator's wrapper-side three-heuristic exploratory-verification surface — by analogy to Story 4.8's row above — NOT an extension of BMAD-core's QA envelope contract; BMAD core has no upstream-relevant analogue at MVP because BMAD core does not own a behavioral-verification specialist with structurally-typed exploratory-heuristic discriminator + heuristic-skipped emission carriage. Consumers: `loud_fail_harness.qa_exploratory_heuristics.evaluate_heuristic_applicability` is composed by the QA wrapper at story-iteration time (Story 4.13 forthcoming wrapper thickening); `loud_fail_harness.qa_exploratory_heuristics.surface_heuristic_skipped` is invoked by the wrapper on the structurally-inapplicable branch; `loud_fail_harness.qa_exploratory_heuristics.tag_heuristic_finding` is composed at finding-construction time when the wrapper drives a heuristic against the running product; the resulting `HeuristicSkippedEmissionRecord` is consumed by Story 4.10 (escalation routing) and Story 4.11 (PR-bundle render — already partially landed at THIS story's bundle-render thickening) and Story 4.13 (QA wrapper completion thickens `agents/qa.md`); downstream consumers build on this convention without modifying the schema shape. | N/A — internal-only. | BMAD core ships an equivalent behavioral-verification envelope surface that includes structurally-typed exploratory-heuristic discriminator + heuristic-skipped emission carriage. |
| `schemas/escalation-bundles/` directory + `schemas/escalation-bundles/verification-fail.yaml` (FR24a verification-fail escalation-bundle contract) + `schemas/escalation-bundles/env-setup-fail.yaml` (FR24b env-setup-fail escalation-bundle contract) + `enumeration_check.py` extension with NEW reconciliation pair `marker-taxonomy.yaml ↔ schemas/escalation-bundles/*.yaml` (Story 4.10 — two escalation-class CONTRACTS, assembly deferred to Epic 5 / Story 5.8) | `automator-internal` | Story 4.10 (FR24a / FR24b — `_bmad-output/planning-artifacts/prd.md` lines 839-840 — "two distinct escalation classes" — verification-fail with `escalate` policy; env-setup-fail with `escalate-with-env-diagnostic` policy + story-state preservation in `review`) consumes the four marker classes from `schemas/marker-taxonomy.yaml` lines 78-84 (`Tier-3-not-configured`), 105-115 (`env-setup-failed`), 117-123 (`plan-drift-detected`), 191-197 (`smoke-first-abort`) AS-IS via the strict-name `marker_class` discovery rule (no marker_class adds — Story 1.4's epic-close marker sweep already enumerated these classes; Story 4.10 CONSUMES them by reference). The structural-choice rationale for the Story 2.1 / Epic 4 mirror-pattern: Story 2.1 / Epic 2 produced `schemas/tea-handoff-contract.yaml` for Epic 4 to consume; THIS story (Story 4.10 / Epic 4) produces the two escalation-bundle contracts under `schemas/escalation-bundles/` for Epic 5 / Story 5.8 to consume — same contract-flow pattern across different epic boundaries. Per the verbatim epic AC at `epics.md` lines 2110-2112 ("Epic 5 imports these contracts and assembles bundles accordingly. Epic 5 does NOT redefine the content rules — it consumes them. And Epic 4 does NOT ship assembly code — only the contracts"), Story 4.10 is contracts-only — NO assembly code, NO `agents/qa.md` modification, NO envelope schema bump (the existing post-Story-4.9 envelope shape carries the verification-fail upstream signal via `status: fail` + the failing `ac_results` entry per the verbatim epic AC at lines 2114-2117), NO `marker-taxonomy.yaml` bump. Per ADR-002 line 185, both contract files at `schemas/escalation-bundles/` are cell-1 architectural-core artifacts — primitive-agnostic + methodology-agnostic; same architectural altitude as `marker-taxonomy.yaml` and `tea-handoff-contract.yaml`. Per ADR-003 Consequence 7 (substrate-component closure at FIVE), `enumeration_check.py`'s extension to a fourth reconciliation pair (`marker-taxonomy.yaml ↔ schemas/escalation-bundles/*.yaml`) is internal evolution of substrate component 4 — substrate-component count remains FIVE, NOT a sixth component. The structural-choice rationale for the strict-name `marker_class` discovery encoding (per AC-5): the verbose object-form for `tier_3_not_configured_markers` (array of objects each with a required `marker_class: { type: string, const: Tier-3-not-configured }`) is traded for closure-equivalence reconciliation against `marker-taxonomy.yaml` without bespoke heuristics; the alternative — flat string-array form — was rejected because the strict-name discovery rule does not unwrap arbitrary field-name strings into marker references, and a heuristic field-name discovery would re-introduce the false-positive risk per `enumeration_check.py` lines 130-145's discovery-rule worked-examples table. The structural-choice rationale for NO `failing_ac_result` field on env-setup-fail (per AC-3(j)): env-setup failures occur BEFORE QA dispatch (Story 4.3's env-provisioning lifecycle gates QA dispatch); there is no AC-failure record to carry. Per the verbatim epic AC at `epics.md` line 2106 — "different remediation, different escalation-bundle shape, different story-state outcome". The contract's narrowed shape (no `failing_ac_result`; no `qa_behavioral_plan_pointer`) IS the structural distinctness from verification-fail. The structural-choice rationale for NO cross-fragment `$ref` to `envelope.schema.yaml`'s `$defs/ac_result` from verification-fail.yaml's `$defs/failing_ac_result` (per AC-2(g)): cross-fragment `$ref` would create a co-versioning coupling NOT justified by a runtime invariant — per ADR-003 Consequence 2's "co-versioning is local; not generalized" principle, cross-fragment duplication is preferred at MVP; the duplication is annotated in each `$defs` block's source-of-truth comment so future maintainers can trace the canonical schema. The structural-choice rationale for the `$id` URL scheme `"https://bmad-autopilot.local/schemas/escalation-bundles/<file>.yaml"` mirrors `envelope.schema.yaml` + `tea-handoff-contract.yaml`'s URL scheme byte-for-byte. The structural-choice rationale for storing ports as `{port: integer, state: string-enum}` objects vs flat `{[port]: state}` mapping (per AC-3(g)): JSON-Schema-2020-12's structural posture handles mappings via `patternProperties` which is harder to validate + harder to read; the explicit object-array form is the BMAD-canonical encoding consistent with other array fields in the codebase. The structural-choice rationale for the single-value `bundle_class` enum on each contract (`["verification-fail"]` vs `["env-setup-fail"]`): mirrors Story 4.7's `meta` field's narrow-enum precedent; future bundle classes (Phase 1.5+ extensions) extend the enums via follow-on schema bumps recorded as new rows below per the append-only discipline. The structural-choice rationale for the single-value `retry_policy` enum on each contract (`["escalate"]` vs `["escalate-with-env-diagnostic"]`): the enum encodes FR24a / FR24b's policy structurally; the const-form is intentionally narrow to forbid downstream consumers from re-deriving the policy decision — the contract IS the policy. The structural-choice rationale for the const-form `current_state: review` + `intended_next_state_skipped: qa` fields on env-setup-fail's `story_state_preservation_note`: encodes ADR-005's cross-state consistency invariant on the contract side; the eventual orchestrator-side state-transition logic (Story 5.8's assembly + the Stop-hook bundle assembly per FR59) consumes the contract's preservation note to decide the post-bundle lifecycle state. The `enumeration_check.py` extension is additive (new optional flag `--escalation-bundles-dir` + new optional discovery path); existing behavior is preserved for repo states where the directory is absent (`is_dir()` check); the absent-directory deferral note `"note: schemas/escalation-bundles/ not present; deferred to story 4.10"` mirrors the existing `_DEPENDENCIES_DEFERRAL_NOTE` byte-for-byte. The naming-lint registration extension is INTENTIONALLY NOT included in this story (the new sub-directory is the first under `schemas/`; canonical naming-lint targets remain the six pre-Story-4.10 cell-1 schemas — `envelope.schema.yaml`, `orchestrator-event.yaml`, `marker-taxonomy.yaml`, `dependencies.yaml`, `tea-handoff-contract.yaml`, `run-state.yaml`); the new contracts' Pattern-1 + Pattern-2 conformance is enforced via the bespoke `test_escalation_bundle_contracts.py` module instead (Pattern-1 walker over user-defined property keys; Pattern-2 walker over `marker_class.const` values); future stories adding more sub-directories MAY revisit this decision when there is a clear case for elevating the new sub-tree to canonical naming-lint coverage. The pluggability-gate extension is INTENTIONALLY NOT included in this story (the gate operates on `agents/` only — the new `schemas/escalation-bundles/` directory is outside its trigger by construction per the gate's allowlist-by-construction posture documented at `pluggability_gate.py` lines 161-176). The eight new tests in `test_enumeration_check.py` exercise the new reconciliation pair (absent dir clean skip; empty dir clean skip; clean reconciliation with both real contracts; missing-marker fail; YAML parse failure; non-mapping failure; CLI flag override; both-pairs-absent simultaneous skip); the thirty new tests in `test_escalation_bundle_contracts.py` exercise schema-validity + structural-distinctness + header-comment integrity + Pattern-1 / Pattern-2 conformance + LF line-endings for both contracts. Consumers: Story 5.8 (escalation-bundle assembly mechanism, `epics.md` lines 2463-2491) consumes both contracts as the assembly target — Story 5.8's assembler reads the `bundle_class` discriminator to route between the two bundle shapes + conforms its assembled bundles to the schema declared here (per the verbatim epic AC at lines 2108-2112). Story 4.13 (QA wrapper-thickening completion, `epics.md` lines 2192-2193) closes the wrapper-side commitment without modifying these contracts. The verbatim epic AC at line 2122 ("the FR62 pluggability gate is unaffected") is structurally satisfied — the pluggability gate continues to pass cleanly without registration extension. | N/A — internal-only. | BMAD core ships an equivalent escalation-bundle contract surface, OR Epic 5 / Story 5.8 lands an assembly mechanism that requires bumps to either contract's structural shape (a co-versioning trigger per the contract-header comment block's discipline). |
| `loud_fail_harness.qa_plan_persistence_compromise.COMPROMISE_NOTE_BODY` constant + `qa_behavioral_plan.render_plan_section` blockquote prepend + `bundle_assembly._render_per_ac_section` blockquote prepend + audit-doc cross-reference (Story 4.11 — plan-persistence-compromise visibility per FR25) | `automator-internal` | Story 4.11 (FR25 — `_bmad-output/planning-artifacts/prd.md` line 841 — "QA documents its plan-persistence-compromise visibly in the QA Behavioral Plan section AND in the PR bundle") ships a single-source-of-truth pure-library module `tools/loud-fail-harness/src/loud_fail_harness/qa_plan_persistence_compromise.py` exposing the canonical `COMPROMISE_NOTE_BODY` constant + the `render_compromise_blockquote` helper. The canonical prose (drift-prevention anchor — every line of the constant is asserted as a substring of THIS audit doc by `test_audit_doc_contains_compromise_note_body_lines`):<br>"This plan is persisted across runs for resumability.<br>Persistence is a known compromise: full QA independence would re-derive the plan every run.<br>See `docs/extension-audit.md` and FR-P2-9 (Phase 2 upgrade replacing this with per-run plan re-derivation cross-check)."<br>Classification rationale: the persistence-vs-purity tradeoff is a substrate-level resumability concern (MVP accepts plan persistence to support `bmad-automation:resume {story-id}` from Story 8.3 without re-deriving the plan; full QA independence per FR-P2-9 — `_bmad-output/planning-artifacts/prd.md` line 128 — Per-run plan re-derivation cross-check — will re-derive the plan every run with a cross-check against the persisted plan at Phase 2). The persistence MECHANISM is `automator-internal`; the `## QA Behavioral Plan` story-doc-section identifier itself remains `upstream-proposal` per the row at line 30 — THIS row is the complement (the same architectural property's mechanism-side classification is distinct from the section-identifier-side classification). Phase-2-upgrade-planned (FR-P2-9). Consumer linkage: the constant is the canonical source; `qa_behavioral_plan.render_plan_section` (Story 4.1) AND `bundle_assembly._render_per_ac_section` (Story 2.11) BOTH consume `render_compromise_blockquote()` so the compromise note is visible in BOTH the story-doc QA Behavioral Plan section AND the PR bundle's `## Per-AC results` section; THIS audit-doc row IS the human-readable surface and the drift-prevention anchor against which the constant is byte-equivalence-checked. Per the verbatim epic AC at `_bmad-output/planning-artifacts/epics.md` line 2139 ("the bundle reads the compromise content from `docs/extension-audit.md` (or from a structured config consumed by the assembler) rather than duplicating prose — same drift-prevention pattern as Story 1.12a's TEA-boundary doc"), THIS story chooses BOTH options simultaneously: the structured config is the Python module's `COMPROMISE_NOTE_BODY` constant; the audit-doc row carries the same prose verbatim; the drift-prevention test forbids them from drifting. | N/A — internal-only with FR-P2-9 Phase-2 upgrade path (per-run plan re-derivation cross-check replaces persistence at Phase 2; the `COMPROMISE_NOTE_BODY` constant + both render sites are removed at Phase 2; THIS audit-doc row's classification flips to "Phase-2-upgraded" and the row is preserved for historical-audit-trail purposes). | Phase 2 lands FR-P2-9's per-run plan re-derivation cross-check (the persistence compromise is then resolved by purity-by-construction; this row's classification flips to "Phase-2-upgraded" and the constant + render sites are removed); OR BMAD core ships an equivalent QA-Behavioral-Plan persistence-vs-purity convention (NFR-I4 acknowledgment trigger; row becomes adapter-window candidate). |
| `loud_fail_harness.qa_evidence_persistence` module + `evaluate_size_budget` + non-emission of `masking-not-configured` marker on absent `masked_selectors` (Story 4.12 — evidence persistence + size budget + truncation marker + sanitization mechanism per FR49 / NFR-P6 / NFR-R4 / NFR-S2) | `automator-internal` | Story 4.12 closes four substrate-level claims simultaneously:<br>(i) **FR49** — canonical persistence path `_bmad-output/qa-evidence/{story-id}/{run-id}/` (gitignored) is owned by the new pure-library module's `compute_evidence_root` + `compute_run_dir` + `EVIDENCE_ROOT` single-source-of-truth literal at `tools/loud-fail-harness/src/loud_fail_harness/qa_evidence_persistence.py`;<br>(ii) **NFR-P6** — configurable byte ceiling enforced by `evaluate_size_budget`'s pure decision function emitting the `evidence-truncated` marker (Story 1.4 taxonomy entry at `schemas/marker-taxonomy.yaml` line 144 — consumed AS-IS, NO taxonomy bump) via the Pattern-5 atomic-on-failure helper `surface_evidence_truncated`;<br>(iii) **NFR-R4** — across-runs durability via `allocate_run_id`'s 16-character lexicographically-sortable UTC stamp `YYYYMMDDTHHMMSSZ`; the orchestrator-driven cadence (separate Task tool invocations) provides the natural sub-second-or-greater spacing — THIS module does NOT introduce a uniqueness lock, does NOT read the filesystem to check existence, does NOT loop on collision (wrapper-side responsibility delegated to Story 4.13);<br>(iv) **NFR-S2** — sanitization is mechanism-not-policy: the existing `MaskedSelectorPolicy` (`playwright_driver.py` line 374; api-side mirror per Story 4.5) is the canonical sanitization mechanism, consumed AS-IS by the driver-level redaction at `playwright_driver.py` line 1275 + `http_driver.py` line 1215 — THIS story does NOT extend the policy class, does NOT modify its empty-tuple default, does NOT add auto-detection heuristics ("looks like a credit card field" is explicitly forbidden per `epics.md` line 2171).<br>**Absence-of-marker doctrine (the structural enforcement of NFR-S2):** when `masked_selectors` is absent or empty, drivers persist evidence raw — NO redaction, NO marker emission, NO log warning. The verbatim epic AC at `_bmad-output/planning-artifacts/epics.md` line 2175 names the rationale: "emitting a marker would create alarm fatigue and constitute auto-scrubbing-by-shame (per NFR-S2's "Automator does not auto-scrub" intent)". The non-emission is enforced **by NOT IMPLEMENTING** — there is no `masking-not-configured` marker class to emit; `marker-taxonomy.yaml` carries no such entry, and the test `test_no_masking_not_configured_marker_class_in_taxonomy` is the structural witness against accidental future addition. The doctrine is NOT a runtime check + suppression (which would itself constitute auto-scrubbing-by-shame in disguise); the absence IS the contract.<br>**NFR-S2 verbatim** (PRD line 970): "Sensitive-data masking is the practitioner's responsibility; the Automator provides the masking mechanism, not the policy."<br>Cross-component reuse posture: `MarkerClassRegistry` + `validate_marker_emission` from `specialist_dispatch.py` AS-IS (mirroring Stories 4.6 / 4.8 / 4.9); `MaskedSelectorPolicy` documented reuse target (`playwright_driver.py` line 374) — THIS module does NOT import it (the policy is consumed by drivers, not by the persistence module); `qa_evidence_tier.py`'s shape patterns mirrored byte-for-byte (Pydantic frozen models; co-exposed `marker_record` + `diagnostic_context`; `_HOW_TO_ENABLE_POINTER` freshness-test pattern). Per the verbatim epic AC at `epics.md` line 2176 ("this rationale is recorded in `docs/extension-audit.md` as a deliberate-decision audit entry — the absence-of-a-marker is itself an audit-worthy choice"), THIS row IS the human-readable surface and the structural-non-emission's audit witness. | N/A — internal-only; the persistence-path conventions ride on FR49's canonical-path discipline (no migration unless BMAD core absorbs the path convention, in which case NFR-I4 acknowledgment trigger applies). The absence-of-marker doctrine is structurally permanent (would only revisit if BMAD core ships an opt-in auto-scrub mechanism, which would itself be a substantial NFR-S2 revision). | BMAD core ships an evidence-persistence convention with a different canonical path (NFR-I4 acknowledgment trigger; row becomes adapter-window candidate); OR a future practitioner-driven policy shift adds an opt-in "warn on missing masked_selectors" toggle (would re-open the absence-of-marker doctrine for a configurable opt-in marker class; the doctrine itself would NOT change — the default remains no-marker per NFR-S2). |
| Absence-of-marker on intentional `init --overwrite-confirmed` override (Story 7.6 — non-destructive guard's explicit-override path) | `automator-internal` | Story 7.6 (FR41 + FR42 — `_bmad-output/planning-artifacts/prd.md` lines 866-867 verbatim — "`init` is non-destructive on existing BMAD projects — never overwrites user-owned content in `_bmad-output/` or story docs" + "`init` re-runs preserve user configuration; upgrades do not lose customizations to config.yaml or qa-runbook.yaml") ships the non-destructive guard at `tools/loud-fail-harness/src/loud_fail_harness/init_non_destructive_guard.py`. The guard's four-branch decision surface (`proceed-fresh` / `preserve-merge` / `overwrite-confirmed` / `halt-would-destroy`) emits the v1-taxonomy marker `init-would-destroy-existing-artifact` (`schemas/marker-taxonomy.yaml` lines 360-370 — consumed AS-IS, NO taxonomy bump) on the halt branch ONLY. Per the verbatim epic AC at `_bmad-output/planning-artifacts/epics.md` line 3058 ("**no marker fires on intentional override** — the destruction is intentional, not unexpected; markers signal unexpected-or-undesirable events; the audit trail comes from the structured log entry, not a marker class"), THE OVERRIDE-CONFIRMED BRANCH EMITS NO MARKER. Per the verbatim epic AC at `epics.md` line 3059 ("this design choice is documented in `docs/extension-audit.md` as a deliberate-decision audit entry"), THIS row IS the human-readable surface and the structural-non-emission's audit witness — mirroring Story 4.12's `masked_selectors` absence-of-marker doctrine row above byte-for-byte in shape.<br>**Mechanism (the four structural enforcers):**<br>(i) **Override-bypass-no-marker structural rule** — `evaluate_non_destructive_guard`'s `action="overwrite-confirmed"` branch does NOT call `record_marker_with_context`. The test suite at `tests/test_init_non_destructive_guard.py::test_evaluate_does_not_emit_marker_on_non_halt_branches` patches the consuming-module's namespace symbol and asserts `mock.call_count == 0` across all three non-halt branches (per Story 7.4's deferred-review fix on patched-symbol-name discipline);<br>(ii) **Structured-log-entry as canonical audit-trail surface** — the override path writes a YAML file at `<project_root>/_bmad-output/init-history/{YYYYMMDDTHHMMSSZ}.log` (timestamp shape mirrors Story 4.12's `allocate_run_id` per `qa_evidence_persistence.py:226-255`; drift-prevention across all `_bmad-output/` audit/evidence surfaces) carrying the keys `timestamp` / `action` / `files_overwritten` / `practitioner_intent` / `loud_fail_marker_emitted: false` (the structural absence-witness embedded in the data) / `rationale_pointer` (referencing this row from inside the log so a maintainer can grep from the log to the doctrine);<br>(iii) **Secondary-confirmation deliberate-friction-mechanism** — `--overwrite-confirmed` AND `--yes` (or interactive secondary confirmation) are BOTH required per `epics.md` lines 3061-3064 verbatim; the override flag passed alone halts with the marker on `halt_route="secondary-confirmation-missing"`. The deliberate friction distinguishes intentional override from accidental destruction (a misconfigured CI script that hard-codes `--overwrite-confirmed` without `--yes` halts loudly rather than silently overwriting);<br>(iv) **Test-suite structural witness** — every non-halt branch exercises `mock.patch` against `loud_fail_harness.init_non_destructive_guard.record_marker_with_context` and asserts `call_count == 0`; the structural enforcement holds at CI time, not just at code-review time.<br>**NFR-S2-equivalent absence-doctrine prose** (mirroring Story 4.12's row above): markers signal unexpected-or-undesirable events; the destruction is intentional, not unexpected — emitting a marker would conflate "I deliberately reset to defaults" with "the Automator surprised me by destroying my work". The audit trail comes from the LOG (which the practitioner can grep + inspect), not from a marker class.<br>Cross-component reuse posture: `MarkerClassRegistry` + `record_marker_with_context` from `marker_wiring.py` AS-IS; the v1 taxonomy entry at `schemas/marker-taxonomy.yaml` lines 360-370 consumed AS-IS (NO taxonomy bump per Story 1.11 atomic-vs-aggregated principle); `_atomic_write_text` mirrored byte-for-byte from `install_path.py:445-473` (Story 7.6 is the SECOND caller; Story 7.2's docstring at `install_path.py:447-460` documents a "third-caller" promotion threshold to `_shared.py` — the helper stays local until the third caller arrives); `resolve_target_path` (Story 7.4) + `resolve_config_path` / `resolve_qa_runbook_path` / `load_config_template` / `load_qa_runbook_template` (Story 7.5) consumed via import. | N/A — internal-only; `init` is an automator-only first-run-installation surface; BMAD core has no equivalent first-run-installation command at MVP, so there is no upstream-relevant analogue. The absence-of-marker doctrine for the override surface is structurally permanent under MVP scope (would only revisit if a future Phase 2 story introduces an `init`-time recovery surface where the override path itself becomes ambiguous — see revisit-conditions column). | Revisit if a future Phase 2 story introduces an `init`-time recovery surface where the override path itself becomes ambiguous (e.g., partial-override or selective-override flags on per-file granularity) — at which point the absence-of-marker doctrine for the `init --overwrite-confirmed` surface specifically may need to be replaced with a configurable opt-in marker class (e.g., `init-partial-override-recorded` for the per-file ambiguous case). Also revisit if a practitioner-driven policy shift adds an opt-in "warn on every override" toggle (would re-open the doctrine for a configurable opt-in marker class; the default remains no-marker per the `epics.md` line 3058 invariant). |
| `agents/qa.md` thickened in place to full FR16-FR25 surface + `thickening_flags.py::is_full_qa_present` flipped True (Story 4.13 — QA wrapper thickening completion + Walking-Skeleton-Mode header cascading flag flip per the Story 2.11 in-place-flip pattern's third worked instance after Story 3.4's `is_full_review_present` flip + Stories 4.1-4.12's cumulative QA-domain pure-library module landings) | `automator-internal` | Story 4.13 closes Epic 4 by composing the wrapper-side surface against the eleven QA-domain pure-library modules already shipped by Stories 4.1-4.12 and by flipping the corresponding thickening flag.<br>(i) **FR16-FR25 surface closed** — FR16 (TEA-independence; `tea_artifacts_consumed: []` structurally encoded) + FR17 (independent product driving via `playwright_driver.verify_ac` for `web` per Story 4.4 / `http_driver.verify_ac` for `api` per Story 4.5) + FR18 (per-AC `ac_results` array; cardinality matches `len(ac_list)` on the no-abort path) + FR19 (AC-assertion-evidence triple structurally enforced by `schemas/envelope.schema.yaml`'s `$defs/ac_result` `if/then` invariant per Story 4.7) + FR20 (three-tier evidence hierarchy via `qa_evidence_tier.evaluate_semantic_verification` per Story 4.8) + FR21 (Tier-3 `not_configured` marker via `surface_tier_3_not_configured` per Story 4.8) + FR22 (three exploratory heuristics via `qa_exploratory_heuristics` per Story 4.9 with the `verification_mode: exploratory-heuristic` finding-discriminator) + FR22b (smoke-first AC-1 abort via `iterate_acs` + `surface_smoke_first_abort` per Story 4.6) + FR23 (QA Behavioral Plan creation + persistence via `persist_or_reuse_plan` per Story 4.1; AC-hash drift detection via `surface_plan_drift` per Story 4.2) + FR24a (verification-fail wrapper-side declaration shape per Story 4.10) + FR24b (env-setup-fail wrapper-side declaration shape per Story 4.10) + FR25 (plan-persistence-compromise visibility note via `render_compromise_blockquote` per Story 4.11).<br>(ii) **Eleven QA-domain pure-library modules composed AS-IS** — `qa_behavioral_plan` (4.1), `qa_plan_drift` (4.2), `env_provisioning` (4.3), `playwright_driver` (4.4), `http_driver` (4.5), `qa_ac_iteration` (4.6), `qa_evidence_tier` (4.8), `qa_exploratory_heuristics` (4.9), `qa_plan_persistence_compromise` (4.11), `qa_evidence_persistence` (4.12), plus `specialist_dispatch.py`'s registry primitives. NO modification to ANY of the eleven modules; THIS story's wrapper-side procedure section names them with their fully-qualified `tools/loud-fail-harness/src/loud_fail_harness/<module>.py` paths per the Story 2.10 / 4.11 / 4.12 citation discipline.<br>(iii) **In-place-thickening invariant** — same `agents/qa.md` file (the canonical agent-definition file at the architecture.md View-1 / View-2 / View-3 placement; the diagonal-asymmetry residence point per ADR-002 cell 5; the Story 2.10 lander); same `name: qa` YAML frontmatter identity; same envelope contract per `schemas/envelope.schema.yaml`'s `$defs/ac_result` shape (NO schema bump); same Task-tool dispatch substrate per ADR-004 + `specialist_dispatch.py` (the agent definition file is read AS DATA by the dispatch substrate; NO entry-point added). The wrapper's internal coverage thickens from the Story-2.10-era AC-1-only / Tier-1-evidence-only minimum to the full FR16-FR25 surface; only the prose changes.<br>(iv) **`is_full_qa_present` flag flip** — verbatim Story 2.11 AC at `_bmad-output/planning-artifacts/epics.md` lines 1521-1522: "the assembler renders the header text by enumerating which thickening features are still missing" + "each downstream epic flips its corresponding flag when its thickening lands". The flag flips `False → True` per the in-place-flip pattern's third worked instance (after Story 3.4's `is_full_review_present` flip + Stories 4.1-4.12's cumulative landing of the eleven QA-domain pure-library modules — the "preparing the substrate" half; THIS story is the "fan-in flip" half).<br>(v) **Cascading Walking-Skeleton-Mode header structural-not-era-based posture** — NO edit to `tools/loud-fail-harness/src/loud_fail_harness/bundle_assembly.py::_render_walking_skeleton_header`; NO edit to the `_THICKENING_SENTENCES` tuple. The omission of the "Tier-1 evidence only (Epic 4 thickens to Tier-2 + Tier-3-where-configured)." sentence from the rendered header body is the structural witness of the flag flip (the renderer iterates `_THICKENING_SENTENCES` and skips entries whose flag returns `True`); the dropped-sentence invariant is the third worked instance after Story 3.4's "Single-layer review" omission and Story 6.1's forthcoming "No loud-fail block" omission.<br>(vi) **Retained `walking-skeleton-bundle` marker emission rationale** — `is_loud_fail_block_present()` continues to return `False` at the post-4.13 substrate state (Epic 6 / Story 6.1 owns the flip that inverts emission). Per the verbatim Story 4.13 AC at `epics.md` lines 2203-2204: "`walking-skeleton-bundle` continues to emit because `is_loud_fail_block_present()` is still false (Epic 6 inverts that condition)" + "Epic 4's thickening does not suppress the marker — Epic 4 does not own the structural condition that drives marker emission". The rule is structural NOT era-based per `marker-taxonomy.yaml` lines 210-216 + `bundle_assembly.py` module docstring lines 41-52 + Story 3.4's audit-doc note. The post-3.4 + post-4.13 cumulative substrate state — TWO flags True (`is_full_review_present` + `is_full_qa_present`); TWO flags False (`is_retry_present` — Epic 5 owns; `is_loud_fail_block_present` — Epic 6 owns) — is the second worked instance of the structural-not-era-based posture earning its keep without an assembler edit.<br>(vii) **FR62 pluggability invariant preserved** — the thickened `agents/qa.md` references sibling specialists ONLY by HUMAN-READABLE names ("the Dev specialist", "the Review-BMAD specialist", "the Phase-1.5 LAD layer"), never by literal slug-form-with-extension or by bare multi-hyphen slug. Module-path references like `tools/loud-fail-harness/src/loud_fail_harness/<module>.py` are NOT specialist references and so do NOT trigger Rule 1 (which fires only on `agents/<slug>...` constructs per `pluggability_gate.py` line 115-128). The `pluggability-gate` continues to exit 0 with 3 passing specialists and 0 cross-reference findings. | N/A — internal-only; the in-place-thickening pattern + four-flag thickening-flags namespace + dynamic Walking-Skeleton-Mode header rendering are all wrapper-layer / substrate-layer constructs with no upstream-relevant surface — BMAD core has no equivalent of an FR62-pluggable QA-specialist agent-definition file (BMAD core does not own a behavioral-verification specialist at MVP), no equivalent of the four-flag thickening-flags namespace (BMAD core does not own an Automator-style PR-bundle assembler), no equivalent of the dynamic Walking-Skeleton-Mode header rendering (BMAD core has no walking-skeleton-mode concept). NFR-I4 does not apply — the convention is never absorbed upstream because it has no upstream-relevant surface. | BMAD core ships an FR62-pluggable behavioral-verification specialist (would re-open the upstream-proposal classification candidacy for the agent-definition file's structural shape; the four-flag thickening-flags namespace would remain `automator-internal` because the walking-skeleton-mode concept is Automator-internal); OR BMAD core absorbs the QA Behavioral Plan section into the upstream story-doc template (the section's structural shape is already classified as `upstream-proposal` in the existing audit row for Story 4.1; the agent-definition file's wrapper-side composition that READS from + WRITES to the section would remain `automator-internal` because the wrapper-side composition surface is private to the Automator's mechanism). |
| `tools/loud-fail-harness/src/loud_fail_harness/retry_budget.py` substrate module + the derived-from-`retry_history` budget-counter discipline (Story 5.1 — first Epic-5 substrate landing per `epics.md` lines 2218-2233). The structural commitment that `budget_remaining = resolved_budget - len(run_state.retry_history)` is DERIVED at decision-time (NOT stored as a separate `schemas/run-state.yaml` field) is the drift-prevention pattern this story codifies; the alternative "stored counter" shape would risk the counter and the `retry_history` array disagreeing across writes. The choice also hits ZERO schema bumps (no `run-state.yaml` MINOR version bump for an additive `budget_remaining` field) and stays forward-compat with Story 5.5's externalized retry history (the count is re-derived from whichever container holds the history). | `automator-internal` | An orchestrator-side budget-mechanics primitive (PRD FR8, line 819 — "Orchestrator enforces a configurable whole-story retry budget; default: 2; configurable via `_bmad/automation/config.yaml`"). It is NOT a BMAD-core lifecycle change; it is flow-policy mechanism living entirely inside the Automator's substrate (the FR62 pluggability gate scans `agents/*.md` only and does not see this module). The "derived counter, not stored counter" structural choice is private to the Automator's substrate; BMAD core has no equivalent of an FR8-pluggable orchestrator-side retry budget (BMAD's existing skills loop manually under human supervision). | N/A — internal-only. | BMAD core ships an equivalent retry-budget mechanism whose structural shape names a counter discipline (stored vs. derived) — at which point the Automator's derived-counter posture is re-evaluated against the upstream choice. Re-affirmed downstream by Stories 5.6 (exhaustion-emission consumes `RetryDecision.HALT_BUDGET_EXHAUSTED` against the derived counter) / 5.8 (escalation-bundle assembly references the same derived counter against externalized history once 5.5 lands). |
| `tools/loud-fail-harness/src/loud_fail_harness/retry_router.py` substrate module + the structured-action-item `dataclass(frozen=True)` payload shape `{finding_id, location, required_change, severity}` derived from `bucket: patch[HIGH or MED]` envelope findings (Story 5.2 — second Epic-5 substrate landing per `epics.md` lines 2218-2233; sibling of Story 5.1's `retry_budget.py`). The structural commitment that action items are `dataclass(frozen=True)` instances (NOT Pydantic models, NOT free-form dicts) reflects the architecture pattern of "Pydantic at boundaries, dataclasses at flow-policy internals" — Pydantic models live in `run_state.py` / `specialist_dispatch.py` for envelope-shape contracts (validated boundaries); `ActionItem` and `DeferredFinding` are derived AFTER the envelope was already validated at `validate_return_envelope` time, so they are orchestrator-internal payloads consumed by Story 5.3's `affected_files` derivation. A future "Pydantic action-item model" would conflate the envelope-side contract surface with the orchestrator-internal decision payload — the dataclass keeps the boundary clean and matches `bundle_assembly.py`'s precedent for orchestrator-internal accumulation structures. The choice also hits ZERO schema bumps (`envelope.schema.yaml` `$defs/finding` consumed AS-IS; `run-state.yaml` `last_envelope` consumed AS-IS; `marker-taxonomy.yaml` no new marker classes — Stories 5.4 / 5.6 own runtime marker emission for scope-assertion-violation + retry-budget-exhausted; THIS story is routing-mechanics-only). | `automator-internal` | An orchestrator-side bucket-driven routing primitive (PRD FR9, line 820 — "Orchestrator routes retry findings back to the responsible specialist using structured action items derived from `patch`-bucket findings — never the full review prose"). The convention is private to the Automator's substrate; BMAD core has no equivalent of an orchestrator-side bucket-driven action-item routing mechanism (BMAD's existing skills loop manually under human supervision, with the human reading the review prose directly). The FR9 "never the full review prose" invariant is the Automator's specific commitment; the AC-5 prose-firewall regression-baseline test is the structural enforcement (the test concatenates `dataclasses.asdict(item)` values across the derived tuple and asserts the envelope's `rationale` substring is absent). The four-bucket → four-outcome routing rule consumes BMAD's existing finding taxonomy verbatim per FR27 (no new buckets). | N/A — internal-only. | BMAD core ships an equivalent bucket-driven action-item routing mechanism — at which point the Automator's `dataclass`-based payload shape is re-evaluated against the upstream choice. Re-affirmed downstream by Stories 5.3 (consumes `ActionItem.location` to derive `affected_files` for the wrapper-side `retry_mode: fix-only` contract pair) / 5.8 (escalation-bundle assembly consumes `RoutingOutcome.ESCALATE` outcome + the `ActionItem` tuple shape) / 5.7 (`deferred-work.md` format spec audit thickens the `record_defer_findings` format-MVP this story commits to). |
| `tools/loud-fail-harness/src/loud_fail_harness/retry_dispatch.py` substrate module + the fix-only retry mechanism shipped as a CONTRACT PAIR across orchestrator-side `RetryDispatchDirective` declaration (`retry_mode: fix-only` + `affected_files`) and Dev-side `scope_expanded_to` reporting (Story 5.3 — third Epic-5 substrate landing per `epics.md` lines 2218-2233; sibling of Story 5.1's `retry_budget.py` and Story 5.2's `retry_router.py`). The structural commitment is that the contract-pair is shipped atomically as a single deployable unit AND tested via a SINGLE end-to-end pair test (`test_fix_only_retry_contract_pair_end_to_end` — NOT decomposed into orchestrator-side + Dev-side tests). The rationale is the verbatim architectural commitment from `epics.md` line 2295: "the context-firewall mechanism is implemented atomically as a unit (contract pairs that ship separately become contract pairs that drift)". Splitting the test into per-side variants would let drift between the orchestrator-side renderer and the Dev-side reader slip through Story 5.4's verification (the verifier consumes both sides; a slightly-different field name on either side would let per-side tests pass while the integration silently fails). The pair-test discipline IS the regression baseline for FR10 + FR11 + FR54 cohesion. The choice also hits ZERO schema bumps (`scope_expanded_to: array[string]` already shipped at Epic 2 per `envelope.schema.yaml` lines 96-101; `retry_mode: enum [fix-only]` + `affected_files: array[string]` already shipped on the `retry-attempted` event class per `orchestrator-event.yaml` lines 245-275; `marker-taxonomy.yaml` no new marker classes — Story 5.4 owns runtime emission of `scope-assertion-violation`). | `automator-internal` | The mechanism is private to the Automator's substrate; BMAD core has no equivalent of orchestrator-managed scope-locked retry (BMAD's existing skills loop manually under human supervision, with the human enforcing scope discipline by reading the retry instructions directly). The FR10 capability-level fix-only constraint + FR11 `scope_expanded_to` reporting are the Automator's specific commitments to context-firewalling at retry boundaries. The renderer's API surface (accepting only `directive` + `action_items`, never the envelope) is the structural enforcement of FR9 at the renderer layer — extending the prose-firewall regression baseline from Story 5.2's action-item layer to the rendered-prompt-body layer. | N/A — internal-only. | BMAD core ships an equivalent fix-only retry mechanism with orchestrator-managed scope locking — at which point the Automator's contract-pair shape is re-evaluated against the upstream choice. Re-affirmed downstream by Stories 5.4 (consumes `extract_scope_expanded_to` in the post-return diff-vs-declaration verifier path) / 5.6 (consumes `RetryDispatchDirective` for the `retry-attempted` event-emission helper) / 5.8 (escalation-bundle assembly references the directive shape AND the violation marker). |
| `tools/loud-fail-harness/src/loud_fail_harness/scope_assertion.py` substrate module + the SubagentStop hook (`hooks/subagent-stop.sh`) thickening that shells out to the `scope-assertion-verify` CLI on every Dev return — shipped as a CONTRACT-CLOSURE PAIR (orchestrator-side computed-diff comparison `loud_fail_harness.scope_assertion.verify_scope_assertion` paired with the SubagentStop hook's exit-code path per FR12 + FR58) (Story 5.4 — fourth Epic-5 substrate landing per `epics.md` lines 2218-2233; sibling of Stories 5.1 / 5.2 / 5.3). The structural commitment is that the verifier is its own substrate module (and the marker `scope-assertion-violation` is its own marker class in `schemas/marker-taxonomy.yaml` lines 237-245) rather than a sub-classification of `hook-failed`, because remediation differs (review Dev's diff vs. declared scope; possibly tighten retry's `affected_files` vs. fixing the hook script). The marker is REMEDIATION-SHAPED, NOT EMISSION-POINT-SHAPED — distinct from `hook-failed` despite sharing the SubagentStop exit path — per the principle codified at Story 1.11. The violation routes to Story 5.6's exhaustion-equivalent escalation path WITHOUT consuming a retry round per `epics.md` line 2349 verbatim. The schema bump (1.1 → 1.2) adds an additive optional `last_retry_directive` field to `schemas/run-state.yaml` so the SubagentStop hook can read the orchestrator's declared scope at hook time. | `automator-internal` | The mechanism is private to the Automator's substrate; BMAD core has no equivalent of orchestrator-managed scope-assertion verification. The FR12 mechanical-enforcement commitment + FR58 SubagentStop hook exit-code path are the Automator's specific substrate-level realization of the FR10 + FR11 capability framing established by Story 5.3. The two-surface closure (pure-library verifier + bash hook) realizes the architectural commitment from `epics.md` line 2343: "the exit-code path lit up in Epic 2 is fully exercised here in Epic 5". | N/A — internal-only. | BMAD core ships an equivalent post-return scope-assertion verification mechanism with orchestrator-managed mechanical enforcement — at which point the Automator's two-surface closure shape is re-evaluated against the upstream choice. Re-affirmed downstream by Stories 5.6 (consumes the violation as an exhaustion-equivalent escalation trigger) / 5.8 (escalation-bundle assembly renders the violation diagnostic via `ScopeAssertionDiagnostic`'s `dataclasses.asdict` output). |
| `tools/loud-fail-harness/src/loud_fail_harness/retry_history.py` substrate module + the externalized `_bmad-output/retry-history/{story-id}/{round-id}/artifacts.yaml` per-round on-disk artifact shape + the `RetryAttempt` PATCH-shape thickening on `schemas/run-state.yaml` (`round_id` + `path` optional sub-properties added to `retry_history.items.properties`; schema_version enum UNCHANGED) AND the `dangling-evidence-ref` marker-class REUSE for retry-history dangling-reference detection (Story 5.5 — fifth Epic-5 substrate landing per `epics.md` lines 2218-2233; sibling of Stories 5.1 / 5.2 / 5.3 / 5.4). The structural commitment is that retry history is EXTERNALIZED to disk under `_bmad-output/retry-history/{story-id}/{round-id}/` (gitignored, mirroring Story 4.12's `_bmad-output/qa-evidence/` precedent) and the run-state's `retry_history[]` array carries only `{retry_attempt, retry_reason, round_id, path}` REFERENCES rather than the per-round content (so the run-state stays small + atomic-write fast per NFR-R5 + NFR-R8); the canonical-write-ordering invariant ("per-round artifact-write success BEFORE run-state advance") is enforced via the orchestrator-skill's `StoryDocCallback`-wrapping pattern that composes `persist_retry_round` first and Story 2.2's `advance_run_state` second on persist success. The marker-class REUSE decision is the load-bearing convention codified here: retry-history dangling-references emit `dangling-evidence-ref` (Story 1.4 taxonomy entry at `schemas/marker-taxonomy.yaml` lines 199-206) verbatim per `epics.md` line 2379 — the diagnostic surface is identical (a referenced on-disk artifact is missing); the absence of a NEW marker class for retry-history-specific dangling-refs IS the structural enforcement of the marker-class-reuse principle from Story 1.11. | `automator-internal` | An orchestrator-side persistence-layer primitive (PRD FR13, line 824 — "retry history (findings, scope, diff per round) is preserved even when retry-budget exhausts"; PRD NFR-R5 line 949 — retry history preservation across exhaustion). The mechanism is private to the Automator's substrate; BMAD core has no equivalent of an externalized retry-history persistence surface (BMAD's existing skills loop manually under human supervision, with the human reading the retry context directly without filesystem persistence). The marker-class reuse posture is the Automator's specific commitment to remediation-shaped (NOT emission-point-shaped) marker classes per Story 1.11; the absence of a new marker class IS the structural enforcement. The PATCH-version-additive schema bump posture (sub-property addition under existing `retry_history.items` field; no rename, no constraint tightening; schema_version enum UNCHANGED) is the Automator's specific commitment to backward-compat at the schema layer — Story 2.2-era + Story 5.1-era MVP-shape `retry_history` entries continue to validate post-bump. | N/A — internal-only. | BMAD core ships an equivalent retry-history persistence mechanism — at which point the Automator's externalization-to-disk + reference-array shape is re-evaluated against the upstream choice. Re-affirmed downstream by Stories 5.6 (retry-history artifacts SURVIVE the retry-budget-exhaustion non-advance per FR14 — gitignored but filesystem-persistent; not auto-cleaned by Story 2.11's merge-ready cleanup hook because exhaustion does not reach merge-ready) / 5.8 (escalation-bundle assembly consumes `resolve_retry_round` to render per-round summaries) / 8.x (SessionStart reattachment lazy-loads references via `resolve_retry_round`; dangling references emit `dangling-evidence-ref` per the same code path the CLI uses). |
| `tools/loud-fail-harness/src/loud_fail_harness/retry_budget_exhaustion.py` substrate module shipped as a TRIGGER-FAN-IN PAIR (Story 5.1's `RetryDecision.HALT_BUDGET_EXHAUSTED` + Story 5.4's `ScopeAssertionViolation`) routed through ONE handler with ONE marker class (Story 5.6 — sixth Epic-5 substrate landing per `epics.md` lines 2218-2233; sibling of Stories 5.1 / 5.2 / 5.3 / 5.4 / 5.5). The structural commitment is that the dual-trigger fan-in lands on a single `record_retry_budget_exhaustion` entry point with a single `escalation-fired` event class (`escalation_class: "retry-budget-exhausted"` for both trigger paths) per `epics.md` line 2350 verbatim ("Story 5.4 reuses this exhaustion pathway"), AND that the marker class `retry-budget-exhausted` (`schemas/marker-taxonomy.yaml` lines 247-252, REUSED verbatim) is REMEDIATION-SHAPED (Story 1.11) distinct from `scope-assertion-violation` because the remediation surface differs (review accumulated retry history vs. fix declared scope). Despite the dual-trigger fan-in, only `retry-budget-exhausted` is emitted by THIS module — `scope-assertion-violation` is emitted upstream by the SubagentStop hook (Story 5.4) and surfaced as PART of the escalation-bundle context (`ExhaustionContext.scope_violation_diagnostic`), not duplicated here as a marker emission. The lifecycle-state mutation to `current_state="escalated"` is via `loud_fail_harness.run_state.advance_run_state` directly (NOT via `commit_transition`) per the architectural commitment in `lifecycle_state_machine.py:97-105` that "`escalated` is reached via a separate code path, NOT via `commit_transition`" — THIS module IS that separate code path. The choice hits ZERO schema bumps (`escalation-fired` event class + `current_state: "escalated"` enum value + `retry-budget-exhausted` marker class are all pre-enumerated by Stories 1.3 / 2.4 / 1.4 respectively; THIS story is the FIRST runtime emitter / mutator for primitives whose schema commitments already shipped). The escalation-bundle assembly is consumed via a caller-injected `EscalationBundleAssembler` callable seam (Story 5.8 owns the FR15-shaped assembler; `default_escalation_bundle_assembler` writes a minimum-viable placeholder until 5.8 lands). The canonical-write-ordering invariant at the escalation level (assembler-write-success first → run-state advance second → event-log-append third) is enforced via the `StoryDocCallback`-wrapping pattern, mirroring `commit_transition`'s ordering at `lifecycle_state_machine.py:752-756` byte-for-byte. | `automator-internal` | An orchestrator-side flow-policy primitive (PRD FR8, line 819 — retry budget; PRD FR14, line 825 — non-advance + state preservation; PRD FR15, line 826 — escalation bundle path; PRD NFR-R5, line 949 — retry-history preservation across exhaustion). The mechanism is private to the Automator's substrate; BMAD core has no equivalent of an orchestrator-managed retry-budget-exhaustion handler with state-preservation discipline. The dual-trigger fan-in posture (`BUDGET_EXHAUSTED` + `SCOPE_ASSERTION_VIOLATION` reuse one pathway) is the Automator's specific commitment to remediation-shaped marker classes per Story 1.11 — the structural choice that two distinct triggers map to one marker class because the remediation is the same (human review of the escalation bundle), distinct from the SubagentStop hook's scope-violation marker which has its own remediation surface (review Dev's diff vs. declared scope). The "separate code path" architectural posture for `current_state="escalated"` (NOT routed through the lifecycle map) is the structural enforcement of ADR-005's escalation-vs-lifecycle-transition distinction — keeping the lifecycle map clean of escalation logic so a future contributor reading `LIFECYCLE_TRANSITIONS` sees ONLY the forward-only canonical path and is forced to discover the escalation pathway via THIS module rather than infer it from the lifecycle dict. | N/A — internal-only. | BMAD core ships an equivalent orchestrator-managed retry-budget-exhaustion handler with state-preservation + escalation-bundle assembly — at which point the Automator's dual-trigger fan-in posture + `EscalationBundleAssembler` callable seam shape are re-evaluated against the upstream choice. Re-affirmed downstream by Stories 5.7 (`deferred-work.md` format spec audit feeds the escalation-bundle content surface Story 5.8 renders) / 5.8 (escalation-bundle assembly consumes `EscalationBundleAssembler` callable seam + the `RetryBudgetExhaustionResult.diagnostic` payload + `compute_escalation_bundle_path` for the canonical bundle location) / 6.1 (loud-fail block consumes `RetryBudgetExhaustionResult.diagnostic_message` for the actionable-pointer single-line stderr surface) / 6.9 (`bundle-assembly-failed` marker emission is the downstream-consumer policy for `EscalationBundleAssemblerFailed` — THIS module raises and re-raises; the marker is emitted by the orchestrator-skill caller) / 8.x (SessionStart reattachment reads `current_state == "escalated"` and routes resume-attempts to the escalation-bundle path NOT to a fresh dispatch; THIS module's state mutation is the entry condition for that recovery path). |
| `deferred-work.md` format spec (BMAD-existing convention; document title `# Deferred Work`; section header `## Deferred from: code review of <story_id> (<YYYY-MM-DD>)`; one-bullet-per-finding shape `- **<finding_id>** [\`<location>\`] — <description>`) — adopted as-is from `_bmad/bmm/4-implementation/bmad-code-review/steps/step-04-present.md` line 32 (the BMAD-METHOD upstream-authored format spec); reinforced by the four `_bmad/bmm/4-implementation/bmad-quick-dev/step-{01,02,04,oneshot}.md` `deferred_work_file: '{implementation_artifacts}/deferred-work.md'` config references; exemplified in continuous active use by `_bmad-output/implementation-artifacts/deferred-work.md` (47 sections across 423 lines as of 2026-05-04); RATIFIED by Story 5.2's `record_defer_findings` runtime emitter at `tools/loud-fail-harness/src/loud_fail_harness/retry_router.py` lines 628-732 (which mirrors the BMAD-METHOD format byte-for-byte across document title, section header, bullet shape, date format, section terminator); FR15's escalation-bundle pointer (`_bmad-output/planning-artifacts/prd.md` line 826 — "contains retry history, outstanding findings, rationale, and a pointer to `deferred-work.md`") targets THIS format (Story 5.7 — seventh Epic-5 landing per `epics.md` lines 2218-2233; the FIRST research-blocker spike to discharge the spike-with-bounded-timebox pattern; sibling of Stories 5.1 / 5.2 / 5.3 / 5.4 / 5.5 / 5.6; consumed by Story 5.8). The structural commitment is that the audit is documentation-only — ZERO substrate-module modifications, ZERO schema modifications, ZERO test additions; the per-convention-row entry + the `docs/research-spikes/2026-05-04-deferred-work-format.md` evidence artifact are the audit's full deliverable surface. Outcome 1 (adopt existing) converged within hours of spike-start (the BMAD-METHOD convention was discoverable AND already exercised by Story 5.2's runtime); the named fallback (outcome 3 — define our own, classify as `automator-internal`, revisit-condition "if BMAD core adopts a deferred-work convention, reconcile" per `epics.md` line 2438 verbatim) did NOT fire because the bounded 1-week timebox did not expire. The pattern's reusable shape (per-spike evidence artifact under `docs/research-spikes/{spike-start-date}-{topic-slug}.md` mirroring the `docs/rationale-validations/` precedent established by Story 3.5; per-convention-row append; closing-remark sub-paragraph in § "Research-blocker handling") is the template Story 7.1 (plugin-primitive-stability spike — sibling research-blocker spike; see `docs/extension-audit.md` line 162) instantiates against a different research blocker. | `automator-internal` | The BMAD-METHOD format is already authored upstream at `_bmad/bmm/4-implementation/bmad-code-review/steps/step-04-present.md` line 32 — so no `upstream-proposal` ask is required of BMAD core (the Automator is not introducing a novel format). The Automator's adoption is internal-only: Story 5.2's `record_defer_findings` mirrors the format byte-for-byte without modification; the convention is corroborated by the four `_bmad/bmm/4-implementation/bmad-quick-dev/step-{01,02,04,oneshot}.md` files' `deferred_work_file` config and append-on-defer semantics confirming the canonical path; the live on-disk artifact at `_bmad-output/implementation-artifacts/deferred-work.md` exemplifies the format in continuous repo-internal use. A future BMAD-core formalization (e.g. promoting the convention from a Step-04 instruction to an authored-spec file) does NOT change the format — so the classification is stable across that hypothetical promotion (the same shape continues to apply). The classification language "BMAD-existing convention used as-is" is the canonical outcome-1 phrasing per `_bmad-output/planning-artifacts/epics.md` line 2436 verbatim. | N/A — internal-only (the format itself is already BMAD-existing; the Automator adopts as-is. If BMAD core promotes the format from convention to authored spec without changing it, the Automator's adoption is unchanged because the format hasn't changed). | If BMAD core publishes a non-trivially-different format spec for `deferred-work.md` (e.g. adds frontmatter; changes the section-header shape; restructures the bullet syntax; introduces a machine-readable companion file such as a JSON-Schema or YAML sidecar) — at which point Story 5.8's escalation-bundle pointer logic AND `record_defer_findings`'s output shape are re-examined against the upstream spec; the convention may move from `automator-internal` to `upstream-proposal` if the Automator wishes to drive a schema upstream (per `_bmad-output/planning-artifacts/epics.md` line 2438 verbatim — "if BMAD core adopts a deferred-work convention, reconcile"). |
| Install-path priority (Claude Code plugin primitive `/plugin install <name>@<marketplace>` vs git-clone-symlink fallback) — bound to `_bmad/automation/config.yaml` `install_method` field per `epics.md` line 2930 (`plugin \| git-clone-symlink`); plugin manifest convention `.claude-plugin/plugin.json` per Claude Code official docs at `https://code.claude.com/docs/en/plugins`; FR35 (`/plugin install bmad-automation` per `_bmad-output/planning-artifacts/prd.md` line 860) + FR36 (git-clone-symlink fallback per `prd.md` line 861) sequencing finalized by Story 7.1's outcome (the FIRST research-blocker spike to instantiate the task-bounded refinement of the spike-with-bounded-timebox pattern; Story 5.7 instantiated the calendar-week-only shape; Story 7.1 instantiates the task-completion-criteria + calendar-minimum + calendar-maximum shape per `docs/research-spikes/2026-05-07-plugin-primitive-stability.md` § "Forward consumers" pattern-selection decision aid). The structural commitment is that the audit is documentation-only — ZERO substrate-module modifications, ZERO schema modifications, ZERO test additions; the per-convention-row entry + the `docs/research-spikes/2026-05-07-plugin-primitive-stability.md` evidence artifact + the closing-remark sub-paragraph in § "Research-blocker handling" immediately after the existing `**Claude Code plugin primitive name and stability** … Discharged by Story 7.1.` anchor (which moved from line 174 to line 175 when this row was inserted at line 54; the closing-remark sub-paragraph lands at line 177 after the anchor with one blank-line separator) are the audit's full deliverable surface. The plugin-manifest authoring (`bmad-autopilot/.claude-plugin/plugin.json`) is OUT OF SCOPE for Story 7.1 — Story 7.2 authors it per THIS row's outcome. The View 2 layout placeholder at `_bmad-output/planning-artifacts/architecture.md` line 1115 (`bmad-automation/plugin.json`) is reconciled to `bmad-automation/.claude-plugin/plugin.json` (the actual Claude Code on-disk convention observed against the user's own `~/.claude/plugins/cache/claude-code-warp/warp/2.0.0/.claude-plugin/plugin.json` install) by Story 7.2. The repo-layout-finalization gated decisions at `architecture.md` lines 1299-1300 + line 1419 + line 1420 unblock by name through THIS row's outcome. | `automator-internal` | The plugin primitive IS exposed and functional at Claude Code 2.1.132 (per the on-disk evidence at `~/.claude/plugins/cache/claude-code-warp/warp/2.0.0/` — the warp plugin install via `/plugin install warp@claude-code-warp` resolved successfully on 2026-04-27 against the then-current Claude Code 2.1.x release AND remains operational at 2026-05-07 against Claude Code 2.1.132) AND the official Anthropic-operated `claude-plugins-official` marketplace serves 35+ plugins (`agent-sdk-dev`, `clangd-lsp`, `code-review`, `feature-dev`, `frontend-design`, `pyright-lsp`, etc.) PLUS 16+ external partner plugins (Adobe, 42Crunch, Asana, Context7, Discord, Firebase, GitHub, GitLab, Greptile, Laravel, Warp, etc.) using the same `.claude-plugin/plugin.json` convention (refuting outcome 3 — plugin unavailable). HOWEVER the primitive lacks a blanket stability statement (NO `GA` / `beta` / `preview` / `experimental` classification of the primitive itself surfaces in `https://code.claude.com/docs/en/plugins` or `https://code.claude.com/docs/en/plugins-reference` — sub-features `experimental.themes` and `experimental.monitors` ARE explicitly tagged experimental but the primitive itself is not, leaving it in the unlabeled "shipped without a formal stability tier" zone) AND exhibits manifest-schema churn (v2.1.129 moved `themes`/`monitors` to `experimental.*`; v2.1.120 widened the `marketplace.json`/`plugin.json` schema; plugin-related bug fixes ship in nearly every release between 2.1.90 and 2.1.132 — refuting outcome 1 "stable"). The Automator's adoption is internal-only: Story 7.2 ships git-clone-symlink as primary and plugin install as opt-in via `--use-plugin-experimental` flag per outcome 2's verbatim classification text from `_bmad-output/planning-artifacts/epics.md` line 2893 ("Claude Code plugin primitive available but flagged experimental"). No `upstream-proposal` ask is required of BMAD core (the Automator is not introducing a Claude-Code-side primitive change); no `upstream-proposal` ask is required of Anthropic (the primitive is Anthropic-owned and the spike is observe-only per the same observe-only posture Story 5.7 maintained against BMAD-METHOD). | Pending Claude Code primitive stabilization. Story 7.2 ships git-clone-symlink as the primary install path; plugin install as opt-in via `--use-plugin-experimental` flag per outcome 2's classification text. Migration to plugin-primary triggers per the revisit condition (re-audit when stabilization signals fire — see Revisit conditions column). | If Claude Code primitive stabilizes — the doc surface publishes a `GA` / stability-statement classification at `https://code.claude.com/docs/en/plugins` or `https://code.claude.com/docs/en/plugins-reference`, OR the primitive ships ≥2 minor / patch versions without observed backward-incompatible manifest-schema changes (a plausible test: ≥2 releases without a `claude plugin validate` warn-emission for previously-supported manifest field positions, mirroring the v2.1.129 themes/monitors soft-deprecation pattern), OR the official Anthropic submission portals (`claude.ai/settings/plugins/submit`, `platform.claude.com/plugins/submit`) ship with explicit "GA" / "stable for production" labelling — re-audit per the same task-completion criteria (3 reference projects, 2 Claude Code versions including the version that introduced the stabilization signal) and consider promoting from experimental to primary; OR if Claude Code primitive becomes unavailable on a future release (the `/plugin install` command is removed or returns a not-implemented error), re-audit and consider demoting to outcome 3 (plugin deferred). |
| Plugin manifest location (`bmad-autopilot/.claude-plugin/plugin.json` per Claude Code's actual on-disk convention; reconciles the View 2 placeholder at `architecture.md` line 1115 from `bmad-automation/plugin.json` to `bmad-automation/.claude-plugin/plugin.json` per Story 7.1 outcome 2 evidence) | `automator-internal` | Story 7.1's evidence artifact at `docs/research-spikes/2026-05-07-plugin-primitive-stability.md` § "Evidence sources reviewed" item 1 confirms via on-disk inspection of `~/.claude/plugins/cache/claude-code-warp/warp/2.0.0/.claude-plugin/plugin.json` that Claude Code's actual on-disk plugin-manifest convention is `.claude-plugin/plugin.json` (NOT `plugin.json` at the plugin root). The official docs surface at `https://code.claude.com/docs/en/plugins-reference` § "Plugin manifest schema" canonicalizes the same convention (`name` is the only required field; `$schema` URL `https://json.schemastore.org/claude-code-plugin-manifest.json` enables editor autocomplete). The manifest location is wrapper-layer-only — Claude Code owns the convention and BMAD core does not own Claude Code plugin distribution; neither party has an `upstream-proposal` migration target. Story 7.2 ships `bmad-autopilot/.claude-plugin/plugin.json` (View 1 source-of-truth; redistributed unchanged in View 2 per `architecture.md` lines 1109-1138) and reconciles the inner-repo `bmad-autopilot/docs/architecture.md` View 2 layout entry to mirror the convention. | N/A — manifest location follows Claude Code's primitive convention; if Claude Code primitive bumps the convention to a different location, Story 7.2's revisit condition fires. | If Claude Code plugin-primitive layout convention bumps the manifest location (e.g., back to root-level `plugin.json` or to a third location), re-audit per the same task-completion criteria as Story 7.1 (3 reference projects × 2 Claude Code versions); update the manifest location accordingly AND propagate the change to View 2 in `bmad-autopilot/docs/architecture.md` and the install-path module's plugin-manifest-location reference at `bmad-autopilot/tools/loud-fail-harness/src/loud_fail_harness/install_path.py`. |

The column ordering above (convention name / classification / rationale / migration plan / revisit conditions) is the canonical layout — preserved across edits so PR diffs remain tractable. New rows are appended below the existing five (chronological-introduction order, append-only); existing rows are NEVER reordered. The append-only discipline IS the audit trail — git history is the per-row introduction log.

Per FR65, every entry has an explicit revisit condition in the "Revisit conditions" column: `upstream-proposal` entries name the NFR-I4 acknowledgment trigger and the RFC-rejection fallback; `automator-internal` entries name the BMAD-core feature whose arrival would reopen the classification; `research-needed` entries name their bounding spike per § Research-blocker handling.

---

## Skip-class recognition workflow

This subsection elaborates ADR-003 (architecture.md lines 205-334) and specifically codifies the operational workflow for the unknown-unknown skip-class routing path delegated to this doc by ADR-003 lines 279-292.

A newly-recognized skip-class — i.e., a runtime skip event the FRs/NFRs did not anticipate at MVP-design time — flows through the four-step process below (verbatim from ADR-003):

```
newly recognized skip-class →
  add to marker-taxonomy.yaml →
  add to orchestrator-event.yaml →
  add fixture / synthetic story →
  re-run CI checks (3), (4), (5) →
  merge
```

The cadence: **the audit doc is reviewed at the close of every epic** (per § Marker taxonomy vs. audit doc — known-knowns / unknown-unknowns split, where the epic-close marker sweep procedural step lives) **and at every release** (per FR65's "with revisit conditions where applicable" framing combined with the extension-purity anti-metric's per-release measurement cadence at PRD line 103). The two cadences compose: epic-close sweeps catch convention drift produced inside the just-finished epic; per-release sweeps catch drift accumulated across multiple epics and any `research-needed` entries whose timebox has expired without resolution.

The routing decision is explicit:

- **Known skip-classes** — those named by FRs / NFRs already in scope at MVP-design time — flow through `schemas/marker-taxonomy.yaml` (Story 1.4) + `schemas/orchestrator-event.yaml` (Story 1.3) + the harness fixture corpus (Story 1.7). These three sources are CI-enforced via the substrate components landed in stories 1.5 (`enumeration_check`) and 1.8 (`fr33-fixture-gate`). The known-skip-class set is reconciled by code at every PR.
- **Unknown-unknown skip-classes** — drift the FRs/NFRs didn't anticipate — flow through this doc and are added to the per-convention table per the append-only discipline above. The audit step is review-enforced, not CI-enforced; reviewers verify the classification is honest and the rationale is load-bearing.

The CI-vs-review split per ADR-003 line 292:

> "Layers A/B/C and the five substrate components are CI-enforced; skip-class recognition is review-enforced. The harness completeness invariant is over the known skip-class set under CI; the unknown-unknown layer is review-enforced via audit."

This is the architectural commitment that makes the audit doc load-bearing rather than aspirational: code reading text catches known drift; humans reading code catch unknown drift; the harness's structural limit is named honestly rather than papered over.

---

## How to add a new convention

A contributor adding a new Automator-introduced convention follows this procedure:

1. **Read the classification taxonomy** (this doc § Classification taxonomy) — confirm you understand what each of the three classifications means and how the choice constrains future migration behavior per NFR-I4.
2. **Read the existing per-convention table** (this doc § Per-convention table) — confirm your convention isn't already enumerated. If it is, the existing entry's classification governs; propose an edit only if the classification is dishonest under new evidence.
3. **Copy `examples/bmad-extension-audit-entry.md`** as the entry template — it shows the canonical shape (convention name / classification / rationale / migration plan / revisit conditions) with a worked example you can pattern-match.
4. **Classify your convention** per the 3-tier taxonomy. The classification is a load-bearing engineering decision, not a documentation chore — misclassifying an upstream-relevant convention as `automator-internal` traps the project on a forked path; misclassifying an internal mechanism as `upstream-proposal` floods upstream with non-load-bearing RFCs.
5. **For `upstream-proposal` classifications, draft the migration plan** per NFR-I4's four-stage flow (acknowledgment → adapter window → deprecation → removal). For `automator-internal`, write `N/A — internal-only`. For `research-needed`, write `pending research conclusion` AND name the bounding spike per § Research-blocker handling.
6. **Append the new row at the bottom of the per-convention table** — existing rows are never reordered. Then submit the PR; the audit doc is review-enforced, not CI-gated, so the reviewer's job is to assess that the classification is honest, the rationale is load-bearing, and the migration plan (if applicable) is realistic.

The contributor-onboarding entry point is `CONTRIBUTING.md`, which references this doc and walks the contributor through the same steps.

---

## Marker taxonomy vs. audit doc — the known-knowns / unknown-unknowns split

This subsection states the canonical principle governing the boundary between the v1 marker taxonomy (`schemas/marker-taxonomy.yaml`) and this audit doc, and codifies the epic-close marker sweep that operationalizes the principle.

> **"If a marker class is required by a named FR or NFR already in scope, it belongs in the v1 taxonomy (`marker-taxonomy.yaml`) — added proactively at epic-close, not discovered at runtime. The audit doc's job is unknown-unknowns: convention drifts the team didn't anticipate, classifications that emerged during implementation, and skip-classes the FRs/NFRs didn't name. Known-knowns travel through the taxonomy bump rule (Story 1.4); unknown-unknowns travel through this audit doc."**

### Epic-close marker sweep

At the close of every epic, the team sweeps that epic's FRs/NFRs for marker classes the requirements name (or imply via dependency-failure profiles in `dependencies.yaml` per SDN-001). Each named-or-implied class is confirmed present in `schemas/marker-taxonomy.yaml`. Missing classes are added BEFORE the epic merges — proactively, at epic-close, not reactively at downstream-epic runtime.

**Worked example — Epic 4's `playwright-mcp-unavailable` proactive add (per FR17 + ADR-002):**

PRD FR17 (`_bmad-output/planning-artifacts/prd.md` line 831 — "QA drives the running product independently — via Playwright MCP for web project types, via HTTP for API project types — against each acceptance criterion") combined with ADR-002's graceful-degrade dependency profile (architecture.md lines 99-204) names a Playwright-MCP-unavailability skip-class. Story 4.4 (Epic 4) is where this skip-class will run in production — but the marker class `playwright-mcp-unavailable` was added to `schemas/marker-taxonomy.yaml` at the close of Epic 1's taxonomy story (Story 1.4), NOT discovered at Epic 4's runtime. The taxonomy entry is verifiable today at `schemas/marker-taxonomy.yaml` lines 216-222 — sweep performed proactively per the principle above.

The worked example demonstrates the sweep IN this doc: the FR-to-marker mapping is named (FR17 + ADR-002 → `playwright-mcp-unavailable`); the existing taxonomy entry is pointed to as the "this is what proactive-add looks like" instance. Future epics' sweeps will produce similar mappings — written into this section at epic close so the audit trail accumulates rather than being re-derived.

---

## Marker class boundaries — the remediation-shape principle

> **"Markers are remediation-shaped, not emission-point-shaped. Two events that share an emission point but require different remediation paths get distinct marker classes — the reconciler routes by class, and routing by sub-classification fields breaks the flat-routing abstraction. Worked example: `hook-failed` and `scope-assertion-violation` share the SubagentStop hook's non-zero exit path but differ in remediation (fix the hook script vs review Dev's actual diff against declared scope), so they are distinct classes. Same pattern for `review-layer-failed` (Epic 3) which is distinct from `hook-failed` for the same reason."**

**Contributor-discipline checklist item:** *Does this marker have a remediation path distinct from existing classes? If yes → distinct class. If no → sub-classification of the existing class.*

The principle is verifiable against the existing taxonomy: `hook-failed`, `scope-assertion-violation`, and `review-layer-failed` are all enumerated in `schemas/marker-taxonomy.yaml` as distinct classes precisely because their remediation paths differ — fix the hook script (`hook-failed`); review Dev's actual diff against the declared scope (`scope-assertion-violation`); investigate the failed review layer's specific failure mode (`review-layer-failed`). The reconciler routes by class; emission-point-based grouping would have collapsed these into a single marker and lost the remediation-routing signal.

---

## Marker class boundaries — the atomic-vs-aggregated principle

> **"Markers represent atomic failure surfaces, not aggregated conditions across multiple atomic failures. If an event can be decomposed into existing markers, decompose it. New marker classes earn their place by representing a remediation surface that doesn't already have one — not by aggregating multiple existing classes under a new umbrella name. Worked example: Epic 7's `init` precondition checks could be aggregated under a hypothetical `init-precondition-failed` umbrella, but each underlying failure (TEA missing, Playwright MCP unavailable, BMAD core wrong version, git unexpected state) maps to existing markers (`env-setup-failed`, `playwright-mcp-unavailable`, etc.) — so `init`'s diagnostic layer aggregates the existing markers in named-invariant diagnostic output (per NFR-O5) rather than introducing a new aggregator class. The diagnostic layer's job is aggregation; the marker taxonomy's job is atomic surfaces."**

**Contributor-discipline checklist item:** *Can this event be decomposed into existing markers? If yes → use existing markers + diagnostic-layer aggregation. If no → new class with distinct remediation surface.*

**Apply together with the remediation-shape principle.** A proposed new marker class earns its place ONLY when (i) its remediation path is distinct from existing classes (the remediation-shape principle above) AND (ii) it is not decomposable into a combination of existing classes (this principle). Failing test (i), the proposal routes to sub-classification of an existing class. Failing test (ii), the proposal routes to diagnostic-layer aggregation per NFR-O5. Only when both tests pass is a new marker class warranted.

---

## Finding-taxonomy bumps — the no-introductions principle

> **"BMAD core's finding taxonomy is the canonical source for `bucket` ∈ `{decision_needed, patch, defer, dismiss}` and `severity` ∈ `{HIGH, MED, LOW}` per FR27. The Automator does NOT fork this taxonomy. ANY proposed addition of a bucket value, severity value, or finding classification field is a BMAD-extension event recorded here per FR64 / FR65; the default classification is `upstream-proposal` (BMAD core's review surface is the natural home for the addition). `automator-internal` applies ONLY when justification proves the bump is scoped strictly to the Automator's wrapper layer with no upstream-relevant surface — by analogy to the `failed_layers` graceful-degradation field at the per-convention table. The forward-compatibility loud-fail path at `validate_return_envelope` is the architectural backstop that makes silent drift impossible: a finding carrying an out-of-enum bucket or severity value, or an extra classification field, is rejected at the substrate seam — the bump must travel through this audit doc and `envelope.schema.yaml` BEFORE it can survive the gate."**

**Contributor-discipline checklist item:** *Is this a new bucket value, severity value, or finding classification field on the Review-BMAD envelope's `findings`? If yes → record the BMAD-extension event in this section's audit trail BEFORE bumping `envelope.schema.yaml`'s enum or `$defs/finding` shape; default classification is `upstream-proposal` unless the convention is provably wrapper-layer-only.*

The principle is verifiable against the existing artifacts: `envelope.schema.yaml` `$defs/finding` already declares `bucket` and `severity` as strict `bucket` enum (line 129) and `severity` enum (line 132) and `additionalProperties: false` on findings (line 110); the Review-BMAD wrapper at `agents/review-bmad-wrapper.md` documents the bucket × severity passthrough invariant as a named contract section (Story 3.2's "Finding-taxonomy passthrough discipline"); the substrate's `validate_return_envelope` rejects any envelope whose findings violate the schema. The three artifacts agree at MVP scope. Epic 3 introduces ZERO new taxonomy values — the no-introductions invariant is established structurally by the wrapper's passthrough section + the schema's enum constraint, and operationally by THIS subsection (what to do IF a future contributor or upstream release proposes a bump). The per-convention table at lines 27-34 is NOT modified at this story; the existing five seed entries remain in their chronological-introduction order; the append-only discipline holds.

The forward-compatibility loud-fail ordering is **record first, bump second**: when upstream `bmad-code-review` legitimately introduces a new bucket or severity value (e.g., a future `improvement` bucket), the operational response is (1) record the BMAD-extension event in the per-convention table per § How to add a new convention, (2) bump `envelope.schema.yaml`'s enum to include the new value, (3) re-run the harness CI sweep so the substrate seam now accepts the new value structurally, (4) update `_CANONICAL_BUCKETS` / `_CANONICAL_SEVERITIES` in `tools/loud-fail-harness/tests/test_review_bmad_wrapper.py` to include the new value. Skipping step (1) is a documented contract violation: the audit trail loses its load-bearing record of when and why the taxonomy bumped. Step (1) is review-enforced — the audit trail is not CI-gated but is a mandatory code-review requirement before any schema enum bump merges; the substrate-seam loud-fail prevents envelopes carrying the new value from surviving validation until step (2) completes, but does NOT block a schema bump from landing without an audit-doc entry.

---

## Research-blocker handling — the spike-with-bounded-timebox pattern

> **"A research blocker that gates downstream work must surface as a story in its dependent epic with: (a) a defined exit criterion enumerating the plausible outcomes, (b) a bounded timebox, (c) a named fallback outcome that ships if the timebox expires without convergence. Without these three, 'parallel with explicit dependency' becomes 'indefinite stall.' Worked example: Epic 5 Story 5.7 audits the `deferred-work.md` format with three named outcomes (adopt existing / extend existing / define our own), 1-week timebox, fallback to outcome 3 (define our own, classify as `automator-internal`). Apply this pattern to Epic 7's plugin-primitive-stability spike and to any future spike-blockered story."**

The pattern's fully-compliant instances at MVP scope (each has a defined exit criterion, a bounded timebox, and a named fallback):

- **Story 5.7** (`5-7-deferred-work-md-format-spec-audit-integration-research-blocker-spike-bounded-with-named-fallback`) — audits the `deferred-work.md` format spec; the canonical worked example above. (Status: discharged 2026-05-04, outcome 1.)
- **Story 7.1** (`7-1-claude-code-plugin-primitive-stability-spike-task-bounded-with-named-fallback`) — Claude Code plugin primitive name and stability; per epics.md the spike is bounded with a named fallback. (Status: discharged 2026-05-07, outcome 2.)

The four open research blockers from architecture.md lines 847-852, listed verbatim, with the discharging story per the spike-with-bounded-timebox pattern:

- **`deferred-work.md` format spec** — blocks loud-fail handling for `defer`-bucket findings. Discharged by Story 5.7.

    > **Spike close (2026-05-04, outcome 1 — adopt existing).** The audit ratified the BMAD-METHOD upstream-authored format at `_bmad/bmm/4-implementation/bmad-code-review/steps/step-04-present.md` line 32 (which Story 5.2's `record_defer_findings` runtime emitter already mirrors byte-for-byte). Evidence artifact: [`docs/research-spikes/2026-05-04-deferred-work-format.md`](research-spikes/2026-05-04-deferred-work-format.md) — four-section spike investigation (`## Spike metadata` / `## Evidence sources reviewed` / `## Outcome-decision flow` / `## Forward consumers`). Per-convention table row: the most recently appended row in `## Per-convention table` above (the seventh Epic-5 landing, immediately following Story 5.6's row), classification `automator-internal` (BMAD-existing convention used as-is per `_bmad-output/planning-artifacts/epics.md` line 2436 verbatim). **Named fallback did NOT fire**: the bounded 1-week timebox did not expire — outcome 1 converged within hours because the BMAD-METHOD convention was discoverable AND already exercised on disk by Story 5.2's runtime. The named-fallback structure (outcome 3 — define our own, classify `automator-internal`, revisit-condition "if BMAD core adopts a deferred-work convention, reconcile" per `epics.md` line 2438) is documented in the evidence artifact's `## Outcome-decision flow` § "Outcome 3" subsection so a future reader sees what the fallback would have looked like even though it didn't fire. **Canonical-worked-example status**: the principle paragraph at line 158 above cites Story 5.7 as the worked example; this closing remark backs that aspirational pointer with an actual story-close record. Story 7.1 (the next research-blocker spike) reuses the same pattern — same evidence-artifact subdirectory layout, same four-section shape, same outcome-enumeration with named fallback, same per-convention-row append.


- **TEA API surface for orchestrator handoff** — does orchestrator await TEA completion; what TEA artifacts does QA ignore vs. consume; how does the boundary surface in run-state. Discharged by Story 2.1.
- **Upstream proposal format for BMAD-METHOD** — blocks submission of `qa` lifecycle state and `## QA Behavioral Plan` proposals. **Intentionally unbounded at Epic 1 close**: no dedicated discharging story exists in the current epic plan because the upstream RFC track for BMAD-METHOD depends on BMAD core's public RFC process being established, which is not yet determined. This blocker will be formally bounded with a discharging story at Epic 1 retrospective or when Epic 6+ planning names the RFC preparation story. The `qa` and `## QA Behavioral Plan` table entries remain `upstream-proposal` with their migration plans intact regardless of this blocker's open status.
- **Claude Code plugin primitive name and stability** — shapes install path (primary vs fallback) and repo layout. Discharged by Story 7.1.

    > **Spike close (2026-05-07, outcome 2 — plugin primitive unstable but functional).** The audit confirmed the plugin primitive is exposed and operational at Claude Code 2.1.132 (per the on-disk `~/.claude/plugins/cache/claude-code-warp/warp/2.0.0/.claude-plugin/plugin.json` install evidence — `/plugin install warp@claude-code-warp` succeeded on 2026-04-27 against the then-current 2.1.x release AND remains operational at spike-start 2026-05-07; the official Anthropic-operated `claude-plugins-official` marketplace serves 35+ internal plugins plus 16+ external partner plugins using the `.claude-plugin/plugin.json` convention) BUT lacks a blanket stability statement (NO `GA` / `beta` / `preview` / `experimental` classification of the primitive itself surfaces in `https://code.claude.com/docs/en/plugins` or `https://code.claude.com/docs/en/plugins-reference` — sub-features `experimental.themes` and `experimental.monitors` ARE explicitly tagged experimental but the primitive itself is not) AND exhibits manifest-schema churn (v2.1.129 moved `themes`/`monitors` to `experimental.*`; v2.1.120 widened `marketplace.json`/`plugin.json` to accept `$schema`/`version`/`description` at top level; plugin-related bug fixes ship in nearly every release between 2.1.90 and 2.1.132). Evidence artifact: [`docs/research-spikes/2026-05-07-plugin-primitive-stability.md`](research-spikes/2026-05-07-plugin-primitive-stability.md) — four-section spike investigation (`## Spike metadata` / `## Evidence sources reviewed` / `## Outcome-decision flow` / `## Forward consumers`) with 6 evidence sources reviewed and a `## Compromise note` documenting the calendar-minimum-not-honored compromise of compressed AI-Dev sessions (vs the multi-day discipline AC-2 expects). Per-convention table row: the most recently appended row in `## Per-convention table` above (the FIRST row added by an Epic-7 story; immediately following Story 5.7's `deferred-work.md` format spec row), classification `automator-internal` (install-path priority is wrapper-layer-only — neither BMAD core nor Anthropic owns the install-path-priority decision; verbatim outcome-2 classification text per `_bmad-output/planning-artifacts/epics.md` line 2893: "Claude Code plugin primitive available but flagged experimental"). **Named fallback did NOT fire**: the bounded 3-week calendar-maximum did not expire — outcome 2 converged within hours because the primitive's exposure was directly observable AND the manifest-schema churn signals were directly observable in the changelog. The named-fallback structure (outcome 3 — plugin unavailable, ship git-clone-symlink only, classify `automator-internal`, revisit-condition "if Claude Code primitive becomes available, re-audit per the same task-completion criteria") is documented in the evidence artifact's `## Outcome-decision flow` § "Outcome 3" subsection so a future reader sees what the fallback would have looked like even though it didn't fire. **Canonical-worked-example status**: THIS closing remark is the SECOND closing remark in the four-blocker enumeration list (after Story 5.7's at lines ~169-170 above) AND the FIRST documented worked example of the spike-with-bounded-timebox pattern's task-bounded refinement (task-completion criteria + calendar-minimum + calendar-maximum); Story 5.7 is the FIRST documented worked example of the calendar-week-only shape. The principle paragraph at line 159 above (cite as line 158 in pre-this-row line numbering) cites Story 7.1 verbatim ("Apply this pattern to Epic 7's plugin-primitive-stability spike"); THIS closing remark backs that aspirational pointer with an actual story-close record. Future spike authors choose between THIS task-bounded shape (when the question is hands-on observational) and Story 5.7's calendar-week-only shape (when the question is spec-readable upstream) per the pattern-selection decision aid at `docs/research-spikes/2026-05-07-plugin-primitive-stability.md` § "Forward consumers" → "Pattern-selection decision aid (per AC-9)". Forward consumer Story 7.2 reads THIS row's classification entry text from the per-convention table to decide install-path priority per `epics.md` lines 2913-2916 verbatim — Story 7.2 ships git-clone-symlink as primary; plugin install opt-in via `--use-plugin-experimental` flag; the plugin manifest authoring lands at `bmad-autopilot/.claude-plugin/plugin.json` (NOT `bmad-autopilot/plugin.json`) per the on-disk Claude Code convention observed by THIS spike.

A `research-needed` classification entry on the per-convention table MUST name its bounding spike from this list (or a successor entry added as research blockers accumulate). Without a bounding spike, the classification has no exit criterion and the convention drifts into the indefinite-stall failure mode the principle exists to prevent.

---

## Marker emission rules — the structural-not-era-based principle (Story 3.4 worked instance)

> **"The `walking-skeleton-bundle` marker emission rule is structural — predicated on whether `is_loud_fail_block_present()` returns `False` — NOT era-based — predicated on a hardcoded 'if Epic == 2' check. Epic 3 retains the marker emission rule unchanged. The marker continues to emit because `is_loud_fail_block_present()` continues to return `False` at Epic 3 substrate state; Epic 3 is NOT the era that triggers suppression. Epic 6 / Story 6.1 owns the loud-fail block landing that flips the predicating flag, at which point emission inverts without any edit to the bundle-assembly rendering code."**

The principle is verifiable against the existing artifacts: `schemas/marker-taxonomy.yaml` lines 210-216's `walking-skeleton-bundle` `diagnostic_pointer` prose names the rule ("absent loud-fail block triggers the marker, NOT 'Epic 2 era triggers the marker' (rule clarified by Epic 6).") AND `tools/loud-fail-harness/src/loud_fail_harness/bundle_assembly.py`'s module docstring at lines 41-52 re-states it AND the runtime predicate at `_emit_walking_skeleton_marker` (lines 680-698) consults `thickening_flags.is_loud_fail_block_present()` directly. Story 3.4 is the first downstream test of the rule: at Epic 3 substrate state the renderer surfaces a thickened review-section bucket × severity grouping AND the `is_full_review_present` flag flips to `True`, but the `walking-skeleton-bundle` marker continues to emit because the predicating flag (`is_loud_fail_block_present`) stays `False`. The companion regression test `test_walking_skeleton_marker_still_emitted_at_epic_3_substrate_state` in `tests/test_bundle_assembly.py` is the baseline guard against accidental Epic-3 / 4 / 5 pre-emption of Epic 6's responsibility; the Story 2.11 AC-4 test `test_walking_skeleton_marker_suppressed_when_loud_fail_block_present` (which substitutes a stub flag namespace) is the structural witness that the inversion fires correctly when Epic 6 lands the production flag flip.

---

## Rationale Validations

This subsection holds pointers to per-rationale validation artifacts under `docs/rationale-validations/`. Each artifact validates a documented rationale clause from a prior story against the actual landed substrate that exercises the rationale, evaluates whether the rationale held in practice, and records a verdict drawn from the canonical enum `{rationale-holds, rationale-invalidated, partial}`. The pattern — **rationale → validation → written artifact** — is referenceable for future epics per `epics.md` lines 1774-1776 (future single-layer-choice re-litigations, e.g., Phase 1.5 LAD per FR29, reference Story 3.5's pattern rather than re-inventing the audit shape).

- **Story 2.9 — Acceptance Auditor as Epic-2 single layer.** Artifact: `docs/rationale-validations/2.9-acceptance-auditor.md`. Verdict: `rationale-holds`. Audited by: Story 3.5 (Epic 3).

---

## Contributor-discipline notes

This subsection holds append-only contributor-discipline notes — fixture/file location invariants, naming overlaps to avoid, and similar review-time conventions that are NOT BMAD-extension classification events (those go in the per-convention table above). These notes are review-enforced per `extension-audit.md`'s overall posture (ADR-003 line 292), NOT CI-enforced; reviewers verify the discipline at PR review time.

### Epic 2 walking-skeleton smoke fixture vs. Epic 7 user-facing onboarding sample (Story 2.13 resolution)

Two artifacts share a "sample story" surface concept but live at structurally different filesystem locations and serve fundamentally different audiences. Conflating them is the most plausible drift the smoke fixture's "test infrastructure only" identity exists to prevent.

- **Epic 2 smoke fixture** — `bmad-autopilot/tools/loud-fail-harness/tests/fixtures/sample-story-walking-skeleton.md`. Audience: Epic 2 maintainers running the harness's CI smoke suite. Lifecycle: CI-only; never user-visible; loaded by `tests/test_walking_skeleton_smoke.py` via `find_repo_root` + relative path. Distribution: per architecture.md View 2 lines 1109–1141, `tools/` is excluded from the shipped distribution unit, so the fixture is structurally guaranteed to be CI-only.
- **Epic 7 user-facing FR39 sample** — `_bmad-output/implementation-artifacts/sample-auto-001.md` (inside the user's BMAD project). Audience: practitioners onboarding to BMAD. Lifecycle: scaffolded once at install time by Epic 7's `init`; user-visible; lives under the user's BMAD project filesystem (View 3 line 1171), NOT inside the `bmad-autopilot/` source repo.

The structural separation invariant: the Epic 2 smoke fixture is NEVER `init`-scaffolded. Epic 7's init code MUST NOT discover, copy, symlink, or reference `tools/loud-fail-harness/tests/fixtures/sample-story-walking-skeleton.md`. The View 2 distribution-unit boundary makes this structurally guaranteed; this note records the discipline explicitly so future contributors see the rationale.

Review-time enforcement contract — a PR is a contract violation flagged at code review if it:

- moves the smoke fixture under `bmad-autopilot/examples/` (e.g., `examples/sample-stories/...` or `examples/synthetic-stories/...`);
- references the smoke fixture from `init`, from the user-facing onboarding-sample scaffolding, or from any deployed runtime artifact (skills, agents, hooks, schemas);
- renames the smoke fixture to overlap with `sample-auto-001.md` (e.g., `sample-walking-skeleton-001.md` is fine; `sample-auto-001-skeleton.md` is not — the `sample-auto-` prefix belongs to the Epic 7 user-facing-sample namespace).

The contributor-discipline note is review-enforced per `extension-audit.md`'s overall posture (ADR-003 line 292): there is no harness gate for this fixture-location invariant; the discipline lives in this document and in PR review.

**Story 7.4 cross-reference (2026-05-08).** The canonical content of the Epic 7 user-facing onboarding sample lives at `bmad-autopilot/tools/loud-fail-harness/src/loud_fail_harness/_data/sample-auto-001.md`. `/bmad-automation init` reads this packaged-data resource via `importlib.resources` and writes a verbatim copy to `_bmad-output/implementation-artifacts/sample-auto-001.md` inside the practitioner's BMAD project (per FR39). The structural separation invariant continues to hold: the smoke fixture lives under `tools/loud-fail-harness/tests/fixtures/`; the user-facing sample's content lives under `tools/loud-fail-harness/src/loud_fail_harness/_data/`; the runtime write target is the user's `_bmad-output/implementation-artifacts/`. None of the three paths overlap.

### Marker emission coverage audit (Story 6.3 resolution)

The canonical "how to extend marker emission coverage" guide lives at [`docs/marker-coverage-audit.md`](marker-coverage-audit.md). The artifact is generated by `tools/loud-fail-harness/src/loud_fail_harness/marker_coverage_audit.py` from `tools/loud-fail-harness/src/loud_fail_harness/_data/marker_coverage_surfaces.yaml`, walks every (marker_class × code_surface) intersection from `schemas/marker-taxonomy.yaml`, and records a verdict (`emitted` / `not-applicable` / `scheduled-by-story`) plus a code-path reference for each.

**Contributor extension discipline.** When adding a new specialist, hook, skip path, or marker class:

1. Append a row to `_data/marker_coverage_surfaces.yaml`'s `surfaces:` list (new surface) or `verdicts:` list (new (marker × surface) intersection — and one row per existing surface for a new marker class).
2. Regenerate the artifact via `cd bmad-autopilot/tools/loud-fail-harness && uv run marker-coverage-audit --regenerate`.
3. Commit `_data/marker_coverage_surfaces.yaml` AND `docs/marker-coverage-audit.md` together — they are co-versioned per the regeneration discipline.

**Complementarity, not replacement.** Per `_bmad-output/planning-artifacts/epics.md` lines 2631-2635: the static audit at `marker_coverage_audit.py` is **complementary** to BOTH Story 1.8's fixture-driven gate (`fr33_fixture_gate.py`) AND Story 6.8's runtime gate (forward-pointer, not yet landed); neither replaces the audit. Each gate covers a distinct gap — fixtures test the harness logic (does the reconciler correctly map skip-events to markers?); runtime tests reference-project completeness (does the actual run produce the expected markers?); the static audit catches code surfaces that have no fixture or runtime evidence yet (gaps in test coverage). Removing any one of the three loses a distinct loud-fail dimension.

**Posture.** The audit is library-as-CLI-aid (mirrors `story-doc-validator`'s posture per `pyproject.toml:25-32`), NOT a CI gate at Story 6.3's landing. The artifact is regenerated manually at epic close OR by future contributors when extending coverage. CI gating is forward-scoped — a future story can add a `marker-coverage-audit` step to `.github/workflows/ci.yml` if drift becomes a recurring failure mode.

### Cost telemetry MVP — boundary-driven OTel pipeline (Story 6.4 resolution)

Story 6.4 lands the per-specialist × per-retry cost-telemetry collection substrate at `tools/loud-fail-harness/src/loud_fail_harness/cost_telemetry.py` per ADR-006 Combo 3 (A3' orchestrator-owned `prompt_id` correlation + B1 operator-managed OTLP backend + C3 hybrid persistence). The substrate is a library sibling of `run_state.py` / `specialist_dispatch.py` — NOT a sixth substrate component beyond ADR-003's enumerated five.

**Production surfaces extended.**

- **`cost-event` event-class production surface** — `cost_telemetry.py` (orchestrator-side `record_cost_event` + `collect`) + `specialist_dispatch.py` (`make_cost_event` schema-validating constructor mirroring `make_specialist_dispatched_event` / `make_specialist_returned_event`). The `cost-event` branch at `schemas/orchestrator-event.yaml:375-416` now has its first production consumers; the schema itself is read-only against this story.
- **`cost-telemetry-unavailable` marker production surface** — `cost_telemetry.derive_cost_telemetry_unavailable_marker` translates `OtelPipelineUnreachable` / `PromptIdCorrelationMissing` exceptions into the `(marker_class, marker_context)` pair per the v1 marker taxonomy at `schemas/marker-taxonomy.yaml:303-312`. Unknown exception types re-raise per the loud-fail doctrine; only the two documented sub-classifications translate.
- **`_data/marker_coverage_surfaces.yaml` post-6.4 file_paths population** — the `cost-telemetry-pipeline` surface entry now carries `file_paths: [tools/loud-fail-harness/src/loud_fail_harness/cost_telemetry.py]`; the `cost-telemetry-unavailable × cost-telemetry-pipeline` row flipped from `verdict: scheduled-by-story` + `discharging_story: "6.4"` to `verdict: emitted` + `code_path: tools/loud-fail-harness/src/loud_fail_harness/cost_telemetry.py:378` per Story 6.3 AC-2's emitted-row shape.
- **`docs/marker-coverage-audit.md` regeneration** — regenerated via `uv run marker-coverage-audit --regenerate` at the post-6.4 working tree (audit date 2026-05-06). The on-disk artifact byte-matches the freshly-rendered output per Story 6.3 AC-3.
- **PR-bundle cost-breakdown section** — `_render_cost_breakdown` in `bundle_assembly.py` (single-rendering-core invariant per Story 5.8 AC-4 / Story 6.1 AC-1; the same sub-renderer is consumed by `bundle_assembly_escalation._render_bundle_body`). Three render branches: green (per-(specialist × retry) markdown table), zero-events sentinel (`## 💸 Cost Breakdown — None`), degraded (`## ⚠️ Cost Breakdown — Telemetry Unavailable` + diagnostic-pointer text + sub-classification line). Structural position: after the loud-fail block, before `## Per-AC results` (merge-ready) / `## Escalation rationale` (escalation).
- **Architecture-doc rationale** — `docs/architecture.md` post-Story-6.4 carries `### Cost telemetry MVP semantics — boundary-driven cadence (Story 6.4 resolution)` recording the four AC-4 verbatim rationale points (NFR-O8 alignment; Claude Code primitive cadence dependency; specialist-timeout bounding; Phase 2 FR-P2 candidate framing).

**Pluggability invariant preserved.** Story 6.4 makes ZERO modifications to `agents/*.md`, `hooks/*.sh`, `skills/bmad-automation/**/*.md`, `thickening_flags.py`, or any pre-existing schema (`marker-taxonomy.yaml`, `orchestrator-event.yaml`, `run-state.yaml`, `envelope.schema.yaml`, `dependencies.yaml`). The substrate adds zero top-level dependencies (the `OtelPipelineProtocol` is a `typing.Protocol` boundary; the orchestrator runtime instantiates a concrete implementation at composition time per ADR-006 Consequence 5's host-Bridge classification).

### In-flight cost streaming + cost-near-ceiling 75% threshold (Story 6.5 resolution)

Story 6.5 lands the in-flight cost-streaming + 75%-of-ceiling threshold-detection substrate at `tools/loud-fail-harness/src/loud_fail_harness/cost_streaming.py` as a NEW SIBLING module composing with Story 6.4's `cost_telemetry.CostAggregation` (zero modifications to `cost_telemetry.py`). The substrate is a library sibling of `cost_telemetry.py` / `event_streaming.py` / `run_state.py` — NOT a sixth substrate component beyond ADR-003's enumerated five.

**Production surfaces extended.**

- **In-flight cost-streaming production surface** — `cost_streaming.py` (`stream_cost_at_boundary` orchestrator-side composition entry point; `format_cost_stream_line` deterministic renderer producing `<HH:MM:SS> [cost] specialist=<kebab> retry=<n> delta=$<x.xx> total=$<y.yy> (<pct>% of $<ceiling.2f>)`; `format_cost_near_ceiling_warning_line` deterministic renderer carrying the literal `"no auto-halt per NFR-O8"` substring per AC-3) + `specialist_dispatch.py` (`record_cost_streaming_at_return_boundary` seam-transition boundary helper composing `cost_telemetry.collect` with `cost_streaming.stream_cost_at_boundary`). The cost-stream line is a derived per-boundary observability render — NOT a schema-validated orchestrator event; the `[cost]` token is a non-event-class diagnostic identifier intentionally distinct from the `event_class` enum.
- **`cost-near-ceiling` marker production surface** — `cost_streaming.evaluate_cost_threshold` detects 75% / 100% threshold crossings via `previous_running_total_usd < threshold AND running_total_usd >= threshold`; consults `already_emitted_markers` to avoid re-emission per Story 1.4's marker-permanence rule; same-boundary 75%+100% jumps emit BOTH markers in ascending-severity order. The marker class itself (taxonomy entry at `schemas/marker-taxonomy.yaml:201-208`) is consumed AS-IS; Story 6.5 makes ONE additive `sub_classifications` enum extension (`[]` → `[ceiling-crossed]`) per Pattern 2's `: <cause>` suffix format — same shape Story 6.4 demonstrated for `cost-telemetry-unavailable`'s two sub-classifications.
- **`_data/marker_coverage_surfaces.yaml` post-6.5 cost-streaming-pipeline coverage** — the `cost-near-ceiling × cost-telemetry-pipeline` row flipped from `verdict: scheduled-by-story` + `discharging_story: "6.5"` to `verdict: emitted` + `code_path: tools/loud-fail-harness/src/loud_fail_harness/cost_streaming.py:282` (the `evaluate_cost_threshold` definition line) per Story 6.3 AC-2's emitted-row shape.
- **`docs/marker-coverage-audit.md` regeneration** — regenerated via `uv run marker-coverage-audit --regenerate` at the post-6.5 working tree (audit date 2026-05-06; emitted-count delta `+1` over the post-6.4 baseline). The on-disk artifact byte-matches the freshly-rendered output per Story 6.3 AC-3.
- **`PerStoryCostCeilingConfigError` exception** — appended to `exceptions.py` after `PromptIdCorrelationMissing` per the per-story append-only convention. Inherits BOTH `ContractViolation` AND `ValueError` mirroring `RetryBudgetConfigError(ValueError)` at `retry_budget.py:120-151` — `ValueError` keeps stdlib `except ValueError` introspection compatible while allowing precise `except PerStoryCostCeilingConfigError` catches at the orchestrator boundary. `resolve_per_story_cost_ceiling_usd` follows `resolve_retry_budget`'s byte-stable input contract; positive int OR positive float is accepted (the per-story ceiling is naturally fractional, unlike `retry_budget`'s int-only contract).
- **Architecture-doc rationale** — `docs/architecture.md` post-Story-6.5 carries `### In-flight cost streaming MVP semantics — boundary-driven cadence + 75% threshold (Story 6.5 resolution)` recording the five AC-4 verbatim rationale points (boundary-cadence framing; NFR-P2 timeout bounding; gap-tolerance as the MVP tradeoff against intra-specialist sampling; no-auto-halt per NFR-O8; Phase 2 FR-P2 candidate framing) and cross-referencing 6.4's H3 to avoid prose duplication.

**Pattern 4 batch-write rule preserved.** Story 6.5 introduces NO parallel atomic-write helper. `record_cost_streaming_at_return_boundary` returns a `tuple[CollectionResult, CostStreamingResult]`; the orchestrator caller composes `CollectionResult.aggregation` (via `cost_telemetry.update_run_state_cost_counters`) + `CostStreamingResult.marker_classifications_to_append` INTO the next-state argument it passes to `advance_run_state` — exactly one atomic write per seam transition.

**Pluggability invariant preserved.** Story 6.5 makes ZERO modifications to `agents/*.md`, `hooks/*.sh`, `skills/bmad-automation/**/*.md`, `thickening_flags.py`, `cost_telemetry.py` (read-only against 6.4's public surface), `bundle_assembly.py` / `bundle_assembly_escalation.py` (6.1's `_render_loud_fail_block` consumes the new markers automatically), `orchestrator-event.yaml`, `run-state.yaml`, `envelope.schema.yaml`, `dependencies.yaml`, or `pyproject.toml` (no new dependency; no new entry-point script — `cost_streaming` is library-only at MVP). The only schema artifact touched is `marker-taxonomy.yaml`'s `cost-near-ceiling` entry for the additive `sub_classifications` enum extension.

### Runtime evidence-trace linkability — bundle-render-time validation (Story 6.6 resolution)

Story 6.6 lands the bundle-render-time evidence-trace linkability validation substrate at `tools/loud-fail-harness/src/loud_fail_harness/evidence_linkability.py` as a NEW SIBLING module composing with Story 4.12's `qa_evidence_persistence` (zero modifications) + Story 5.5's `retry_history` (zero modifications) + Story 4.7/4.8's `$defs/evidence_ref` shape (zero modifications). The substrate is a library sibling of `cost_telemetry.py` / `cost_streaming.py` / `event_streaming.py` / `run_state.py` — NOT a sixth substrate component beyond ADR-003's enumerated five.

**Production surfaces extended.**

- **Bundle-render-time evidence-trace linkability surface** — `evidence_linkability.py` carries the public API: `DanglingEvidenceRef` frozen dataclass with the source-vs-fields invariant (`__post_init__` raises `EvidenceLinkabilityInvariantError` on violation per Pattern 5 / NFR-O5); `detect_dangling_qa_evidence_refs` walks `ac_results[].evidence_refs[].path`; `detect_dangling_retry_history_refs` walks `run_state.retry_history[].path` (compose-not-fork — delegates to `retry_history.detect_dangling_refs` for the actual filesystem check); `compute_dangling_evidence_marker_classifications` partitions the union by source and emits AT MOST TWO `dangling-evidence-ref: <sub>` marker strings in alphabetical order; `format_dangling_inline_marker` produces the inline-at-reference-location text; `validate_evidence_linkability_at_render` is the canonical composition entry point returning a frozen `EvidenceLinkabilityResult` carrying the partitioned dangling refs + the marker classifications to append.
- **`dangling-evidence-ref` marker production surface flipped to bundle-assembler** — the canonical assembler-side emission point moves to `evidence_linkability.py:534` (the `compute_dangling_evidence_marker_classifications` line); the existing CLI-side `retry_history.py:690` `_format_dangling_block` stderr surface remains valid as a SECONDARY emission point for the Story 5.5 `retry-history-resolve` CLI hook code path (preserved as a complementary signal).
- **`marker-taxonomy.yaml` `dangling-evidence-ref` sub_classifications extension** — extended from `[]` to `[qa-evidence, retry-history]` (alphabetical) per Pattern 2's `: <cause>` suffix format. Same shape Story 6.5 demonstrated for `cost-near-ceiling`'s `[ceiling-crossed]` and Story 6.4 demonstrated for `cost-telemetry-unavailable`'s `[otel-pipeline-unreachable, prompt-id-correlation-missing]`. The `marker_class` identifier itself is unchanged; the `diagnostic_pointer` text at lines 220-225 is unchanged; `pointer_context_fields: []` is preserved.
- **`EvidenceLinkabilityInvariantError` exception** — appended to `exceptions.py` after `PerStoryCostCeilingConfigError` per the per-story append-only convention. Inherits BOTH `ContractViolation` AND `ValueError` mirroring `PerStoryCostCeilingConfigError` (Story 6.5) and `RetryBudgetConfigError` (`retry_budget.py:120-151`) — `ValueError` keeps stdlib `except ValueError` introspection compatible while allowing precise `except EvidenceLinkabilityInvariantError` catches at substrate boundaries. The diagnostic surfaces the violated invariant name (`source-vs-fields-mismatch`) per NFR-O5.
- **`bundle_assembly.py` augmentation seams (E1 + E2)** — `_render_per_ac_section` gains a default-empty `qa_evidence_dangling: tuple[DanglingEvidenceRef, ...] = ()` keyword-only parameter; per-evidence-ref bullets matching `(ac_id, path)` against entries in the tuple get an inline `⚠️ dangling-evidence-ref: qa-evidence — Remediation: regenerate the evidence OR fix the reference (path=...)` suffix. `assemble_bundle` adds a NEW composition step BEFORE `_render_loud_fail_block` calling `validate_evidence_linkability_at_render`, then merges the resulting marker tuple into the active_markers passed to BOTH `_render_loud_fail_block` and `_render_cost_breakdown` via the NEW private `_merge_evidence_linkability_markers` helper (de-duplicates by full marker-string equality preserving Story 1.4's marker-permanence rule). NEW `repo_root: pathlib.Path | None = None` keyword-only parameter on `assemble_bundle` — defaults to `pathlib.Path.cwd()` for the evidence-trace linkability validation path. Tests override via the kw arg.
- **`bundle_assembly_escalation.py` mirror seams** — `_render_retry_history` gains a default-empty `retry_history_dangling: tuple[DanglingEvidenceRef, ...] = ()` keyword-only parameter; per-round bullets matching `(round_id, path)` against entries in the tuple get the inline indicator. `assemble_escalation_bundle` adds the same `validate_evidence_linkability_at_render` composition step against `context.retry_history` (the escalation context does NOT carry `ac_results` — that surface lives on the merge-ready bundle's QA envelope; the merged active_markers tuple feeds the loud-fail block; the retry_history_dangling tuple threads into `_render_retry_history`).
- **`_data/marker_coverage_surfaces.yaml` post-6.6 row flip** — the `dangling-evidence-ref × bundle-assembler` row's `code_path` field flipped from `tools/loud-fail-harness/src/loud_fail_harness/retry_history.py:690` to `tools/loud-fail-harness/src/loud_fail_harness/evidence_linkability.py:534` per Story 6.3 AC-2's emitted-row shape. The `verdict: emitted` posture is preserved; `audit_date` bumped to `2026-05-06`.
- **`docs/marker-coverage-audit.md` regeneration** — regenerated via `uv run marker-coverage-audit --regenerate` at the post-6.6 working tree. The on-disk artifact byte-matches the freshly-rendered output per Story 6.3 AC-3; emitted-count is unchanged from post-6.5 (the row was already `emitted` post-5.5; only `code_path` updates).
- **Architecture-doc rationale** — `docs/architecture.md` post-Story-6.6 carries `### Runtime evidence-trace linkability — bundle-render-time validation + dangling-evidence-ref sub-classifications (Story 6.6 resolution)` recording the seven AC-4 verbatim rationale points (NFR-O7 invariant; same-marker-class for both sources per remediation-shape principle; sub-classifications per Pattern 2; markers in BOTH loud-fail block AND inline; Story 5.5 CLI complementarity; visibility-not-enforcement posture; Phase 2 FR-P2 candidate framing).

**Marker-class-reuse principle preserved.** The same `dangling-evidence-ref` marker class handles BOTH QA-evidence and retry-history sources. Remediation is "find or restore the missing artifact" regardless of source — the load-bearing convention Story 1.11 ratified and Story 5.5 extended for retry-history. Sub-classifications preserve diagnostic granularity without proliferating marker classes; per-ref detail is preserved via the inline indicators in the per-AC / per-round body. The loud-fail block carries the source-class summary (one marker per source-class regardless of count within that source) — same decomposition pattern Story 6.4 demonstrated for per-(specialist × retry) cost-breakdown vs single `cost-telemetry-unavailable` marker.

**Pluggability invariant preserved.** Story 6.6 makes ZERO modifications to `agents/*.md`, `hooks/*.sh`, `skills/bmad-automation/**/*.md`, `thickening_flags.py`, `retry_history.py` (Story 6.6 IMPORTS `DANGLING_EVIDENCE_REF_MARKER` + `RetryAttemptRef` + `detect_dangling_refs`), `qa_evidence_persistence.py`, `cost_telemetry.py`, `cost_streaming.py`, `orchestrator-event.yaml`, `run-state.yaml`, `envelope.schema.yaml`, `dependencies.yaml`, or `pyproject.toml` (no new dependency; no new entry-point script — `evidence_linkability` is library-only at MVP). The only schema artifact touched is `marker-taxonomy.yaml`'s `dangling-evidence-ref` entry for the additive `sub_classifications` enum extension.

### Onboarding benchmark (Story 7.9 resolution)

The canonical longitudinal record of the Automator's first-loop performance against NFR-P3's published 5-minute commitment lives at [`docs/onboarding-benchmark.md`](onboarding-benchmark.md). The artifact carries the seven-component breakdown specified by AC-2 (per-component timings — aggregate-only is REJECTED as a deliverable), is generated by `tools/loud-fail-harness/src/loud_fail_harness/onboarding_benchmark.py`, and is consumed by `.github/workflows/release-benchmark.yml` on release branches and tags ONLY.

**Per-release update discipline.** Every Automator release adds ONE row to the artifact's per-release table between the `<!-- benchmark-rows:begin -->` / `<!-- benchmark-rows:end -->` anchor markers. New rows are inserted IMMEDIATELY before the end marker; existing rows are NEVER reordered (append-only, mirroring `docs/marker-coverage-audit.md` and the per-convention table at this document's `## Per-convention table`). Missed-target releases include EITHER (a) a one-line remediation summary in the `Notes` column OR (b) a deferral entry pointing at the follow-up issue per AC-4 verbatim ("missed-target releases include a remediation note OR a deferral entry referencing a follow-up issue"). The `Missed Component` column names the field with the largest contribution to the overage; when the argmax-component is itself within its historical-mean budget, the field name is suffixed with `+aggregate-overage` to surface the death-by-a-thousand-cuts case (`epics.md:3169`).

**Regeneration command.** `cd bmad-autopilot/tools/loud-fail-harness && uv run onboarding-benchmark --hardware-tier <tier> --version <release>` — invoked from `.github/workflows/release-benchmark.yml` on every release-branch push and release-tag publication. The `--hardware-tier` flag is REQUIRED because hardware tier is a coarse human-judgment label, not machine-detectable; the remaining environment-notes columns (Claude Code version, OS, Python) are auto-probed at run-time per AC-5's reproducibility contract.

**Relationship to NFR-P3 + FR44.** NFR-P3 (`prd.md:936`) is the published commitment: "≤ 5 minutes from `/bmad-automation init` completion to first successful sample-story loop merge-ready". FR44 (`prd.md:869`) is the user-facing functional requirement: "First-run complete story loop on the sample story succeeds in ≤ 5 minutes on a typical developer laptop (onboarding-time target)". The benchmark IS the release-gating mechanism for the NFR-P3 commitment — exit-code `1` on missed target gates the release per the discipline of the published commitment (`epics.md:3175`).

**Relationship to Story 7.4's sample-story fixture.** The benchmark fixture is `tools/loud-fail-harness/src/loud_fail_harness/_data/sample-auto-001.md` — Story 7.4's canonical sample-story content, loaded via `loud_fail_harness.sample_story_scaffold.load_sample_story_content`. The fixture is NEVER modified by the benchmark. Modifications to the fixture INVALIDATE prior-row comparability — a fixture change requires explicit versioning and a notes-column annotation on the first post-bump row so trend-analysis readers can tell why the timings shifted (per AC-6's longitudinal-trend-visibility commitment).

**Posture.** The benchmark is library-as-CLI-aid (mirrors `marker-coverage-audit`'s posture per `pyproject.toml` lines 70-74), invoked at release time ONLY (NOT per PR). The release-cadence cost-vs-coverage tradeoff (AC-5) makes per-PR full first-loop benchmarks economically infeasible: every PR running ≈5 minutes of Anthropic API time would be cost-prohibitive without proportional coverage gain. The release-CI workflow consumes the `ANTHROPIC_API_KEY` repository secret to authenticate the `claude` CLI invocation that drives the orchestrator → Dev → Review-BMAD → QA → bundle-assembler chain.

**Story 7.9 INTRODUCES NO new substrate component, NO new marker class, NO new external Python dependency.** The substrate-component count (ADR-003 Consequence 1) remains FIVE; `onboarding_benchmark.py` is a CONSUMER LIBRARY parallel to `marker_coverage_audit.py` / `init_non_destructive_guard.py` / `tea_boundary_orientation.py`. The marker taxonomy at `schemas/marker-taxonomy.yaml` is consumed AS-IS at all 25 v1 closed classes — Story 7.9 emits NO marker (per Story 1.11's atomic-vs-aggregated principle, markers signal atomic failure surfaces, not informational signals; release-time observations are out of taxonomy scope). No new entry in `_data/marker_coverage_surfaces.yaml` is required.

## Orchestrator-side marker wiring (Story 6.7 close, 2026-05-06)

Story 6.7 lands the canonical orchestrator-side wiring for three previously-raised-but-not-persisted skip events: `specialist-timeout` (NFR-P2), `hook-failed: <hook-name>` (NFR-R6), and `context-near-limit: <specialist>` (NFR-P4). The substrate provides recorder helpers + dispatch-boundary composition seams + render-time alphabetical normalization; the orchestrator-skill (LLM-runtime) decides WHEN to invoke per the sensor-not-advisor posture. The same landing discharges D-6.2-1 deferred-work for `Tier-3-not-configured` and `playwright-mcp-unavailable`'s `marker_contexts` population.

- **`marker_wiring.py` substrate module** — NEW sibling of `cost_telemetry.py` / `cost_streaming.py` / `evidence_linkability.py` / `event_streaming.py` / `run_state.py` per architecture.md lines 311-315. NOT a sixth substrate component beyond ADR-003's enumerated five — a runtime helper composed by the dispatch wrapper / hook-result handler / QA wrapper / playwright driver. Public surface: three named recorders (`record_specialist_timeout_marker`, `record_hook_failure_marker`, `record_context_near_limit_marker`); generic `record_marker_with_context` (the canonical pattern Story 6.2 left as a forward-looking helper); render-time normalizer `compute_alphabetical_marker_order`; three `Literal` typing aliases (`HookName`, `SpecialistName`, `SpecialistTimeoutSubCause`) + three tuple-form enumerations (`HOOK_NAMES`, `SPECIALIST_NAMES`, plus the per-class `SPECIALIST_TIMEOUT_MARKER` / `HOOK_FAILED_MARKER` / `CONTEXT_NEAR_LIMIT_MARKER` constants). Pure functions throughout — no I/O; the recorders return a NEW `RunState` (frozen Pydantic `model_copy(update=...)`) for the caller to compose with `advance_run_state` per Pattern 4's batch-write rule. Marker-permanence rule (Story 1.4): per-recorder de-dup by full marker-string equality.
- **`specialist_dispatch.py` boundary helpers** — TWO new public helpers added to the existing 17-name `__all__` (post-6.7 baseline: 19 names): (a) `handle_specialist_timeout_at_dispatch_boundary(*, exception, run_state, marker_recorder?)` for the caught `SpecialistTimeoutExceeded` path; (b) `handle_context_near_limit_at_dispatch_boundary(*, is_near_limit, specialist, run_state, marker_recorder?)` for the `is_context_near_limit` callable signal source. Both use lazy import of `marker_wiring.record_*_marker` to avoid the indirect circular dependency (`marker_wiring` imports `MarkerClassRegistry` from `specialist_dispatch`). Neither helper calls `advance_run_state` — Pattern 4's batch-write rule keeps run-state writes batched at the orchestrator-skill layer. The intra-package import allowlist gains `loud_fail_harness.marker_wiring` (test enforced via `_INTRA_PACKAGE_IMPORT_ALLOWLIST`).
- **`orchestrator_run_entry.py` hook-result helper** — ONE new public helper: `handle_hook_exit_code(*, exit_code, hook_name, run_state)` returns the input run-state unchanged on zero-exit (cheap happy path) OR composes with `record_hook_failure_marker` on non-zero. Lazy import of `marker_wiring`. The `__all__` count bumps from 18 to 19. The hook scripts at `hooks/{subagent-stop,stop,session-start}.sh` are unchanged — the orchestrator-side handler takes `hook_name` as an explicit parameter (sourced from the invocation context, NOT parsed from stderr); the canonical hook-name stderr prefix `subagent-stop:` already exists in `subagent-stop.sh` for practitioner ergonomics in shell logs.
- **`bundle_assembly.py` `_render_loud_fail_block` augmentation** — ONE new import (`compute_alphabetical_marker_order`) + ONE iteration-seam wrapper changing `for marker_class in active_markers:` to `for marker_class in compute_alphabetical_marker_order(active_markers):`. The persistent `run_state.active_markers` tuple is unchanged per Story 1.4's marker-permanence rule; only the rendered iteration order is normalized for stable display per AC-4. Existing fixtures with 0 or 1 marker (or pre-alphabetical multi-marker layouts) byte-match their pre-6.7 outputs; the alphabetical contract is structural for the multi-marker cross-story case.
- **`marker-taxonomy.yaml` two additive entry extensions** — (T1) `hook-failed.sub_classifications` extends from `[non-zero-exit, timeout, missing-binary]` to `[missing-binary, non-zero-exit, session-start, stop, subagent-stop, timeout]` (alphabetical merge — three additive entries; the existing three preserved per Story 1.4's marker-permanence rule which forbids removing taxonomy entries). The runtime-emitted suffix is the hook-name per AC-2 verbatim. (T2) `context-near-limit` gains `pointer_context_fields: [specialist]` + `sub_classifications: [dev, qa, review-bmad]` (alphabetical) + an interpolated `diagnostic_pointer` text incorporating `{specialist}` and a remediation hint pointing at `docs/architecture.md` § Context-budget management. Same shape Story 6.5 ratified for `cost-near-ceiling`'s `[ceiling-crossed]` extension AND Story 6.6 ratified for `dangling-evidence-ref`'s `[qa-evidence, retry-history]` extension.
- **D-6.2-1 discharge — `qa_evidence_tier.py` + `playwright_driver.py` companion helpers** — TWO new helpers compose with `record_marker_with_context` to populate `marker_contexts` from caller-supplied context. (D1) `qa_evidence_tier.record_tier_3_not_configured_in_run_state(*, run_state, ac_id, marker_registry?)` populates `{ac_id}` per the taxonomy's `pointer_context_fields: [ac_id]` declaration. (D2) `playwright_driver.record_playwright_mcp_unavailable_in_run_state(*, run_state, project_type, version_range, marker_registry?)` populates `{project_type, version_range}`. Both add `RunState` under `TYPE_CHECKING` to avoid circular imports; both use lazy import of `marker_wiring.record_marker_with_context`. The `__all__` of each module gains the new export; `qa_evidence_tier`'s strict-count test bumps from 11 to 12 names; `playwright_driver`'s `__all__` is permissive.
- **`_data/marker_coverage_surfaces.yaml` seven row flips** — 5 6.7-specific rows + 2 D-6.2-1 unique rows (the `specialist-timeout × orchestrator-run-state-helper` row counts in BOTH groups) flipped from `verdict: scheduled-by-story` + `discharging_story: "6.7"` + rationale to `verdict: emitted` + `code_path: <canonical>`. The `audit_date` bumps to `2026-05-06`. Post-6.7 audit summary: 406 intersections; 29 emitted (was 22); 369 not-applicable; 8 scheduled-by-story (remaining stories: 6.8 / 6.9 + Epics 7-8). Zero new gaps.
- **`docs/marker-coverage-audit.md` regeneration** — regenerated via `uv run marker-coverage-audit --regenerate` at the post-6.7 working tree. The on-disk artifact byte-matches the freshly-rendered output per Story 6.3 AC-3.
- **Architecture-doc rationale** — `docs/architecture.md` post-Story-6.7 carries `### Orchestrator-side marker wiring — specialist-timeout / hook-failed / context-near-limit persistence + alphabetical render order (Story 6.7 resolution)` recording the canonical recording surfaces + composition seams + order-stable rendering split + sensor-not-advisor signal-source posture + visibility-not-enforcement loud-fail-doctrine framing + the context-budget management remediation reference + the additive-substrate composition discipline.

**Marker-class-reuse principle preserved.** ZERO new marker classes introduced. The three target marker classes were already in the v1 closed taxonomy (Story 1.4); Story 6.7 only WIRES them at runtime. The two additive `sub_classifications` enum extensions (`hook-failed`, `context-near-limit`) follow the convention Stories 6.5 / 6.6 ratified for `cost-near-ceiling` + `dangling-evidence-ref`.

**Sensor-not-advisor about the signal source.** The substrate is intentionally agnostic about WHERE the context-near-limit signal originates. The dispatch-wrapper layer's `is_context_near_limit: Callable[[SpecialistEnvelope], bool] | None = None` parameter accepts (a) an explicit envelope field, (b) a runtime token-count estimate, (c) a Claude Code primitive callback, or (d) some other future signal. Phase 2 thickening (e.g., a canonical envelope field; an automated probe) is a forward-looking FR-P2 candidate.

**Pluggability invariant preserved.** Story 6.7 makes ZERO modifications to `agents/*.md`, `skills/bmad-automation/**/*.md`, `thickening_flags.py`, `retry_history.py`, `evidence_linkability.py`, `cost_telemetry.py`, `cost_streaming.py`, `qa_evidence_persistence.py`, `orchestrator-event.yaml`, `run-state.yaml`, `envelope.schema.yaml`, `dependencies.yaml`, or `pyproject.toml` (no new dependency; no new entry-point script). Hook scripts at `hooks/{subagent-stop,stop,session-start}.sh` are unchanged — the orchestrator-side hook-result handler sensors exit codes via the explicit `hook_name` parameter, not by parsing stderr text. The four thickening flags (`thickening_flags.py`) remain `True` post-6.7. The only schema artifacts touched are `marker-taxonomy.yaml`'s `hook-failed` and `context-near-limit` entries for the additive enum + pointer-context-fields extensions.

## FR33 runtime reconciliation gate (Story 6.8 close, 2026-05-07)

Story 6.8 lands the FR33 runtime reconciliation CI gate at `tools/loud-fail-harness/src/loud_fail_harness/fr33_runtime_gate.py` as a NEW SIBLING module to Story 1.8's `fr33_fixture_gate.py`. Both gates compose with substrate-component-3's `reconciler.reconcile` (Story 1.4) WITHOUT modification per the shared-reconciler invariant ratified at Story 1.8 AC-4 + Story 6.8 AC-1 (epics.md line 905 + line 2783). The substrate is a CI gate sibling — NOT a sixth substrate component beyond ADR-003's enumerated five at architecture.md lines 311-315.

- **Substrate gate purpose** — `fr33_runtime_gate.py` carries the public API: `RuntimeReplaySource: Final[str]` provenance constant; `RuntimeFinding` / `RuntimeReference` / `RuntimeGateResult` Pydantic v2 frozen dataclasses with the four-bucket partitioning (passing / runtime_reconciliation_mismatch / schema_shape_broken / marker_taxonomy_mismatch); `extract_runtime_skip_events(events, *, event_schema, file_path)` filtering captured events.jsonl entries by the optional `marker_class` payload field with shape-broken entries routed to `schema-shape-broken` findings; `extract_runtime_markers(active_markers, *, story_id, taxonomy, file_path)` constructing `Marker` instances from the captured run-state's `active_markers` tuple with unknown-taxonomy entries routed to `marker-taxonomy-mismatch` findings; `replay_runtime_capture(events_path, run_state_path, taxonomy, event_schema)` calling the SHARED `reconciler.reconcile` and partitioning `silent_skips` into `runtime_reconciliation_mismatch` findings using the AC-2 verbatim diagnostic template; `run_fr33_runtime_gate(captures, taxonomy, event_schema, *, captures_root)` aggregating per-capture findings with sorted-output discipline parallel to Story 1.8's `(file_path, marker_class)` lex-sort precedent; `format_findings` rendering the NEW `## Runtime reconciliation findings` section header per AC-2's distinct-shape diagnostics commitment; `_build_arg_parser` + `main` mirroring `fr33_fixture_gate.main`'s shape with the three-tier exit-code triage (0 clean / 1 reconciliation-mismatch / 2 schema-or-taxonomy-mismatch).

- **Shared-reconciler invariant** — `reconciler.reconcile` IS the SINGLE substrate-component-3 surface. Both `fr33_runtime_gate.py` and `fr33_fixture_gate.py` import it directly per the within-module-only-coupling discipline (story 1.8 + 6.8 use identical import statements `from loud_fail_harness.reconciler import (...) reconcile (...)`). The shared-reconciler invariant is witnessed by `tests/test_fr33_runtime_gate.py::test_shared_reconciler_invariant_both_gates_same_callable` — both gates' `reconcile` references resolve to the SAME callable object via Python's module-attribute identity check. Future contributors extending FR33 enforcement compose with `reconcile` WITHOUT branching the matching logic.

- **Runtime-shape diagnostic distinct from fixture-gate** — the runtime gate's CI-failure message format is structurally distinct from the fixture gate's: section header `## Runtime reconciliation findings` (NOT in fixture gate's output); message template `Runtime reconciliation failed: reference-project run produced skip-event class `<X>` at code surface `<surface>` but no emitted marker reconciled.` (NOT containing the fixture gate's `Fixture reconciliation failed: synthetic-story` substring per AC-2's "their failure signatures are inspectable separately" commitment). Exit-code triage is distinct: runtime gate exit 2 = schema-shape-broken OR marker-taxonomy-mismatch; exit 1 = runtime-reconciliation-mismatch; clean = exit 0 (vs. fixture gate's harness-bug-wins precedence). Debuggers grep for the runtime header to identify which gate fired without re-deriving from the marker-class string (Story 1.11's flat-routing principle).

- **Capture-fixture corpus (split across two sibling directories per AC-7)** — TWO MVP captures committed: (F1) **happy-path corpus** at `tools/loud-fail-harness/tests/fixtures/runtime-captures/clean/` carrying `events.jsonl` + `run-state.yaml` with TWO declared skip-events (`LAD-skipped` per Story 4.7 + `Tier-3-not-configured` per Story 4.8) reconciling cleanly to TWO matching markers in `active_markers` — exercises the gate's exit-0 path; default-globbed by the CI step. (F2) **failure-case corpus** at `tools/loud-fail-harness/tests/fixtures/runtime-captures-failure-cases/missing-emission/` carrying the same shape but `run-state.yaml`'s `active_markers` is missing `Tier-3-not-configured` — exercises the gate's exit-1 path with the AC-2 verbatim diagnostic naming the missing marker class + the captured `qa_evidence_tier.py:354` code surface; NOT default-globbed (referenced explicitly by unit tests). The split keeps AC-7's "CI step exits 0 on green build" requirement clean while preserving the "missing-emission gate exits 1" invariant for unit tests. The synthesis convention is documented at `tools/loud-fail-harness/tests/fixtures/runtime-captures/README.md`: events.jsonl canonical shape (Story 2.12); run-state.yaml canonical shape (Story 2.2); `marker_class` and `emission_site` payload-extension semantics; happy-path-vs-failure-case corpus split; minimization convention (one capture-directory = one minimized real-run footprint; trim non-skip events for readability while preserving reconciliation completeness); default-glob path picks up new HAPPY-PATH capture directories automatically — no test code change required to extend happy-path coverage.

- **Capture-format extension fields (relaxed shape vs canonical schema)** — the runtime gate validates SHAPE via the four common required fields (`event_class`, `event_id`, `timestamp`, `story_id`) rather than calling `validate_event` against `schemas/orchestrator-event.yaml` (which would loud-fail on every `marker_class`-bearing skip-event entry per the canonical schema's `additionalProperties: false` constraint). The `event_schema` argument is accepted for symmetry with story 1.8's contract and for future Phase-2 promotion of `marker_class` / `emission_site` to canonical schema fields without changing the public surface. Tracked in `deferred-work.md` as Phase 2 thickening.

- **`marker-taxonomy.yaml` `reconciler-mismatch-runtime` entry extension** — extended additively per Story 1.4's marker-permanence rule: `pointer_context_fields` extends from `[]` to `[code_surface, skip_event_class]` (alphabetical per Pattern 1's identifier-list convention); `diagnostic_pointer` text gains `{skip_event_class}` and `{code_surface}` placeholders for the AC-2 verbatim interpolation + a closing remediation hint pointing at `docs/architecture.md` § Runtime gate input contract; `sub_classifications` stays `[]` (the diagnostic granularity is carried via the two pointer-context-field interpolations rather than via sub-classifications — runtime mismatch is one mode with two interpolated dimensions). The convention follows Stories 6.5 / 6.6 / 6.7 ratified for additive entry extensions.

- **New `pyproject.toml` entry-point** — `fr33-runtime-gate = "loud_fail_harness.fr33_runtime_gate:main"` added immediately after the existing `fr33-fixture-gate` line, preserving the FR33-pair grouping. The story-1.8 → story-6.8 alphabetical-suffix sequencing within the `fr33-*` entry-point pair is intentional.

- **New CI step `FR33 runtime reconciliation gate (story 6.8)`** — added after the existing `FR33 fixture-driven gate` step in `.github/workflows/ci.yml`. Comment block documents that this gate is paired-but-distinct from the fixture-driven gate per Story 6.8 AC-2: different input source (captured runs vs synthetic fixtures), same reconciler component, distinct exit-code triage, distinct CI-failure diagnostic shape. No positional arguments — the script defaults to globbing `tests/fixtures/runtime-captures/*/`.

- **`_data/marker_coverage_surfaces.yaml` ONE row flip** — the `reconciler-mismatch-runtime × reconciliation-gate-runtime` row at lines 2042-2046 flipped from `verdict: scheduled-by-story` + `discharging_story: "6.8"` + rationale to `verdict: emitted` + `code_path: tools/loud-fail-harness/src/loud_fail_harness/fr33_runtime_gate.py:672` (the `run_fr33_runtime_gate` definition line) per Story 6.3 AC-2's emitted-row shape. The `audit_date` bumps to `2026-05-07`. The `surfaces:` entry for `reconciliation-gate-runtime` also flips its `file_paths` from `[]` to the new gate module path. Post-6.8 audit summary: 406 intersections; 30 emitted (was 29 post-6.7); 369 not-applicable; 7 scheduled-by-story (remaining stories: 6.9 + Epics 7-8). Zero new gaps.

- **`docs/marker-coverage-audit.md` regeneration** — regenerated via `uv run marker-coverage-audit --regenerate` at the post-6.8 working tree. The on-disk artifact byte-matches the freshly-rendered output per Story 6.3 AC-3.

- **Architecture-doc rationale** — `docs/architecture.md` post-Story-6.8 carries `### FR33 runtime reconciliation gate — shared reconciler component, runtime-shape diagnostic, captured-input contract (Story 6.8 resolution)` recording the five AC-1 + AC-2 + AC-3 + AC-4 verbatim rationale points (shared-reconciler invariant; runtime gate input contract; `reconciler-mismatch-runtime` taxonomy entry extension; distinct-shape diagnostics; Phase 2 thickening directions tracked in `deferred-work.md`).

**Marker-class-reuse principle preserved.** ZERO new marker classes introduced by Story 6.8. The `reconciler-mismatch-runtime` marker class was reserved in the v1 closed taxonomy by Story 1.4 for this exact purpose; Story 6.8 only WIRES it at the runtime gate. The additive entry extension (`pointer_context_fields` + interpolated `diagnostic_pointer`) follows the convention Stories 6.5 / 6.6 / 6.7 ratified for `cost-near-ceiling` + `dangling-evidence-ref` + `hook-failed` + `context-near-limit`.

**Pluggability invariant preserved.** Story 6.8 makes ZERO modifications to `agents/*.md`, `hooks/*.sh`, `skills/bmad-automation/**/*.md`, `thickening_flags.py`, `reconciler.py` (the substrate-component-3 surface is reused as-is), `fr33_fixture_gate.py` (the fixture gate continues to operate independently), `marker_wiring.py` / `evidence_linkability.py` / `cost_telemetry.py` / `cost_streaming.py` / `retry_history.py` (orthogonal — no overlap), `orchestrator-event.yaml`, `run-state.yaml`, `envelope.schema.yaml`, or `dependencies.yaml`. The four thickening flags (`thickening_flags.py`) remain `True` post-6.8. The only schema artifact touched is `marker-taxonomy.yaml`'s `reconciler-mismatch-runtime` entry for the additive `pointer_context_fields` + interpolated `diagnostic_pointer` extension. The only `pyproject.toml` change is the additive `fr33-runtime-gate` entry-point script registration.

## Bundle-assembly failure detection (Story 6.9 close, 2026-05-07)

Story 6.9 lands the orchestrator-side substrate library that detects bundle-assembly logical failures at `bundle_assembly.main`'s entry-point seam and surfaces them via the `bundle-assembly-failed` marker across three reinforcing channels (FR59 + NFR-O5). The substrate is a library sibling of `review_layer_failure.py` / `marker_wiring.py` / `cost_telemetry.py` / `cost_streaming.py` / `evidence_linkability.py` per architecture.md lines 311-315 — NOT a sixth substrate component beyond ADR-003's enumerated five. The implementation mirrors Story 3.3's `surface_failed_layers` source-of-truth-with-CI-lint pattern byte-for-byte: a single emission function PLUS a structural-lint gate that prevents any developer from emitting one channel without the others.

- **`bundle_assembly_failure.py` substrate library** — NEW sibling of `review_layer_failure.py`. Public API: `BUNDLE_ASSEMBLY_FAILED_MARKER` (kebab-case constant), `BUNDLE_ASSEMBLY_FAILED_EXIT_CODE: Final[int] = 2` (the AC-3 cross-failure-matrix discriminator), `AssemblyFailureStep` (Literal alias enumerating the five failure modes from epics.md line 2818 verbatim — `envelope-mismatch`, `finding-render-crash`, `internal-exception`, `missing-finding-fields`, `taxonomy-unresolved`), `AssemblyFailureRecord` (frozen dataclass carrying the captured failure context), `classify_assembly_failure(exc, partial_bundle_path?)` (pure helper mapping each documented exception class to its `AssemblyFailureStep`), `surface_assembly_failure(*, story_id, run_id, run_state_path, bundle_root, exc, failed_step, partial_bundle_path?, registry?, marker_recorder?, stderr?)` (the SINGLE source-of-truth emission path with validate-then-mutate atomicity per Pattern 5; mirrors `surface_failed_layers`'s pre-loop validation discipline). The `marker_recorder` keyword-only injection seam mirrors the convention from Story 6.7's `record_*` helpers; default `record_marker_with_context`.

- **`bundle_assembly_failure_emission_gate.py` CI-lint gate** — NEW module mirroring `review_layer_failure_emission_gate.py` byte-for-byte. Public API: `ForbiddenEmissionFinding` (Pydantic frozen model — file_path, line_number, rule, matched_text), `GateResult` (Pydantic frozen model — scanned_files, forbidden_emission_violation), `_scan_file_for_violations`, `_discover_scan_targets`, `run_gate(repo_root) -> GateResult`, `format_findings(result) -> str`, `_build_arg_parser`, `main(argv) -> int`. Two regex rules: (a) marker-literal — any string literal `"bundle-assembly-failed"` outside the source-of-truth module is a violation; (b) fallback-file-suffix — any direct mention of the `*.assembly-failure.log` suffix outside the source-of-truth module is a violation. Allowlist: the source-of-truth module itself + the gate module itself. Exit codes: 0 clean / 1 invariant violation / 2 harness-level error.

- **`bundle_assembly.py:main` augmentation** — ONE new import block (3 names: `BUNDLE_ASSEMBLY_FAILED_EXIT_CODE`, `classify_assembly_failure`, `surface_assembly_failure`); ONE rewrite of `main`'s try/except. The existing pre-condition triage (`SpecialistDispatchLogNotFound`, `RunStateStoryIdMismatch`) remains at exit 1 WITHOUT emitting the marker per the remediation-shape principle (epics.md lines 2841-2842 — those are pre-condition contract violations, not assembler-logic failures). `EnvelopeReValidationFailed` and `UnknownMarkerClass` previously caught by the inner triage are now classified as assembler-logic failures via `classify_assembly_failure` (mapping to `envelope-mismatch` and `taxonomy-unresolved` respectively), routed through `surface_assembly_failure`, and exit 2. `SystemExit` and `KeyboardInterrupt` propagate unmodified per Pattern 5. The new outer `except BaseException` carries a per-line `noqa: BLE001` (Story 6.9 outer catchall is the documented architectural pattern).

- **`orchestrator_run_entry.py:handle_hook_exit_code` augmentation** — ONE new conditional gating the `record_hook_failure_marker` invocation: when `hook_name == "stop"` AND `exit_code == BUNDLE_ASSEMBLY_FAILED_EXIT_CODE` (=2), the helper returns the input run-state UNCHANGED — no `hook-failed: stop` marker is appended (the assembler already emitted `bundle-assembly-failed` via `surface_assembly_failure`). For ANY other non-zero exit code under `stop` — and for non-zero exits under `subagent-stop` / `session-start` regardless of code — the helper invokes `record_hook_failure_marker` per the existing path. Cross-failure independence preserved (AC-3): when both surfaces fail, both markers fire because the Stop hook's mechanical-failure exit code is non-zero AND not equal to 2 (e.g., 1 from `set -e` failure, 127 from missing `python3`). Lazy import of `BUNDLE_ASSEMBLY_FAILED_EXIT_CODE` avoids the circular dependency through `bundle_assembly_failure → marker_wiring`.

- **`marker-taxonomy.yaml` ONE additive entry extension** — `bundle-assembly-failed` entry extends from `pointer_context_fields: []` and `sub_classifications: []` to `pointer_context_fields: [exception_type, failed_step, run_id, story_id]` (alphabetical; populated by `surface_assembly_failure`'s `marker_contexts` write) AND `sub_classifications: [envelope-mismatch, finding-render-crash, internal-exception, missing-finding-fields, taxonomy-unresolved]` (alphabetical merge of the five failure modes from epics.md line 2818 verbatim). The existing two-sentence `diagnostic_pointer` text is preserved verbatim per Story 1.4's marker-permanence rule and AUGMENTED with placeholder interpolation per Story 6.2's enrichment convention: appends `Step: {failed_step}. Story: {story_id}. Run: {run_id}. Exception: {exception_type}. See _bmad-output/pr-bundles/{story_id}/{run_id}.assembly-failure.log for full traceback.` PLUS the remediation-shape distinction text (hook-failed vs bundle-assembly-failed). Same shape Stories 6.5 / 6.6 / 6.7 ratified for `cost-near-ceiling` / `dangling-evidence-ref` / `context-near-limit`.

- **`_data/marker_coverage_surfaces.yaml` TWO row flips** — (R1) `bundle-assembly-failed × bundle-assembler` flipped from `verdict: scheduled-by-story` + `discharging_story: "6.9"` + rationale to `verdict: emitted` + `code_path: tools/loud-fail-harness/src/loud_fail_harness/bundle_assembly_failure.py:354` (the `surface_assembly_failure` definition line — within the audit's ±25-line emission-reference window of `BUNDLE_ASSEMBLY_FAILED_MARKER` references in the function body). (R2) `bundle-assembly-failed × hook-stop` flipped from `verdict: scheduled-by-story` + `discharging_story: "6.9"` + rationale to `verdict: emitted` + `code_path: tools/loud-fail-harness/src/loud_fail_harness/orchestrator_run_entry.py:1290` (the new exit-code-2 conditional inside `handle_hook_exit_code`). Note: the story spec called for a `<file>:<line> + <file>:<line>` multi-path syntax, but the audit module's `_ParsedCodePath.parse` only supports a single `<file>:<line>` form — single canonical paths are used, with the architecture-doc and extension-audit-doc rationale capturing the full multi-surface composition. The `audit_date` bumps to `2026-05-07`. Post-6.9 audit summary: 406 intersections; 32 emitted (was 30 post-6.8); 369 not-applicable; 5 scheduled-by-story (remaining: Epics 7-8 only). Zero new gaps.

- **`docs/marker-coverage-audit.md` regeneration** — regenerated via `uv run marker-coverage-audit --regenerate` at the post-6.9 working tree. The on-disk artifact byte-matches the freshly-rendered output per Story 6.3 AC-3.

- **New `pyproject.toml` entry-point** — `bundle-assembly-failure-emission-gate = "loud_fail_harness.bundle_assembly_failure_emission_gate:main"` added immediately after the existing `review-layer-failure-emission-gate` line, preserving the emission-gate landing-order grouping per the file's preceding comment-block convention.

- **New CI step `bundle-assembly-failure-emission-gate (story 6.9)`** — added after the existing `review-layer-failure-emission-gate` step in `.github/workflows/ci.yml`. The step's failure exits CI non-zero per Pattern 5.

- **Canonical assembly-failure fixture** — NEW `examples/assembly-failures/sample-assembly-failure.log` byte-identical to the post-6.9 `surface_assembly_failure` output for a seeded `envelope-mismatch` failure (with `generated_at` timestamp normalized to `2026-05-07T00:00:00Z`). NEW `examples/assembly-failures/README.md` briefly documenting the structured-text format. Canonical-fixture regression test asserts byte-identity per Story 6.1's fixture-regression discipline.

- **Architecture-doc rationale** — `docs/architecture.md` post-Story-6.9 carries `### Bundle-assembly failure detection — three-channel atomic emission for `bundle-assembly-failed` (Story 6.9 resolution)` recording the five-item rationale: (1) three reinforcing channels and the canonical fallback-file path; (2) source-of-truth-with-CI-lint structural enforcement per Story 3.3's precedent; (3) exit-code-2 convention distinguishing assembler-logic vs. Stop-hook-mechanical failure; (4) pre-condition failures stay at exit 1 without emitting the marker; (5) Phase 2 thickening directions (compound sub-classifications; graceful-degradation rendering) tracked in `deferred-work.md`.

**Marker-class-reuse principle preserved.** ZERO new marker classes introduced by Story 6.9. The `bundle-assembly-failed` marker class was reserved in the v1 closed taxonomy by Story 1.4 for this exact purpose; Story 6.9 only WIRES it at runtime. The additive entry extension (`pointer_context_fields` + `sub_classifications` + interpolated `diagnostic_pointer`) follows the convention Stories 6.5 / 6.6 / 6.7 / 6.8 ratified for `cost-near-ceiling` + `dangling-evidence-ref` + `hook-failed` + `context-near-limit` + `reconciler-mismatch-runtime`.

**Pluggability invariant preserved.** Story 6.9 makes ZERO modifications to `agents/*.md`, `hooks/*.sh`, `skills/bmad-automation/**/*.md`, `thickening_flags.py`, `marker_wiring.py`, `review_layer_failure.py`, `cost_telemetry.py`, `cost_streaming.py`, `evidence_linkability.py`, `retry_history.py`, `orchestrator-event.yaml`, `run-state.yaml`, `envelope.schema.yaml`, `dependencies.yaml`, or `assemble_bundle`'s success path inside `bundle_assembly.py`. The four thickening flags (`thickening_flags.py`) remain `True` post-6.9. The only schema artifact touched is `marker-taxonomy.yaml`'s `bundle-assembly-failed` entry for the additive `pointer_context_fields` + `sub_classifications` + interpolated `diagnostic_pointer` extension. The only `pyproject.toml` change is the additive `bundle-assembly-failure-emission-gate` entry-point script registration. The only `.github/workflows/ci.yml` change is the additive new gate-invocation step. Epic 6 closes with all 9 stories `done`.

### MVP completion evidence (Story 8.7)

The canonical "MVP completion evidence" artifact lives at [`docs/mvp-completion-evidence.md`](mvp-completion-evidence.md). It maps every MVP FR (68 — FR1-FR66 + sub-letters minus FR29) and every NFR (34) to the user journey that exercised it, the observable behavior demonstrated, and an evidence link. The artifact is generated and validated by `tools/loud-fail-harness/src/loud_fail_harness/mvp_completion_evidence.py` per Story 8.7 AC-4 + AC-8; the per-journey evidence captures live under `docs/mvp-completion-evidence/journey-<n>/` per AC-6.

**Post-MVP per-FR/NFR-add cadence.** When a Phase-1.5 / Phase-2 story adds a new FR or NFR enumerated in `prd.md`, the validator at `tools/loud-fail-harness/src/loud_fail_harness/mvp_completion_evidence.py` will fail CI with a `missing-requirement-row` finding. The dev adding the FR/NFR is responsible for:

1. Adding the corresponding row to `docs/mvp-completion-evidence.md`'s coverage matrix (between the `<!-- coverage-rows:begin -->` and `<!-- coverage-rows:end -->` anchors), populating all five required columns.
2. Capturing the journey evidence under `docs/mvp-completion-evidence/journey-<n>/` if a new artifact is required (e.g., a new envelope shape OR a new bundle variant).
3. Re-running `uv run mvp-completion-evidence` from `bmad-autopilot/tools/loud-fail-harness/` until 0 findings.
4. Committing the artifact + per-journey captures together.

The post-MVP cadence is **review-enforced** (mirrors `extension-audit.md`'s overall posture per ADR-003 line 292) NOT auto-enforced beyond the validator gate's structural assertion that EVERY enumerated FR/NFR has a row. The dev adding the FR is responsible for adding the row; the validator only signals missing rows, never silently auto-generates them.

**Discipline rationale.** The artifact's bus-factor-mitigation function (per `epics.md:3420-3423`) requires that every MVP FR/NFR retains observable-behavior traceability across project lifetime. Without the per-FR/NFR-add cadence, post-MVP additions would erode the artifact's completeness over time; the CI gate makes erosion visible immediately rather than at the next MVP-cut-equivalent event.

## Per-component additions

Append-only table of substrate-module additions that compose around the canonical five components (envelope_validator, event_validator, reconciler, enumeration_check, fixture_coverage) without becoming a sixth component per ADR-003 Consequence 1. Each row records the introducing story, the module's surface (Pydantic API + responsibility), and the documented rule the module encodes.

| Story | Module | Surface | Rule |
|---|---|---|---|
| 9.2 | `tools/loud-fail-harness/src/loud_fail_harness/project_type_detection.py` | `detect_project_type(DetectionRequest) -> DetectionOutcome` + `write_detected_project_type(project_root, project_type) -> WriteResult` | Deterministic top-level-only precedence: **mobile** (`ios/`+`android/` directories \| `pubspec.yaml` \| `app.json` with top-level `expo`/`react-native` key) → **web** (`package.json` deps/devDeps include `next`/`react`/`vue`/`nuxt`/`svelte`/`vite`/`@angular/core`/`astro`) → **api** (`package.json` deps/devDeps include `express`/`fastify`/`koa`/`@nestjs/core`/`hono`, OR `pyproject.toml` `[project].dependencies` / `[tool.poetry.dependencies]` includes `fastapi`/`flask`/`django`/`starlette`/`aiohttp`, OR `go.mod`/`Cargo.toml`/`pom.xml`/`build.gradle` exists) → **ambiguous** (mobile-plus-non-mobile conflict halts init via stderr diagnostic + non-zero exit, no marker) → **no-indicators** (no rule fired, halts identically). Pluggability-pure (FR62); imports stdlib + pydantic + `init_preconditions.ProjectType` only. Marker taxonomy v1 closed-set preserved (27 classes unchanged). |
