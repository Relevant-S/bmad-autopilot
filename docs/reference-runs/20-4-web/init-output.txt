$ /bmad-automation init

bmad-automation init: precondition checks (Story 7.3 / FR37 / FR38)
  ✓ claude-code: available, version 2.1.32 (>=2.0.0 required)
  ✓ bmad-core: available, version 6.2.2 (>=6.0 required)
  ✓ tea-module: available (total-block dependency satisfied)
  ✓ git: available, working tree clean
  ✓ python: available, 3.12.5 (>=3.12 required)
  ✓ node: available, 22.4.1 (>=22 required for playwright-mcp)
  ✓ playwright-mcp: available (probe via browser_navigate("about:blank") → 200)
  ✓ lad-mcp: configured + reachable (probe via `claude mcp list` → lad ✓ Connected);
      OPENROUTER_API_KEY env var present (NAME-only check per NFR-S1 — VALUE
      never read at the substrate-precondition seam; the upstream MCP server
      reads VALUE at server-startup time via the install-handle env-var flow).
      NOT skipped (no `LAD-skipped` marker fired at init time per Story 10.5
      AC-3 init-time discipline — the configured-AND-API-key-present path).
  ✓ qa-flakiness store: NOT a precondition-checked dependency — the per-AC
      pass/fail history at _bmad-output/qa-flakiness/{story-id}.yaml is created
      lazily on first QA run (Story 20.2; gitignored). No init-time probe.

bmad-automation init: project-type detection (Story 9.2 / FR-P1.5-2)
  ✓ detected project_type=web via top-level-only precedence
    (sample-qa-coverage-001 — the established e-commerce cart/checkout
     synthetic surface reused as the Epic-20 web surface per Story 20.4 Dev
     Notes; the plan re-derivation cross-check + flakiness accumulation are
     project-type-agnostic, so a single web run witnesses both FRs completely)
  ✓ wrote project_type: web to _bmad/automation/config.yaml
    (write_detected_project_type via Pattern 4 atomic tempfile+os.replace)

bmad-automation init: scaffolding sample story (Story 7.4 / FR39)
  ✓ proceed-merge-stable; existing sample-qa-coverage-001.md detected;
    canonical fields preserved per Story 7.6 non-destructive guard
    (FR41 — no overwrite of user customizations).

bmad-automation init: scaffolding config + qa-runbook stubs (Story 7.5 / FR40)
  ✓ wrote _bmad/automation/config.yaml with documented defaults
    + review_lad section: enabled: true; api_key_env_var: OPENROUTER_API_KEY
      (the Phase 1.5 LAD block per ADR-008 + Story 10.4 AC-1).
  ✓ wrote _bmad/automation/qa-runbook.yaml with the Epic-19 opt-in blocks
    carried forward (all enabled — the 19.1/19.2 SHIP-ALL-ON posture) AND
    NO `flakiness:` block — its ABSENCE means the Story 20.3 defaults apply
    (threshold_consecutive_runs: 3; threshold_transient_fail_count: 1).
    Absence is NOT a marker (FR42 user-owned-file discipline):
      heuristics:
        web:
          empty-state: enabled
          error-state: enabled
          auth-boundary: enabled
          rate-limit-boundary: enabled
          locale-i18n-edge: enabled
          large-input-boundary: enabled
          permission-boundary: enabled
      a11y:
        enabled: true
        delta_threshold: 0
        scope: per-ac
      visual_regression:
        enabled: true
        delta_threshold: 0
        scope: per-ac
        baseline_storage: gitignored
      # (no `flakiness:` block — defaults 3 / 1 apply per Story 20.3)
  ✓ FR41 non-destructive guard: no existing user config overwritten

bmad-automation init: TEA-boundary first-run orientation (Story 7.8 / FR34)
  → TEA validates per-AC behavioral plans; the Automator exercises the running
    product against acceptance criteria. The two are complementary, not
    redundant. See docs/tea-vs-automator.md for the full boundary contract.

bmad-automation init: complete. Run `/bmad-automation run sample-qa-coverage-001`
to exercise the Epic-20 LAD-enabled web loop (Phase 2 / Story 20.4 — per-run
plan re-derivation cross-check + longitudinal flakiness accumulation).
