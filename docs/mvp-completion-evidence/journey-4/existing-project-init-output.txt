$ /bmad-automation init  # on existing BMAD project (PRD framing — bail-back)

bmad-automation init: precondition checks (Story 7.3)
  ✓ all hard-dependency preconditions met (FR37 / FR38)

bmad-automation init: non-destructive guard (Story 7.6 / FR41)
  → existing _bmad-output/ detected (10 story files, 6 epic files)
  → guard verdict: ALLOW (no overwrite of user-owned content)
  → no marker emitted (intentional override path skipped — install IS
    the intended action; FR41 non-destructive guarantee preserved)

bmad-automation init: scaffolding sample story (Story 7.4 / FR39)
  → sample-auto-001.md already exists; preserve user content
  → no overwrite per FR41

bmad-automation init: config + qa-runbook stubs (Story 7.5 / FR40)
  → existing _bmad/automation/config.yaml detected
  → preserving user customizations per FR42 (re-run preserves config)

bmad-automation init: TEA-boundary first-run orientation (Story 7.8 / FR34)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ◆ TEA boundary clarification (one-time message; FR34)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  TEA validates per-AC behavioral plans authored by the test
  architect (AC writer's intent). The Automator EXERCISES the
  running product against acceptance criteria via Dev → Review →
  QA loop. The two are complementary:
    - TEA: "is this the right test plan?"
    - Automator: "does the running product satisfy the AC?"
  See docs/tea-vs-automator.md for the boundary contract.
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

bmad-automation init: complete on existing project (no destruction).
