# Reference Projects (BMAD Agent Development Automator)

Per PRD Success Criteria verbatim (`_bmad-output/planning-artifacts/prd.md` line 475 — "2–3 BMAD projects of varying types (web, API, mobile) run the full loop each release. Concrete reference projects named in the repo README at launch."): this artifact indexes the BMAD reference projects the Automator's release-discipline is empirically evaluated against, with a per-project row pointing at the captured run record(s) for the most recent release. Phase 1 (MVP) shipped per-journey evidence captures via Story 8.7 under `docs/mvp-completion-evidence/journey-{1..4}/`; Phase 1.5 introduces the per-release reference-run pattern under `docs/reference-runs/<story-id>-<project-type>/` (the mobile row, Story 9.6, lands first; Phase 2+ will append additional rows per project type per release).

The discipline rule: append a new row when a new reference project is selected for an upcoming Phase 2+ release type; update the `Latest Run Record` cell in-place when a subsequent release captures a fresher run against an existing reference project.

## Per-project reference index

| Project | Project Type | Source | Latest Run Record | Reproducibility | First Run Story | Notes |
|---|---|---|---|---|---|---|
| `bmad-autopilot` (sample-auto-001 happy path) | `web` | `bmad-autopilot/` development workspace itself (Story 8.7 AC-3 option (b) stand-in extended to Phase 1.5 LAD per AC-1(b), and to Phase 2 epic scope per `epics-phase-2.md` line 454, and to Phase 2 sprint scope per `epics-phase-2.md` line 526) | [`docs/reference-runs/22-7-auto-merge-web/`](reference-runs/22-7-auto-merge-web/) | `committed-in-repo` | 8.7 | Phase 1's reference projects shared the canonical `sample-auto-001` fixture (Story 7.4 — `tools/loud-fail-harness/src/loud_fail_harness/_data/sample-auto-001.md`) across project types; the journey-1 capture exercised the GET /hello endpoint shape mapping to both web and api at MVP. Phase 1.5 LAD-enabled run (Story 10.7) captured at `docs/reference-runs/10-7-lad-web/`; the Phase 1 baseline run record at `docs/mvp-completion-evidence/journey-1/` is preserved for the pre-LAD comparison. The Phase 1.5 run produced 12 LAD-source findings via a real `mcp__lad__code_review` invocation against the combined Stories 10.4 + 10.5 substrate (1279 lines) — the sycophancy-escape value claim's first empirical witness (all 12 findings LAD-only per the per-finding 3-layer-would-have-missed analysis in `docs/reference-runs/10-7-lad-web/narrative.md` § LAD-only finding analysis). Live re-capture against an external reference project is forward-scoped to Phase 2 per Story 7.1's plugin-primitive stability spike. Phase 1 patch Story 13.7 captured the FR22c-active web run at `docs/reference-runs/13-7-web/` (within-AC flow-branch coverage — enumerated `flow_branches[]` per AC, per-`must-visit`-branch Tier-1/Tier-2 evidence, and `heuristic-skipped: flow-branch-<id>` markers for `intentionally-skipped` branches; the LAD-enabled 4-layer review posture is preserved); `docs/reference-runs/10-7-lad-web/` is preserved unmodified as the historical pre-FR22c web capture (the genuine 12-finding LAD-findings record). Phase 2 Story 15.5 captured the epic-scope web run at `docs/reference-runs/15-5-epic-web/` (the `Latest Run Record` cell migrated here in-place per AC-5) — a synthetic multi-story epic (`epic-915`) driven end-to-end through `epic_lifecycle.run_epic_loop` to `epic-complete` with the per-epic cost partition + running (mid-epic) and close epic-PR-bundle snapshots witnessed; injected-runner determinism stands in for live per-story dispatch, the epic-orchestration substrate is genuinely exercised (incl. ≥1 real worktree create→cleanup); `docs/reference-runs/13-7-web/` is preserved unmodified as the historical per-story FR22c capture. Phase 2 Story 16.4 captured the sprint-scope web run at `docs/reference-runs/16-4-sprint-web/` (the `Latest Run Record` cell migrated here in-place per AC-7) — a synthetic multi-epic sprint (`sprint-916-ref` over `epic-916` + `epic-917` with three stories) driven end-to-end through `sprint_lifecycle.run_sprint_loop` to `sprint-complete` with the read-only `status --sprint` render (sprint→epics→stories tree, aggregate cost, per-sprint retry budget, escalation rate, scoped active-markers) + the 16.3 sprint-status artifact assembled from the SAME caches; injected-runner determinism stands in for live per-epic/per-story dispatch, the sprint-orchestration substrate is genuinely exercised (incl. ≥1 — here BOTH — epic units driving a real nested `run_epic_loop`); `docs/reference-runs/15-5-epic-web/` is preserved unmodified as the historical epic-scope capture. Phase 2 Story 18.4 captured the parallel-mode (epic-scope) web run at `docs/reference-runs/18-4-parallel-web/` (the `Latest Run Record` cell migrated here in-place per AC-6) — a synthetic 2-story epic (`epic-918` with `918-1-alpha` / `918-2-bravo`) driven end-to-end through `epic_lifecycle.run_epic_loop(parallel_stories: true, max_parallel_stories: 2)` to `epic-complete` with both stories proven concurrent (a loud-fail-bounded `threading.Barrier(2)` rendezvous; peak observed-concurrency 2), zero orphan worktrees + no `worktree-stale-lock` persisted, the clean-run no-pollution positive witness (the production disjoint-claim provider keeps Story 18.2's live detector silent) + a separate colliding-provider negative witness (`parallel-story-state-pollution: shared-port-collision` fires + `epic-paused-on-escalation` + in-flight drain), and the per-story (= per-worktree) + per-epic cost partition (`epic_cost_total == sum(per_story_cost)`); injected-runner determinism + the barrier rendezvous stand in for live per-story dispatch, the parallel-dispatch + worktree-isolation + disjoint-provider + pollution-detection substrate is genuinely exercised (incl. a latent seed-ordering defect this FIRST real `run_epic_loop(parallel_stories=True)` + real-git integration surfaced and fixed as a contract-pair — the carrier write was deferred to the new `ClaimCarrierSeed`/`seed_carrier` seam invoked after `create_worktree`); `docs/reference-runs/16-4-sprint-web/` is preserved unmodified as the historical sprint-scope capture. Phase 2 Story 19.6 captured the Epic-19 all-capabilities web run at `docs/reference-runs/19-6-web/` (the `Latest Run Record` cell migrated here in-place per AC-10) — the QA-coverage-expansion witness: the full 7-heuristic sweep (5 applicable-with-evidence + `heuristic-skipped: rate-limit-boundary`/`permission-boundary` for the two structurally-inapplicable, both among the four 19.2-added sub_classifications), the web-only a11y audit (first-run `a11y-baseline-stale` per AC), and the visual-regression audit (first-run `visual-regression-baseline-missing` per AC), all surfaced on the `qa-envelope.yaml` Epic-19 arrays that validate against the unmodified `schemas/envelope.schema.yaml`; the LAD-enabled 4-layer review posture is preserved and per-specialist cost stays under the NFR-P1 ceiling (the empirical confirmation of the 19.1/19.2 SHIP-ALL-ON activation gate); `docs/reference-runs/18-4-parallel-web/` is preserved unmodified as the historical parallel-mode capture. Phase 2 Story 20.4 captured the Epic-20 QA-independence + flakiness web run at `docs/reference-runs/20-4-web/` (the `Latest Run Record` cell migrated here in-place per AC-9) — the QA-independence-restoration witness: the FR-P2-9 per-run plan re-derivation cross-check on BOTH branches (`green` on the clean run-003 reuse-existing path co-located with the RETAINED FR25 compromise note + `plan-rederivation-drift-detected` on a run-004 follow-on after a qa-runbook mutation declared `rate-limit-boundary` applicable to AC-2, `drift_surfaces: [heuristic_applicability]`, persisted plan NOT overwritten), and the FR-P2-8 longitudinal flakiness corpus accumulating ≥3 consecutive transient-fail runs on AC-2 → `flakiness-threshold-exceeded` (with AC-1 a clean silent control), backed by a committed `qa-flakiness-log.yaml` snapshot of the gitignored live store; both QA envelopes + the corpus validate against the unmodified schemas AND are CI-pinned by the maintainer-ratified reference-replay test `tools/loud-fail-harness/tests/test_reference_run_20_4.py` (the deterministic-substrate strengthening over 19.6's Debug-Log-only proof); the LAD-enabled 4-layer posture is preserved and neither Epic-20 surface adds a cost row. `docs/reference-runs/19-6-web/` is preserved unmodified as the historical pre-20.4 Epic-19 QA-coverage capture. No mobile/api Epic-20 capture — both surfaces are project-type-agnostic, so a second-type directory adds no net-new witness (AC-9 scoping decision). Phase 2 Story 21.2 captured the Epic-21 background-execution web run at `docs/reference-runs/21-2-background-web/` (the `Latest Run Record` cell migrated here in-place per AC-8) — the FR-P2-7 `partial`-surface witness (Story 21.1 spike verdict `partially-stable` → path `partial`): `background_execution`-gated dispatch via the daemon-backed `claude --bg` primitive (NOT the in-session `Agent run_in_background` path the spike rejected per `#63023`), with the inline foreground loop NOT taken on the flag-on path and bit-identical foreground behavior on the flag-off default; git-ground-truth-reconciled `/bmad-automation status --background-agents-json` classifying each `claude agents --json` run `in-flight` / `completed-confirmed` / `unconfirmable` and emitting the loud-fail `background-primitive-unstable` marker (taxonomy PATCH bump 1.17→1.18, closed-set 41→42; `dependencies.yaml` MINOR bump 1.7→1.8 for the `background-primitive` opt-in-skip entry) EXACTLY on the unconfirmable-on-resume path; a stand-in capture (the dispatch-seam construction + reconciliation classification + marker emission are genuinely witnessed by `tests/test_epic_21_background_reference_run_fixture.py` driving the real `make_git_ground_truth_probe` against a real `tmp_path` git repo with injected loop/launcher stubs — a live `claude --bg` daemon round-trip is out of scope for a deterministic CI fixture). The unqualified silent-survival guarantee is DEFERRED with a named revisit trigger (NO `deferred-work.md` entry — verdict not `unstable`; the deferral lives in the spike artifact + the extension-audit revisit condition). Project-type-agnostic (the dispatch + status surfaces are orchestrator/status concerns), so no mobile/api capture. `docs/reference-runs/20-4-web/` is preserved unmodified as the historical pre-21.2 Epic-20 capture. Phase 2 Story 22.7 captured the auto-merge draft→ready round-trip web run at `docs/reference-runs/22-7-auto-merge-web/` (the `Latest Run Record` cell migrated here in-place per AC-7) — the FIRST genuine landed-commit witness for the system's first actuator: `gh pr ready <branch>` fired BEFORE `gh pr merge --squash <branch>` so an armed `enabled AND gate-green AND done` conjunction lands a REAL squashed commit on `main` instead of always skipping with `auto-merge-skipped: merge-failed` on Phase-1's draft PRs (Epic 17 retro Action #1 — the readiness step is loud-fail, surfacing the new `ready-failed` sub-classification, taxonomy PATCH bump 1.20→1.21 with the 44-class closed-set preserved, and NEVER attempting the merge after a readiness failure). UNLIKE every prior reference run, this surface is remote-mutating and cannot be witnessed against the in-repo synthetic surface; the genuine `gh pr ready`→`gh pr merge --squash` round-trip is captured by the committed, runnable `capture-real-round-trip.sh` (which runs the actuator's exact two `gh` calls) and is **maintainer-executed** — until `run-output.txt` is committed, Epic 23 must NOT harvest this as the genuine witness (AC-7 rejects injected-runner-only); the orchestrator arming decision is unit-tested (staged), the live `gh` round-trip is the genuinely-witnessed portion. `docs/reference-runs/21-2-background-web/` is preserved unmodified as the historical pre-22.7 Epic-21 capture. Forward consumer: Story 23.2 (`phase-2-completion-evidence.md`). |
| `bmad-autopilot` (sample-auto-001 happy path) | `api` | `bmad-autopilot/` development workspace itself (Story 8.7 AC-3 option (b) stand-in) | [`docs/mvp-completion-evidence/journey-1/`](mvp-completion-evidence/journey-1/) | `committed-in-repo` | 8.7 | Per AC-2(b) default for Phase 1: the GET /hello endpoint in `sample-auto-001` is canonically an HTTP API surface; journey-1 serves as both web and api reference at the MVP cut. Phase 2+ may decouple the rows when a dedicated API-only reference project is selected. |
| `bmad-autopilot` (mobile reference run, Story 9.6) | `mobile` | `bmad-autopilot/` development workspace itself (Story 8.7 AC-3 option (b) stand-in extended to Phase 1.5) | [`docs/reference-runs/19-6-mobile/`](reference-runs/19-6-mobile/) | `committed-in-repo` | 9.6 | The captured artifacts describe the mobile-MCP-driven journey conceptually and cite the canonical Story 9.3 substrate (`mobile_driver.py` ten-method `MobileDriver` Protocol + mobile-mcp v0.0.54 `@mobilenext/mobile-mcp` tool surface per ADR-007) + Story 9.4 mobile heuristics (`mobile_heuristic_spec.py` three-entry `MOBILE_HEURISTIC_SPECS` closed table) + Story 9.5 `mobile-blocked` sub-classifications (init-unavailable / mid-run-unavailable). Live re-capture against a real maintainer-owned mobile project (PRD line 815 verbatim — "practitioner-actually-useful, not synthetic demo") is forward-scoped to Phase 2 / Story 11.2 (`_bmad-output/planning-artifacts/epics-phase-1.5.md` line 357 — "Mobile + LAD Reference-Project Run Records Populated") when the Automator runtime is deployable to a target user mobile project. Phase 1 patch Story 13.7 captured the FR22c-active mobile run at `docs/reference-runs/13-7-mobile/` (within-AC flow-branch coverage — enumerated `flow_branches[]` per AC, per-`must-visit`-branch Tier-1/Tier-2 mobile-MCP evidence, and `heuristic-skipped: flow-branch-<id>` markers for `intentionally-skipped` branches); `docs/reference-runs/9-6-mobile/` is preserved unmodified as the historical pre-FR22c mobile capture. Phase 2 Story 19.6 captured the Epic-19 mobile QA-coverage run at `docs/reference-runs/19-6-mobile/` (the `Latest Run Record` cell migrated here in-place per AC-10) — the 6-of-7 mobile heuristic subset (5 applicable-with-evidence + `heuristic-skipped: permission-boundary`; `rate-limit-boundary` SILENTLY matrix-excluded per ADR-010 — NO marker) + the visual-regression audit (first-run `visual-regression-baseline-missing` per AC, via the mobile-mcp `mobile_take_screenshot` surface), with NO a11y (web-only per NFR-I3); three-layer LAD-disabled review, per-specialist cost under the NFR-P1 ceiling; the `qa-envelope.yaml` Epic-19 arrays validate against the unmodified `schemas/envelope.schema.yaml`; `docs/reference-runs/13-7-mobile/` is preserved unmodified as the historical FR22c-active mobile capture. Forward consumer: Story 23.2 (`phase-2-completion-evidence.md`, including the H8 live mobile re-capture). |

## Cross-references

- **Story 8.7** (`docs/mvp-completion-evidence.md` + `docs/mvp-completion-evidence/journey-{1..4}/`) — the Phase 1 closing-evidence artifact that established the per-journey-directory shape THIS file's mobile row inherits via `docs/reference-runs/9-6-mobile/`. All four Phase 1 journey narratives explicitly cite "Story 8.7 AC-3 option (b)" stand-in posture; THIS file's three rows continue that posture.
- **Story 11.2** (`_bmad-output/planning-artifacts/epics-phase-1.5.md` line 357 — "Mobile + LAD Reference-Project Run Records Populated"; lines 363–370 detail) — the forward consumer that reads THIS file when populating the corresponding rows in `phase-1.5-completion-evidence.md`. Story 11.2 will append a `mobile` row AND a `web + LAD` row to the Phase 1.5 completion-evidence artifact, sourcing the data via THIS file's `Latest Run Record` cells (mobile row → `docs/reference-runs/9-6-mobile/`; web row's Phase-1.5 Latest Run Record → `docs/reference-runs/10-7-lad-web/`).
- **Story 10.7** (`_bmad-output/implementation-artifacts/10-7-reference-lad-enabled-run-fixture-end-to-end.md`) — the Phase 1.5 LAD-enabled reference run; reference run record under `docs/reference-runs/10-7-lad-web/`; Story 11.2 forward-consumer reads this directory's `pr-bundle.md` + cost section + marker bundle when populating `phase-1.5-completion-evidence.md`'s LAD-enabled row.
- **PRD Risk Mitigation** (`_bmad-output/planning-artifacts/prd.md` line 815 verbatim — "reference projects should be *actively useful* projects (ideally ones the maintainer already uses), not synthetic demos") — the discipline rule the maintainer applies when selecting future Phase 2+ reference projects; synthetic placeholder apps are explicitly rejected.
- **Regeneration discipline** — append-only at the row level: a NEW row is added when a NEW reference project is selected for an upcoming release type (e.g., a `desktop` row when desktop-MCP support lands post-Phase-1.5). The `Latest Run Record` cell is updated in-place per release when a subsequent release captures a fresher run against an EXISTING reference project (e.g., when Phase 2's mobile run captures fresher artifacts against a real maintainer-owned mobile project, that row's `Latest Run Record` pointer migrates from `reference-runs/9-6-mobile/` to `reference-runs/<phase-2-story-id>-mobile/`). The `First Run Story` cell is immutable — it always names the story that captured the first run record for that project type.
