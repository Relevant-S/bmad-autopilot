# Canonical positive `specialist-dispatched` event.
#
# Sources:
#   - PRD ADR-001 Consequence 1 (orchestrator emits a schema-validated event
#     at every seam transition; specialist dispatch is one such seam)
#   - Story 1.3 (substrate component 2 — wire-up proof for the event-validator
#     CI gate; the full canonical event-fixture set lands incrementally as
#     later stories produce events)
#
# Per Pattern 3, event class names are kebab-case; field names within events
# are snake_case. Validates against schemas/orchestrator-event.yaml.

event_class: specialist-dispatched
event_id: ev-1-3-seed-0001
timestamp: "2026-04-26T00:00:00Z"
story_id: "1.3"
specialist: dev
prompt_id: prompt-seed-0001
retry_attempt: 0
