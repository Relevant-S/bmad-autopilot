# Review-BMAD specialist envelope — status=pass, single-layer Acceptance Auditor success.
#
# Source list: PRD Reference Artifacts (line 653) names five canonical envelopes.
# This fixture lands with Story 2.9 (minimal Review-BMAD-wrapper subagent, single-layer)
# to extend the Story 1.2 + Story 2.8 corpus with the Review-BMAD happy-path shape:
# the Acceptance Auditor layer ran successfully and emitted a low-severity deferred
# finding the orchestrator's flow policy ignores per FR9 (bucket=defer routes to
# no retry).
#
# The third PRD-canonical Review-BMAD fixture (`review-patch-findings.yaml`) is
# OUT of THIS story's scope and lands when a downstream story (likely Story 3.1
# or Story 5.2) first needs it as a fixture, per the Story 1.2 strategy "the rest
# land with whichever story first needs them as fixtures".
#
# Validated against schemas/envelope.schema.yaml by the harness CI gate
# (envelope-validator).

status: pass
artifacts:
  - bmad-autopilot/_bmad-output/implementation-artifacts/2-9-minimal-review-bmad-wrapper-subagent-single-layer.md
findings:
  - id: review-001
    source: auditor
    title: Story doc references epics.md line numbers verbatim
    detail: >-
      Acceptance Auditor noted the story doc's References block hardcodes
      epics.md line numbers; flagged as informational only since BMAD-core's
      story-doc convention permits line references and the orchestrator's
      flow policy does not act on bucket=defer findings.
    location: bmad-autopilot/_bmad-output/implementation-artifacts/2-9-minimal-review-bmad-wrapper-subagent-single-layer.md:1
    bucket: defer
    severity: LOW
rationale: >-
  Acceptance Auditor reviewed the story-doc acceptance criteria against the
  implementation; one low-severity informational finding deferred. Layer ran
  to completion; no AC violations escalated to the patch bucket.
failed_layers: []
