# QA specialist envelope — status=pass, AC-1-only Tier-1-evidence-only Epic 2 scope.
#
# Source list: PRD Reference Artifacts (line 653) names the canonical envelope
# corpus. This fixture lands with Story 2.10 (minimal QA subagent, AC-1 only,
# Tier-1 evidence only) to extend the Story 1.2 + Story 2.8 + Story 2.9 corpus
# with the QA happy-path shape: AC-1 verified mechanically, one Tier-1 evidence
# artifact captured under _bmad-output/qa-evidence/{story-id}/{run-id}/, the
# ac_results array carries exactly one entry per the verbatim Epic-2 commitment.
#
# The third PRD-canonical QA fixture (`qa-blocked-env-setup.yaml`) is OUT of
# THIS story's scope and lands with Epic 4 Story 4.10 when env-setup escalation
# contracts land, per the Story 1.2 strategy "the rest land with whichever
# story first needs them as fixtures".
#
# Validated against schemas/envelope.schema.yaml by the harness CI gate
# (envelope-validator).

status: pass
artifacts:
  - _bmad-output/qa-evidence/sample-001/run-2026-04-29-001/ac1-http-200.log
findings: []
rationale: >-
  AC-1 verified mechanically: HTTP POST to /healthz returned 200 within the
  expected timeout; one Tier-1 evidence artifact captured under the canonical
  qa-evidence path. No AC violations observed.
ac_results:
  - ac_id: AC-1
    status: pass
    assertions:
      - HTTP POST /healthz returned status code 200
    evidence_refs:
      - path: _bmad-output/qa-evidence/sample-001/run-2026-04-29-001/ac1-http-200.log
        tier: tier-1-mechanical
    semantic_verification: not_applicable
